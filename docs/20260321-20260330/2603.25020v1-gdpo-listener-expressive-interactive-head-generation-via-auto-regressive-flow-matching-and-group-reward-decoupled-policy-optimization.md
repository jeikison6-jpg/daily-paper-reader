---
title: "GDPO-Listener: Expressive Interactive Head Generation via Auto-Regressive Flow Matching and Group reward-Decoupled Policy Optimization"
title_zh: GDPO-Listener：通过自回归流匹配和分组奖励解耦策略优化实现表现力交互式头部生成
authors: "Zhangyu Jin, Maksim Siniukov, Deuksin Kwon, Ashutosh Chaubey, Mohammad Soleymani"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25020v1"
tags: ["query:flgrpo"]
score: 10.0
evidence: 自回归流匹配与组奖励解耦策略优化
tldr: 针对虚拟人交互中听者动作易陷入“均值回归”导致面部僵硬的问题，本文提出GDPO-Listener框架。该框架结合自回归流匹配（AR-FM）架构与分组奖励解耦策略优化（GDPO），通过对FLAME参数组进行独立奖励归一化，显著提升了生成动作的表达力与多样性，并支持语义文本控制，在多个数据集上表现优异。
selection_source: fresh_fetch
motivation: 现有的交互式头部生成方法在处理听者反应时常因均值回归问题导致动作僵硬，且缺乏复杂非语言动作的表达空间。
method: 提出一种自回归流匹配架构以实现稳定学习，并结合分组奖励解耦策略优化（GDPO）来显式激励高方差的表达性生成。
result: 在Seamless Interaction和DualTalk数据集上的实验表明，该方法在运动方差、视觉表现力和语义可控性方面均优于现有基准。
conclusion: GDPO-Listener通过创新的架构与优化策略，有效解决了交互式头部生成中的表达力不足问题，实现了高度拟人的动态交互。
---

## 摘要
为双人交互生成逼真的 3D 头部运动是虚拟人合成中的一个重大挑战。虽然最近的方法在说话人头部生成方面取得了显著成果，但它们在听者运动中经常遇到“回归均值”问题，导致运动塌陷为静态面部，且缺乏复杂非语言运动的参数空间。在本文中，我们提出了 GDPO-Listener，这是一个能够实现高表现力说话和听觉运动生成的创新框架。首先，我们引入了一种自回归流匹配架构，以实现稳定的监督学习。其次，为了克服运动学上的静止，我们应用了分组奖励解耦策略优化（GDPO）。通过在不同的 FLAME 参数组之间隔离奖励归一化，GDPO 显式地激励了具有高方差的表现力生成。最后，我们实现了显式的语义文本控制，以支持可定制的响应。在 Seamless Interaction 和 DualTalk 数据集上的广泛评估表明，与现有基准相比，该方法在长期运动学方差、视觉表现力和语义可控性方面表现出更优越的性能。

## Abstract
Generating realistic 3D head motion for dyadic interactions is a significant challenge in virtual human synthesis. While recent methods achieve impressive results with speaking heads, they frequently suffer from the `Regression-to-the-Mean' problem in listener motions, collapsing into static faces, and lack the parameter space for complex nonverbal motions. In this paper, we propose GDPO-Listener, a novel framework that achieves highly expressive speaking and listening motion generation. First, we introduce an Auto-Regressive Flow Matching architecture enabling stable supervised learning. Second, to overcome kinematic stillness, we apply the Group reward-Decoupled Policy Optimization (GDPO). By isolating reward normalization across distinct FLAME parameter groups, GDPO explicitly incentivizes high variance expressive generations. Finally, we enable explicit semantic text control for customizable responses. Extensive evaluations across the Seamless Interaction and DualTalk datasets demonstrate superior performance compared to existing baselines on long-term kinematic variance, visual expressivity and semantic controllability.

---

## 论文详细总结（自动生成）

### GDPO-Listener 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
在虚拟人合成领域，生成逼真的双人交互（Dyadic Interaction）3D 头部运动具有极高挑战性。
*   **核心痛点：** 现有的听者（Listener）头部生成模型普遍存在**“均值回归”（Regression-to-the-Mean）**问题。由于听者的反应与说话者的语音是“一对多”的非确定性映射，传统的监督学习（如 MSE 损失）会导致模型倾向于预测所有可能反应的平均值，最终表现为面部僵硬、静态且缺乏生气。
*   **功能缺失：** 现有方法往往限制了 FLAME 参数空间（如缺少眨眼、点头），且缺乏显式的语义控制（如无法根据对话内容调整情绪）。
*   **研究目标：** 提出一种能够生成高表现力、长序列稳定且具备语义可控性的说话/听者头部生成框架。

#### 2. 方法论：核心思想与关键技术
该框架分为两个训练阶段，结合了生成式流匹配与强化学习优化：
*   **核心架构：自回归流匹配（AR-Flow）**
    *   使用 Transformer 结构的 VAE 学习连续的运动潜空间，避免离散量化带来的抖动。
    *   引入 **TMRoPE（时间对齐多模态旋转位置嵌入）**，解决不同频率输入（高频音频与低频运动）的跨模态对齐问题。
    *   采用自回归方式预测运动潜变量，支持无限长序列生成。
*   **关键技术：分组奖励解耦策略优化（GDPO）**
    *   **从 ODE 到 SDE：** 在后训练阶段，将确定性的常微分方程（ODE）采样转为随机微分方程（SDE），引入探索性噪声。
    *   **奖励解耦：** 针对 FLAME 参数中量级差异巨大的不同组（如表情、下颌、颈部、眼睑等），独立进行奖励归一化。这防止了大幅度动作（如头部旋转）掩盖微小但关键的动作（如眨眼）。
    *   **奖励函数设计：** 包含位置方差惩罚、速度方差惩罚、均值漂移惩罚和 MSE 损失，重点优化运动的动态分布而非逐帧对齐。
*   **可控性增强：**
    *   **语义控制：** 通过文本 Prompt（如“开心地笑”、“悲伤地低头”）引导生成。
    *   **强度控制：** 利用无分类器引导（CFG）缩放因子，在推理时无需重训即可调节动作的夸张程度。

#### 3. 实验设计
*   **数据集：**
    *   **Seamless Interaction：** 包含 718 小时训练集和 31.5 小时测试集的大规模双人交互数据集。
    *   **DualTalk：** 用于验证模型的泛化能力。
    *   **表现力子集：** 专门筛选出高方差运动的片段，以严格评估“均值回归”问题的解决情况。
*   **对比基准（Baselines）：** 包括 DiffposeTalk、L2L、ARTalk 和 DualTalk 等 SOTA 方法。
*   **评估指标：** 
    *   说话人：唇部顶点误差（LVE）、平均头部距离（MHD）。
    *   听者：侧重分布匹配，使用面部/姿态/下颌动态偏差（FDD/PDD/JDD）。

#### 4. 资源与算力
*   **硬件：** 使用了 **8 台 NVIDIA L40s GPU**。
*   **训练时长：** 
    *   第一阶段（监督学习）：50 个 Epoch，学习率 $10^{-4}$。
    *   第二阶段（GDPO 后训练）：2 个 Epoch，学习率 $5 \times 10^{-6}$。
*   **模型规模：** 核心可训练参数仅为 **19.3M**，远小于基准模型（如 DualTalk 的 647.3M），极其轻量化。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在两个大型数据集上进行了详尽的定量评估，并针对“表现力子集”进行了专项测试。
*   **消融实验：** 验证了 GDPO 优化、文本 Prompt 输入以及流匹配采样步数对最终效果的影响。
*   **用户研究：** 邀请了 27 名参与者进行 3 选 1 的强制选择测试，涵盖唇形同步、自然度、生动度和多样性四个维度。
*   **评价：** 实验设计全面，通过引入“表现力子集”和“长序列推理测试”有效地证明了模型在解决静态塌陷方面的优势，实验结果客观且具有说服力。

#### 6. 主要结论与发现
*   **克服均值回归：** GDPO 后训练显著提升了听者的运动方差，生成的反应不再是僵硬的“扑克脸”。
*   **语义一致性：** 引入文本控制后，模型能根据对话语境（如听到对方生病时表现出同情）生成正确的非语言反馈。
*   **长序列稳定性：** 相比基准模型随时间推移运动逐渐衰减至静态，GDPO-Listener 在 3600 帧以上的长序列中仍能保持动态活力。
*   **效率优势：** 在保持高性能的同时，参数量减少了约 30 倍，支持实时生成。

#### 7. 优点（亮点）
*   **创新的后训练范式：** 首次将分组奖励解耦的强化学习（GDPO）应用于 3D 面部动力学，解决了听者生成的本质难题。
*   **精细的参数空间：** 扩展了 FLAME 模型，支持眨眼和眼球运动，这在提升“社会存在感”方面至关重要。
*   **高度可控：** 结合了文本语义控制和 CFG 强度调节，为创作者提供了极大的灵活性。

#### 8. 不足与局限
*   **文本标注依赖：** 训练阶段依赖于视觉语言模型（如 Qwen3-VL）生成的自动标注，标注的准确性可能影响语义控制的上限。
*   **计算开销：** 虽然推理轻量，但 GDPO 训练阶段需要进行自回归采样（Rollout），计算图较大，对显存和训练策略有较高要求。
*   **极端动作伪影：** 当 CFG 缩放因子设置过高时，可能会产生超出物理极限的夸张动作或面部畸变。

（完）
