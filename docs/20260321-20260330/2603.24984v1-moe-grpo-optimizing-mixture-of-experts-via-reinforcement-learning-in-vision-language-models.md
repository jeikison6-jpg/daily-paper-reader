---
title: "MoE-GRPO: Optimizing Mixture-of-Experts via Reinforcement Learning in Vision-Language Models"
title_zh: MoE-GRPO：在视觉语言模型中通过强化学习优化混合专家模型
authors: "Dohwan Ko, Jinyoung Park, Seoung Choi, Sanghyeok Lee, Seohyun Lee, Hyunwoo J. Kim"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24984v1"
tags: ["query:flgrpo"]
score: 9.0
evidence: 在视觉语言模型中使用GRPO进行强化学习
tldr: 本研究针对视觉语言模型（VLM）中混合专家（MoE）架构的路由优化问题，提出了 MoE-GRPO 框架。传统 top-K 路由易导致专家过拟合及选择受限，该方法将专家选择建模为序列决策，利用组相对策略优化（GRPO）算法，通过奖励反馈学习自适应路由策略。此外，引入模态感知引导以增强训练稳定性。实验证明，该方法在多模态基准测试中表现优异，提升了专家选择的多样性与任务专业化水平。
selection_source: fresh_fetch
motivation: 传统的确定性 top-K 路由机制容易导致专家过拟合，且难以探索到最优的专家组合。
method: 提出基于强化学习的 MoE-GRPO 框架，结合 GRPO 算法和模态感知路由引导来优化专家的动态选择。
result: 在多个图像和视频多模态基准测试中，MoE-GRPO 的性能一致优于标准 top-K 路由及其变体。
conclusion: 通过强化学习优化路由策略能有效缓解专家过拟合，并实现更高效的任务级专家分工。
---

## 摘要
混合专家模型（MoE）已成为降低 Transformer 架构计算开销的一种有效方法，它通过为每个 token 稀疏地激活一部分参数，同时保持了较高的模型容量。这一范式最近已被扩展到视觉语言模型（VLM），从而在降低计算成本的同时实现了可扩展的多模态理解。然而，广泛采用的确定性 top-K 路由机制可能会忽略更优的专家组合，并导致专家过拟合。为了解决这一局限性并提高专家选择的多样性，我们提出了 MoE-GRPO，这是一个基于强化学习（RL）的框架，用于优化基于 MoE 的 VLM 中的专家路由。具体而言，我们将专家选择建模为一个序列决策问题，并使用组相对策略优化（GRPO）对其进行优化，使模型能够通过探索和基于奖励的反馈来学习自适应专家路由策略。此外，我们引入了一种模态感知路由引导，通过阻止路由器探索针对特定模态激活频率较低的专家，从而增强了训练的稳定性和效率。在多模态图像和视频基准测试上的大量实验表明，MoE-GRPO 通过促进更多样化的专家选择，始终优于标准的 top-K 路由及其变体，从而缓解了专家过拟合，并实现了任务级的专家专业化。

## Abstract
Mixture-of-Experts (MoE) has emerged as an effective approach to reduce the computational overhead of Transformer architectures by sparsely activating a subset of parameters for each token while preserving high model capacity. This paradigm has recently been extended to Vision-Language Models (VLMs), enabling scalable multi-modal understanding with reduced computational cost. However, the widely adopted deterministic top-K routing mechanism may overlook more optimal expert combinations and lead to expert overfitting. To address this limitation and improve the diversity of expert selection, we propose MoE-GRPO, a reinforcement learning (RL)-based framework for optimizing expert routing in MoE-based VLMs. Specifically, we formulate expert selection as a sequential decision-making problem and optimize it using Group Relative Policy Optimization (GRPO), allowing the model to learn adaptive expert routing policies through exploration and reward-based feedback. Furthermore, we introduce a modality-aware router guidance that enhances training stability and efficiency by discouraging the router from exploring experts that are infrequently activated for a given modality. Extensive experiments on multi-modal image and video benchmarks show that MoE-GRPO consistently outperforms standard top-K routing and its variants by promoting more diverse expert selection, thereby mitigating expert overfitting and enabling a task-level expert specialization.

---

## 论文详细总结（自动生成）

以下是对论文《MoE-GRPO: Optimizing Mixture-of-Experts via Reinforcement Learning in Vision-Language Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：混合专家模型（MoE）通过稀疏激活参数，在不增加计算开销的情况下提升了视觉语言模型（VLM）的容量。
*   **核心问题**：目前主流的 MoE 架构多采用**确定性 Top-K 路由机制**。这种贪婪选择方式存在两个弊端：
    1.  **缺乏探索**：限制了对更优专家组合的搜索，容易陷入局部最优。
    2.  **专家过拟合**：模型倾向于过度依赖一小部分专家，导致参数利用率不均且泛化能力受限。
*   **研究目标**：探索如何通过强化学习（RL）优化专家的动态路由策略，以实现更优的专家组合和任务专业化。

### 2. 方法论：MoE-GRPO 框架
*   **核心思想**：将 MoE 的专家选择建模为一个**序列决策问题**，并利用**组相对策略优化（GRPO）**算法，根据可验证的奖励（如回答正确性）来迭代优化路由策略。
*   **关键技术细节**：
    *   **双重优化目标**：
        *   **Token-GRPO**：优化 token 序列的生成质量，确保模型输出符合预期。
        *   **Gate-GRPO**：直接针对每一层的路由网络进行优化，通过奖励反馈增强高贡献专家的权重，抑制低贡献专家。
    *   **模态感知路由引导（Modality-Aware Router Guidance）**：为了缩小 RL 的搜索空间并提高稳定性，预先计算专家对“视觉”和“文本”token 的亲和力分数，在探索过程中屏蔽掉与当前模态极不相关的专家。
    *   **随机采样探索**：在训练阶段，通过多项分布采样（Multinomial Sampling）生成多个专家路由路径（Rollouts），利用组内相对奖励计算优势函数（Advantage），从而强化高奖励的路径。

### 3. 实验设计
*   **模型架构**：将 **InternVL3.5-1B** 转换为 MoE 版本（8 选 2 专家，总参数 2.9B，激活参数 1.3B）。此外在 CLIP-MoE 上验证了跨数据集泛化性。
*   **基准测试（Benchmarks）**：
    *   **图像理解**：MME, MMBench, MMStar, MMT-Bench, AI2D。
    *   **视频理解**：VideoMME, MLVU, LongVideoBench, MVBench。
*   **对比方法**：
    *   **Det-FT**：标准的确定性 Top-K 微调。
    *   **Stoch-FT-Multi**：基于多项分布采样的随机微调。
    *   **Stoch-FT-Noise**：在路由分数中加入高斯噪声的随机微调。
    *   **其他路由算法**：Expert Choice 路由、最优传输（Optimal Transport）路由。

### 4. 资源与算力
*   **硬件设备**：使用了 **4 × NVIDIA RTX Pro 6000 Blackwell Max-Q GPUs**。
*   **训练配置**：
    *   训练步数：25,000 步。
    *   Batch Size：4。
    *   训练时长：**约 1 天**。
    *   数据量：从 One-Thinker 中采样了 100K 条多轮视觉指令微调数据。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 个主流多模态基准测试，并进行了跨数据集（10 个目标数据集）和领域泛化（ImageNet 变体）实验。
*   **消融实验**：详细验证了 Token-GRPO 与 Gate-GRPO 的必要性，以及模态感知引导对收敛速度和稳定性的贡献。
*   **分析深度**：通过可视化专家激活分布、计算路由熵（Entropy）和 Jensen-Shannon 散度（JSD），深入分析了专家选择的多样性和任务级专业化。
*   **评价**：实验设计全面，对比了多种随机化变体和先进路由算法，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：MoE-GRPO 在 7/9 的基准测试中优于所有基线方法，平均准确率比标准 Top-K 提升了约 2.0%。
*   **缓解过拟合**：在跨数据集和领域泛化实验中，MoE-GRPO 显著优于确定性微调，证明了 RL 探索能有效防止模型对特定专家组合的过度拟合。
*   **专家多样性**：MoE-GRPO 显著提高了路由分布的熵（从 1.05 升至 1.82），使专家激活更加均衡且具有任务针对性。
*   **训练稳定性**：引入模态感知引导后，模型收敛更快且奖励波动的标准差更小。

### 7. 优点与亮点
*   **首创性**：据论文所述，这是首个将专家路由建模为序列决策并使用 RL（特别是 GRPO）进行优化的 VLM 研究。
*   **细粒度控制**：通过 Gate-GRPO 提供了层级的密集监督信号，比仅在输出端进行 RL 优化更高效。
*   **实用性**：引入的模态感知引导解决了 RL 在大规模搜索空间中训练不稳定的痛点。

### 8. 不足与局限
*   **奖励函数单一**：目前主要依赖基于准确率（Accuracy-based）的二值奖励，对于更复杂的开放式生成任务，可能需要更精细的奖励模型（Reward Model）。
*   **模型规模限制**：实验主要集中在 1B-3B 规模的模型上，尚未在大规模（如 70B+）MoE 模型上验证其扩展性。
*   **计算开销**：虽然推理成本不变，但训练时需要生成多个 Rollouts，相比标准微调，训练阶段的计算资源需求更高。

（完）
