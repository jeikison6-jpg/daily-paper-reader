---
title: Policy-based Tuning of Autoregressive Image Models with Instance- and Distribution-Level Rewards
title_zh: 基于实例级和分布级奖励的自回归图像模型策略微调
authors: "Orhun Buğra Baran, Melih Kandemir, Ramazan Gokberk Cinbis"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23086v1"
tags: ["query:flgrpo"]
score: 9.0
evidence: 通过组相对策略优化 (GRPO) 优化图像模型
tldr: 针对自回归图像生成模型在最大似然训练中缺乏质量与多样性优化的问题，本文提出一种基于强化学习（RL）的轻量化微调框架。通过引入创新的分布级Leave-One-Out FID奖励，结合实例级奖励（CLIP/HPSv2）和自适应熵正则化，有效解决了RL微调中常见的模式崩溃问题。实验证明，该方法在少量迭代内显著提升了LlamaGen等模型的生成质量与多样性，且无需分类器自由引导（CFG）即可达到竞争性能。
selection_source: fresh_fetch
motivation: 传统的自回归图像模型训练缺乏对生成质量和多样性的直接优化，且现有强化学习方法易导致输出多样性崩溃。
method: 提出一种基于GRPO算法的RL框架，结合实例级奖励与新颖的分布级LOO-FID奖励，并利用自适应熵正则化稳定多目标学习。
result: 在LlamaGen和VQGAN架构上仅需数百次迭代即可显著提升各项指标，且能在不使用CFG的情况下保持高性能并降低推理成本。
conclusion: 该研究证明了结合分布级与实例级奖励的策略微调能有效提升自回归图像生成的质量与多样性，为高效模型对齐提供了新思路。
---

## 摘要
自回归 (AR) 模型在图像生成方面非常有效，但其标准的最大似然估计 (MLE) 训练缺乏对样本质量和多样性的直接优化。虽然强化学习 (RL) 已被用于对齐扩散模型，但这些方法通常会遭遇输出多样性崩溃的问题。类似地，现有的针对 AR 模型的 RL 方法严格依赖于实例级奖励，通常以牺牲分布覆盖度为代价来换取质量。为了解决这些局限性，我们提出了一个轻量级的 RL 框架，将基于 token 的 AR 合成建模为马尔可夫决策过程 (MDP)，并通过群体相对策略优化 (GRPO) 进行优化。我们的核心贡献是引入了一种新颖的分布级留一法 FID (LOO-FID) 奖励；通过利用特征矩的指数移动平均，它显式地鼓励样本多样性，并防止策略更新过程中的模式崩溃。我们将此与复合实例级奖励（CLIP 和 HPSv2）相结合，以确保严格的语义和感知保真度，并使用自适应熵正则化项来稳定多目标学习。在 LlamaGen 和 VQGAN 架构上进行的大量实验表明，仅需几百次微调迭代，即可在标准质量和多样性指标上取得显著改进。结果还表明，即使不使用无分类器指导 (Classifier-Free Guidance)，该模型也可以通过更新产生具有竞争力的样本，从而避开其两倍的推理成本。

## Abstract
Autoregressive (AR) models are highly effective for image generation, yet their standard maximum-likelihood estimation training lacks direct optimization for sample quality and diversity. While reinforcement learning (RL) has been used to align diffusion models, these methods typically suffer from output diversity collapse. Similarly, concurrent RL methods for AR models rely strictly on instance-level rewards, often trading off distributional coverage for quality. To address these limitations, we propose a lightweight RL framework that casts token-based AR synthesis as a Markov Decision Process, optimized via Group Relative Policy Optimization (GRPO). Our core contribution is the introduction of a novel distribution-level Leave-One-Out FID (LOO-FID) reward; by leveraging an exponential moving average of feature moments, it explicitly encourages sample diversity and prevents mode collapse during policy updates. We integrate this with composite instance-level rewards (CLIP and HPSv2) for strict semantic and perceptual fidelity, and stabilize the multi-objective learning with an adaptive entropy regularization term. Extensive experiments on LlamaGen and VQGAN architectures demonstrate clear improvements across standard quality and diversity metrics within only a few hundred tuning iterations. The results also show that the model can be updated to produce competitive samples even without Classifier-Free Guidance, and bypass its 2x inference cost.

---

## 论文详细总结（自动生成）

### 论文总结：基于实例级和分布级奖励的自回归图像模型策略微调

#### 1. 核心问题与整体含义（研究动机和背景）
自回归（AR）图像生成模型（如 LlamaGen）虽然在扩展性上表现优异，但其传统的**最大似然估计（MLE）**训练目标与人类的感知质量或语义对齐并不直接相关。此外，虽然强化学习（RL）已被用于微调扩散模型，但往往会导致**输出多样性崩溃（Mode Collapse）**。
本文旨在探索一种轻量级的 RL 微调框架，通过同时引入**实例级（针对单张图像质量）**和**分布级（针对整体多样性）**的奖励，使 AR 模型在提升生成质量和语义对齐的同时，保持甚至增强样本的多样性，并尝试摆脱推理成本高昂的无分类器引导（CFG）技术。

#### 2. 方法论：核心思想与关键技术
论文将 AR 图像合成建模为马尔可夫决策过程（MDP），并采用**群体相对策略优化（GRPO）**算法进行微调。其核心技术包括：
*   **复合奖励函数**：结合了实例级奖励（CLIPScore 负责语义对齐，HPSv2 负责人类审美偏好）和新颖的分布级奖励。
*   **留一法 FID 奖励（LOO-FID）**：这是本文的核心创新。为了在微调中显式优化分布质量，作者引入了基于 Inception 特征的 FID 奖励。通过维护特征矩的**指数移动平均（EMA）**来平滑小批量数据的噪声，并计算单个样本加入/移除后对整体 FID 的贡献（Leave-One-Out），从而奖励那些有助于使生成分布接近真实分布的样本，有效防止模式崩溃。
*   **自适应熵正则化**：引入动态调整的熵奖励项，根据训练进度和当前 token 分布的熵值自动调节，确保模型在优化奖励的同时保持足够的探索空间，防止策略过早收敛到低熵状态。

#### 3. 实验设计
*   **数据集与场景**：主要在 **ImageNet 256×256** 类别条件生成任务上进行验证。
*   **Benchmark 与基准模型**：以 **LlamaGen (B/L/XL)** 和 **VQGAN** 为基础架构。
*   **对比方法**：
    *   预训练的基准模型（Baseline）。
    *   传统的 MLE 持续微调。
    *   **AR-GRPO**（仅使用实例级奖励的并发研究）。
    *   **VA-π**（另一种并发的变分策略对齐方法）。
*   **评估指标**：FID（分布质量）、IS（典型性）、CLIPScore（语义对齐）、Precision/Recall（质量与多样性平衡）以及人类偏好研究。

#### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA A100 GPU**。
*   **训练时长**：针对不同规模的骨干网络，完成 600 次迭代大约需要 **10 到 14 小时**。
*   **推理效率**：由于微调后的模型在不使用 CFG 的情况下也能达到极高水平，推理速度可提升约 **2 倍**（避开了 CFG 的双倍前向计算）。

#### 5. 实验数量与充分性
论文进行了较为详尽的实验验证：
*   **多规模验证**：涵盖了从 Base 到 XL 不同参数量的 LlamaGen 模型。
*   **消融实验**：分别验证了 CLIP 奖励、HPS 奖励、FID 奖励以及 KL 正则化、熵正则化的各自贡献。
*   **架构泛化性**：在 VQGAN 架构上也验证了方法的有效性。
*   **对比充分性**：与最新的并发工作（如 VA-π）进行了直接的量化对比，并包含 50k 样本的完整评估。
*   **主观评估**：进行了包含数百对图像的人类偏好测试，增强了结论的说服力。

#### 6. 主要结论与发现
*   **性能提升**：仅需几百次迭代，模型在 FID、IS 和 CLIPScore 上均显著优于 MLE 微调和原始模型。
*   **多样性保护**：LOO-FID 奖励显著提升了 Recall 指标，证明了其在防止 RL 微调导致的多样性崩溃方面的有效性。
*   **去 CFG 潜力**：微调后的模型在 **CFG=1.0（即无引导）** 时的表现甚至优于原始模型在 CFG=1.5 时的表现，这为低延迟图像生成提供了可能。
*   **效率优势**：相比于持续的 MLE 训练，策略微调在样本效率和最终性能上具有压倒性优势。

#### 7. 优点（亮点）
*   **创新的分布奖励**：首次在 AR 图像模型微调中引入 LOO-FID，解决了 RL 训练中长期存在的“奖励黑客”和多样性缺失问题。
*   **工程实用性**：LOO-FID 计算开销极低（单次迭代仅增加约 30ms），且能显著降低推理阶段的计算成本。
*   **稳定性设计**：通过 EMA 和自适应熵正则化，使多目标 RL 训练在大规模视觉模型上变得稳定可行。

#### 8. 不足与局限
*   **奖励模型依赖**：性能高度依赖于预训练奖励模型（如 CLIP 和 HPSv2）的准确性，若这些模型存在偏见，微调后的模型也会继承。
*   **任务局限**：目前主要集中在类别条件生成（Class-conditional），尚未在更复杂的长文本到图像（Text-to-Image）生成任务上进行大规模验证。
*   **超参数敏感性**：虽然论文给出了固定参数，但在不同任务或新奖励函数下，可能仍需精细调整各奖励项的权重。

（完）
