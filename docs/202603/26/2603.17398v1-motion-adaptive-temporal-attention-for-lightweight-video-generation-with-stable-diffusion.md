---
title: Motion-Adaptive Temporal Attention for Lightweight Video Generation with Stable Diffusion
title_zh: 基于 Stable Diffusion 的轻量级视频生成的运动自适应时间注意力机制
authors: "Rui Hong, Shuxue Quan"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17398v1"
tags: ["query:rl-vg"]
score: 9.0
evidence: 用于时间相干性的运动自适应注意力
tldr: "本研究提出一种基于冻结 Stable Diffusion 的轻量级视频生成方法，核心是运动自适应时间注意力机制。该机制根据运动强度动态调整感受野：高运动序列采用局部注意力以保留细节，低运动序列采用全局注意力以增强一致性。通过在 UNet 中注入仅 25.8M（约 2.9%）的可训练参数，并结合时间相关噪声初始化，该方法在 WebVid 数据集上实现了极具竞争力的生成效果，且无需额外的显式时间一致性损失。"
selection_source: fresh_fetch
motivation: 旨在解决现有视频生成模型参数量大、计算成本高，且难以在保持场景一致性的同时捕捉快速运动细节的问题。
method: 提出运动自适应时间注意力机制，在 UNet 不同层级联注入全局和局部注意力模块，并结合运动感知门控与时间相关噪声初始化。
result: "仅需 25.8M 额外参数（占基准 UNet 的 2.9%），在 100K 视频训练下即可在 WebVid 验证集上达到领先的生成质量。"
conclusion: 实验证明标准的去噪目标足以提供隐式时间正则化，且噪声相关性与运动幅度之间的权衡为推理阶段提供了灵活的生成控制。
---

## 摘要
我们提出了一种基于冻结的 Stable Diffusion 模型的运动自适应时间注意力机制，用于参数高效的视频生成。我们的方法并非统一处理所有视频内容，而是根据估计的运动内容动态调整时间注意力的感受野：高运动序列在帧间进行局部注意力以保留快速变化的细节，而低运动序列则进行全局注意力以增强场景一致性。我们通过级联策略将轻量级时间注意力模块注入所有 UNet Transformer 块中——在下采样和中间块中使用全局注意力以实现语义稳定，在上采样块中使用运动自适应注意力以进行细粒度细化。结合时间相关噪声初始化和运动感知门控，该系统仅增加了 25.8M 个可训练参数（占基础 UNet 的 2.9%），在 100K 视频上训练后，在 WebVid 验证集上取得了具有竞争力的结果。我们证明了仅标准去噪目标就能提供足够的隐式时间正则化，其表现优于添加显式时间一致性损失的方法。我们的消融研究揭示了噪声相关性与运动幅度之间明显的权衡，为多样化的生成行为提供了实用的推理阶段控制。

## Abstract
We present a motion-adaptive temporal attention mechanism for parameter-efficient video generation built upon frozen Stable Diffusion models. Rather than treating all video content uniformly, our method dynamically adjusts temporal attention receptive fields based on estimated motion content: high-motion sequences attend locally across frames to preserve rapidly changing details, while low-motion sequences attend globally to enforce scene consistency. We inject lightweight temporal attention modules into all UNet transformer blocks via a cascaded strategy -- global attention in down-sampling and middle blocks for semantic stabilization, motion-adaptive attention in up-sampling blocks for fine-grained refinement. Combined with temporally correlated noise initialization and motion-aware gating, the system adds only 25.8M trainable parameters (2.9\% of the base UNet) while achieving competitive results on WebVid validation when trained on 100K videos. We demonstrate that the standard denoising objective alone provides sufficient implicit temporal regularization, outperforming approaches that add explicit temporal consistency losses. Our ablation studies reveal a clear trade-off between noise correlation and motion amplitude, providing a practical inference-time control for diverse generation behaviors.

---

## 论文详细总结（自动生成）

以下是对论文《Motion-Adaptive Temporal Attention for Lightweight Video Generation with Stable Diffusion》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保持预训练图像扩散模型（如 Stable Diffusion）生成质量的同时，以极低的计算成本和参数量实现时间连贯且运动自然的视频生成。
*   **研究背景**：现有的视频生成方法要么需要全量微调（成本极高），要么采用统一的时间注意力机制（如 AnimateDiff），忽略了不同运动强度（如快速奔跑与静止风景）对时间建模需求的差异。
*   **动机**：提出一种“运动自适应”的轻量级方案，通过动态调整注意力感受野来平衡场景一致性与运动细节。

### 2. 方法论：核心思想与技术细节
该方法在冻结的 Stable Diffusion UNet 中注入了仅 **25.8M**（约占基准 UNet 的 2.9%）的可训练参数。
*   **运动自适应时间注意力 (Adaptive Temporal Attention)**：
    *   **动态偏差 (Motion-adaptive bias)**：在注意力矩阵中引入距离偏差 $b_m(i, j) = -\lambda(m) \cdot |i - j|$。当运动得分 $m$ 较高时，$\lambda$ 增大，注意力集中在相邻帧（局部）；当 $m$ 较低时，注意力跨越所有帧（全局）。
    *   **运动感知门控 (Motion-aware gating)**：使用一个受运动强度调控的门控系数 $g(m)$ 来决定时间模块对特征更新的贡献强度。
*   **级联注入策略 (Cascaded Strategy)**：
    *   **下采样与中间块**：采用**全局时间注意力**，旨在稳定低分辨率下的宏观语义和背景一致性。
    *   **上采样块**：采用**运动自适应注意力**，在高分辨率层级保护细粒度特征免受运动模糊影响。
*   **时间相关噪声初始化 (Temporally Correlated Noise)**：
    *   在训练和推理时，通过自回归过程 $\epsilon_f = \rho \cdot \epsilon_{f-1} + \sqrt{1-\rho^2} \cdot \xi_f$ 引入相关噪声。这为模型提供了场景一致性的先验，$\rho$ 系数可在推理时调节运动幅度。
*   **零初始化 (Zero Initialization)**：所有新增层在初始状态均为恒等映射，确保不破坏预训练模型的原始分布。

### 3. 实验设计
*   **数据集**：从 **WebVid-10M** 中抽取 100K 个视频子集进行训练。
*   **基准 (Benchmark)**：WebVid 验证集（随机抽取 400 个标题）。
*   **对比方法**：
    *   **AnimateDiff**：在相同的 100K 数据集上重新训练其运动模块作为公平对比。
    *   **Base SD 1.5**：不加时间模块的原始模型（作为消融基准）。
*   **评估指标**：CLIP-SIM（文本对齐）、Frame Consistency (FC，帧间一致性)、Motion Magnitude (MM，运动幅度)、FID（单帧质量）、FVD（视频整体质量）。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A100 GPU** 集群（具体数量未标明，但提到“across A100 GPUs”）。
*   **训练配置**：训练 30 个 epoch，batch size 为 55，采用混合精度 (FP16) 和梯度检查点 (Gradient Checkpointing) 技术。
*   **参数量**：新增参数仅 25.8M，远低于 AnimateDiff 的 417M。

### 5. 实验数量与充分性
*   **实验规模**：进行了多项关键消融实验，包括：
    1.  **损失函数对比**：对比了“仅去噪损失”与“去噪+显式一致性损失”，发现显式损失反而抑制了运动多样性。
    2.  **噪声相关性 $\rho$**：测试了不同 $\rho$ 值对生成效果的影响。
    3.  **基准模型迁移**：在 SD 1.5 和 SD 2.1 上均验证了有效性。
*   **充分性评价**：实验设计较为客观，特别是通过在相同数据量下重训 AnimateDiff 来确保对比的公平性。虽然 400 个样本的验证集规模不算巨大，但涵盖了定性与定量的多维度分析。

### 6. 主要结论与发现
*   **隐式正则化优于显式**：标准的去噪目标足以让模型学习到时间规律，添加显式的时间一致性损失（Temporal Consistency Loss）反而会导致视频趋于静态。
*   **参数效率极高**：仅用 AnimateDiff 约 1/16 的参数量，就实现了相近的生成质量。
*   **推理可控性**：噪声相关系数 $\rho$ 是一个强大的推理调节杠杆，无需重训即可在“高一致性/低运动”与“低一致性/高运动”之间权衡。

### 7. 优点与亮点
*   **自适应性**：首次在轻量级插件中引入基于运动强度的动态注意力偏差，解决了视频生成中“一刀切”的时间建模问题。
*   **极度轻量**：2.9% 的参数增量使其非常适合在消费级显卡上进行微调或部署。
*   **级联设计**：巧妙地结合了全局语义稳定与局部细节细化，符合视觉处理的层级逻辑。

### 8. 不足与局限
*   **分辨率与时长限制**：实验仅生成 8 帧、256x256 分辨率的视频，与当前 SOTA 模型（如 Sora 或 Gen-3）的高清长视频相比仍有差距。
*   **性能上限**：尽管参数效率高，但在相同训练数据下，其 FID/FVD 指标仍略逊于参数量更大的 AnimateDiff，说明模型容量在一定程度上限制了天花板。
*   **运动估计依赖**：推理时的运动得分 $m$ 依赖于 prompt 关键词映射或手动设置，自动化程度仍有提升空间。

（完）
