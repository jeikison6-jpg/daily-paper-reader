---
title: "PAM: A Pose-Appearance-Motion Engine for Sim-to-Real HOI Video Generation"
title_zh: PAM：一种用于从模拟到现实（Sim-to-Real）手物交互视频生成的姿态-外观-运动引擎
authors: "Mingju Gao, Kaisen Yang, Huan-ang Gao, Bohan Li, Ao Ding, Wenyi Li, Yangcheng Yu, Jinkun Liu, Shaocong Xu, Yike Niu, Haohan Chi, Hao Chen, Hao Tang, Li Yi, Hao Zhao"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22193v1"
tags: ["query:rl-vg"]
score: 9.0
evidence: 用于虚实迁移手物交互视频生成的统一引擎
tldr: 本研究针对手物交互（HOI）生成领域中姿态、外观和运动研究割裂的问题，提出了PAM（姿态-外观-运动）统一引擎。PAM能够实现可控的HOI视频生成，无需真实首帧即可完成从模拟到真实的转换。通过在DexYCB和OAKINK2数据集上的实验，PAM在视频质量和姿态准确性上均优于现有基准，并能通过合成数据显著提升下游手部姿态估计任务的性能，为具身智能和AR/VR应用提供了强有力的支持。
selection_source: fresh_fetch
motivation: 现有的HOI生成研究在姿态合成、单图生成和视频生成方面相互孤立，且往往依赖真实首帧作为输入，限制了其在实际模拟到真实场景中的部署。
method: 提出了一种名为PAM的统一引擎，通过整合深度、分割和关键点等多种多模态输入条件，实现连贯且可控的手物交互视频生成。
result: "在DexYCB数据集上，PAM显著降低了FVD和MPJPE误差，并证明了仅需50%真实数据配合合成数据即可达到100%真实数据的训练效果。"
conclusion: PAM通过统一姿态、外观和运动框架，为高质量HOI视频生成提供了有效方案，并展示了合成数据在增强下游视觉任务中的巨大潜力。
---

## 摘要
手物交互（HOI）重建与合成正成为具身智能（Embodied AI）和增强现实/虚拟现实（AR/VR）的核心。然而，尽管取得了快速进展，现有的 HOI 生成研究仍分散在三个互不相干的轨道上：(1) 仅姿态合成，预测 MANO 轨迹但不生成像素；(2) 单图像 HOI 生成，从掩码或 2D 线索中幻化外观但缺乏动力学；(3) 视频生成方法，需要完整的姿态序列和真实的第一帧作为输入，阻碍了真正的从模拟到现实（sim-to-real）部署。受 Joo 等人 (2018) 理念的启发，我们认为 HOI 生成需要一个统一的引擎，将姿态、外观和运动整合到一个连贯的框架中。因此，我们推出了 PAM：一种用于可控 HOI 视频生成的姿态-外观-运动引擎。我们引擎的性能通过以下方式得到验证：(1) 在 DexYCB 数据集上，我们获得了 29.13 的 FVD（相比之下 InterDyn 为 38.83）和 19.37 mm 的 MPJPE（相比之下 CosHand 为 30.05 mm），同时生成的视频分辨率为 480x720，高于 256x256 和 256x384 的基准。(2) 在 OAKINK2 数据集上，我们的全多条件模型将 FVD 从 68.76 提升至 46.31。(3) 对 DexYCB 输入条件的消融实验表明，结合深度、分割和关键点始终能产生最佳结果。(4) 对于使用 SimpleHand 的下游手部姿态估计任务，通过 3,400 个合成视频（20.7 万帧）增强训练，使得仅使用 50% 真实数据加合成数据训练的模型即可达到 100% 真实数据基准的水平。

## Abstract
Hand-object interaction (HOI) reconstruction and synthesis are becoming central to embodied AI and AR/VR. Yet, despite rapid progress, existing HOI generation research remains fragmented across three disjoint tracks: (1) pose-only synthesis that predicts MANO trajectories without producing pixels; (2) single-image HOI generation that hallucinates appearance from masks or 2D cues but lacks dynamics; and (3) video generation methods that require both the entire pose sequence and the ground-truth first frame as inputs, preventing true sim-to-real deployment. Inspired by the philosophy of Joo et al. (2018), we think that HOI generation requires a unified engine that brings together pose, appearance, and motion within one coherent framework. Thus we introduce PAM: a Pose-Appearance-Motion Engine for controllable HOI video generation. The performance of our engine is validated by: (1) On DexYCB, we obtain an FVD of 29.13 (vs. 38.83 for InterDyn), and MPJPE of 19.37 mm (vs. 30.05 mm for CosHand), while generating higher-resolution 480x720 videos compared to 256x256 and 256x384 baselines. (2) On OAKINK2, our full multi-condition model improves FVD from 68.76 to 46.31. (3) An ablation over input conditions on DexYCB shows that combining depth, segmentation, and keypoints consistently yields the best results. (4) For a downstream hand pose estimation task using SimpleHand, augmenting training with 3,400 synthetic videos (207k frames) allows a model trained on only 50% of the real data plus our synthetic data to match the 100% real baseline.

---

## 论文详细总结（自动生成）

以下是对论文《PAM: A Pose–Appearance–Motion Engine for Sim-to-Real HOI Video Generation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不需要真实世界参考图像（如视频首帧）的情况下，仅凭模拟器中的姿态序列生成高质量、物理一致且视觉逼真的手物交互（HOI）视频，实现从模拟到现实（Sim-to-Real）的跨越。
*   **研究背景**：现有的 HOI 生成研究存在碎片化问题：
    1.  **仅姿态合成**：只预测 MANO 轨迹，缺乏像素生成。
    2.  **单图生成**：能生成外观但缺乏动态连贯性。
    3.  **视频生成**：通常依赖真实视频的首帧作为输入，限制了其在纯模拟环境（无真实图像）中的应用。

### 2. 方法论
PAM 引擎采用**解耦的三阶段架构**，将复杂的时空生成任务分解为姿态、外观和运动三个部分：
*   **阶段 I：姿态生成（Pose Generation）**：利用预训练模型（如 GraspXL）根据初始/目标手部姿态和物体几何体，合成平滑且物理合理的 MANO 手部运动轨迹和物体 6-DoF 位姿序列。
*   **阶段 II：外观生成（Appearance Generation）**：基于可控图像扩散模型（Flux），结合**深度图、语义分割掩码和手部关键点**三种模态作为控制信号，生成具有多样性的视频首帧。
*   **阶段 III：运动生成（Motion Generation）**：基于可控视频扩散模型（CogVideoX），将阶段 I 生成的完整姿态序列渲染为多模态条件序列，并结合阶段 II 生成的首帧，合成时空连贯的 480×720 高分辨率视频。
*   **关键技术细节**：引入了“三模态条件（Trimodal Conditions）”控制机制，通过 ControlNet 架构注入几何（深度）、语义（分割）和精细结构（关键点）信息，确保手部动作的精确性。

### 3. 实验设计
*   **数据集**：
    *   **DexYCB**：单手抓取物体的基准数据集。
    *   **OAKINK2**：复杂的双手操作物体数据集。
*   **对比方法（Benchmarks）**：与 SOTA 方法 **ManiVideo**、**InterDyn** 和 **CosHand** 进行对比。
*   **评估指标**：
    *   **视频质量**：FVD（弗雷歇视频距离）。
    *   **图像质量**：LPIPS、SSIM、PSNR。
    *   **运动保真度**：MF（Motion Fidelity，基于点追踪）。
    *   **姿态准确性**：MPJPE（平均关节位置误差）。
*   **下游任务验证**：使用生成的合成视频训练手部姿态估计模型（SimpleHand），验证生成数据对提升模型性能的实际价值。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H800 GPU**（80GB 显存版本）。
*   **训练细节**：采用 AdamW 优化器和 DeepSpeed 训练架构，训练步数为 8,000 步。
*   **推理效率**：在单张 H20 GPU 上，生成一段 40 帧视频的总耗时约为 **301.1 秒**（其中运动生成阶段耗时最久，约 245.7 秒）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的定量对比（表 1、表 2）和定性展示（图 4、图 10-13）。
*   **消融实验**：极其充分，涵盖了：
    1.  **输入条件组合**：验证了深度+分割+关键点三者结合的最优性。
    2.  **鲁棒性测试**：测试了对输入噪声的抵抗力。
    3.  **组件替换**：对比了不同姿态生成器（GraspXL vs D-Grasp）和视频骨干网络（CogVideoX vs SVD）。
    4.  **手部表示**：对比了关键点与网格投影的效果。
*   **客观性**：通过下游任务的“数据效率曲线”验证了合成数据的有效性，证明了 50% 真实数据 + 合成数据即可达到 100% 真实数据的效果，实验设计客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：在 DexYCB 上，PAM 的 FVD 达到 29.13（远优于 InterDyn 的 38.83），MPJPE 降至 19.37mm（优于 CosHand 的 30.05mm）。
*   **多模态必要性**：仅靠分割掩码不足以捕捉复杂的手部动作，引入手部关键点能显著降低姿态误差。
*   **Sim-to-Real 潜力**：PAM 能够仅凭模拟器的物理参数生成足以训练真实世界视觉模型的视频数据，展示了强大的数据增强能力。

### 7. 优点
*   **统一性**：首次将 HOI 的姿态、外观和运动整合进一个无需真实首帧的统一引擎。
*   **高保真度**：生成的视频分辨率（480×720）显著高于以往方法（通常为 256×256），且视觉细节更丰富。
*   **实用性强**：通过下游任务验证了其作为“数据工厂”的价值，对具身智能训练具有直接意义。

### 8. 不足与局限
*   **误差传递**：阶段 I 的姿态生成若出现物理错误（如穿模或接触缺失），会直接传播到最终视频中，导致视觉上的物理不一致。
*   **推理延迟**：生成过程涉及多个大型扩散模型，单段视频生成需约 5 分钟，难以满足实时交互需求。
*   **复杂场景限制**：虽然在双手任务上有所尝试，但对于极度复杂的长程操作任务，其时空连贯性仍有提升空间。

（完）
