---
title: "Offline Decision Transformers for Neural Combinatorial Optimization: Surpassing Heuristics on the Traveling Salesman Problem"
title_zh: 用于神经组合优化的离线决策 Transformer：在旅行商问题上超越启发式算法
authors: "Hironori Ohigashi, Shinichiro Hamada"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25241v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 离线强化学习框架和决策 Transformer
tldr: 针对旅行商问题（TSP）等组合优化难题，本文提出一种基于离线强化学习的决策Transformer框架。该方法通过整合指针网络处理可变动作空间，并利用分位数回归进行乐观的收益条件建模，直接从启发式算法数据集中学习。实验证明，该模型不仅能模仿现有算法，还能通过合成策略超越其训练所用的四种经典启发式算法，展示了离线RL在提升组合优化性能方面的巨大潜力。
selection_source: fresh_fetch
motivation: 传统的在线强化学习在组合优化中难以部署且未充分利用现有的启发式算法知识。
method: 结合指针网络处理节点选择的动态动作空间，并采用分位数回归对目标收益进行乐观估计。
result: 实验结果显示，该方法在TSP问题上生成的路径质量始终优于其训练数据中包含的四种经典启发式算法。
conclusion: 离线强化学习能够有效整合并超越现有领域知识，为解决复杂的组合优化问题提供了新路径。
---

## 摘要
诸如旅行商问题（TSP）之类的组合优化问题在工业界至关重要，但属于 NP 困难问题。神经组合优化已展现出潜力，但其对在线强化学习（RL）的依赖阻碍了部署，且未能充分利用数十年的算法知识。我们通过应用离线强化学习框架——决策 Transformer（Decision Transformer）来解决这些局限性，直接从启发式解决方案的数据集中学习更优策略；其目标不仅是模仿，而是综合并超越这些算法。具体而言，我们（i）集成了指针网络（Pointer Network）以处理与实例相关的、可变的节点选择动作空间，以及（ii）采用 expectile 回归对待得回报（Return-to-Go）进行乐观条件化，这对于最优值差异巨大的实例至关重要。实验表明，我们的方法始终能产生比其训练所用的四种经典启发式算法更高质量的路径，证明了离线强化学习在释放并超越现有领域知识中所蕴含性能方面的潜力。

## Abstract
Combinatorial optimization problems like the Traveling Salesman Problem are critical in industry yet NP-hard. Neural Combinatorial Optimization has shown promise, but its reliance on online reinforcement learning (RL) hampers deployment and underutilizes decades of algorithmic knowledge. We address these limitations by applying the offline RL framework, Decision Transformer, to learn superior strategies directly from datasets of heuristic solutions; it aims to not only to imitate but to synthesize and outperform them. Concretely, we (i) integrate a Pointer Network to handle the instance-dependent, variable action space of node selection, and (ii) employ expectile regression for optimistic conditioning of Return-to-Go, which is crucial for instances with widely varying optimal values. Experiments show that our method consistently produces higher-quality tours than the four classical heuristics it is trained on, demonstrating the potential of offline RL to unlock and exceed the performance embedded in existing domain knowledge.

---

## 论文详细总结（自动生成）

以下是对论文《Offline Decision Transformers for Neural Combinatorial Optimization: Surpassing Heuristics on the Traveling Salesman Problem》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：组合优化问题（如旅行商问题 TSP）在工业界应用广泛但属于 NP 困难问题。现有的神经组合优化（NCO）方法大多依赖**在线强化学习（Online RL）**，这在实际部署中面临数据获取成本高、环境模拟难等挑战，且未能充分利用数十年来积累的启发式算法知识。
*   **核心目标**：探索能否利用**离线强化学习（Offline RL）**框架，直接从现有的启发式算法数据集中学习，不仅模仿这些算法，还要通过策略合成实现“青出于蓝而胜于蓝”，超越原始启发式算法的性能。

### 2. 方法论
论文提出了一个针对 TSP 优化的决策 Transformer（Decision Transformer, DT）框架，核心技术包括：
*   **序列建模框架**：将 TSP 求解建模为轨迹序列 $\tau = (..., o_t, \hat{R}_t, a_t, ...)$。其中 $o_t$ 是节点嵌入，$\hat{R}_t$ 是待得回报（Return-to-Go, RTG），$a_t$ 是选择的下一个节点。
*   **集成指针网络（Pointer Network）**：标准 DT 处理的是固定语义的动作空间，而 TSP 的动作（节点索引）在不同实例间含义不同。作者引入指针网络，通过注意力机制在编码器的节点嵌入上进行“指向”，从而处理变长的、实例相关的动作空间。
*   **乐观 RTG 预测（Expectile Regression）**：由于不同 TSP 实例的最优解差异巨大，设定统一的 RTG 目标并不合理。模型引入了一个预测机制，利用 **Expectile 回归**（非对称最小二乘法）来预测该实例可能达到的最高回报。通过调节超参数 $\alpha > 0.5$，使模型对低估误差更敏感，从而产生“乐观”的目标设定。
*   **多任务损失函数**：结合了动作预测的交叉熵损失（$L_{CE}$）和 RTG 预测的 Expectile 损失（$L_{2,\alpha}$）。

### 3. 实验设计
*   **数据集与场景**：针对 2D 欧几里得 TSP 问题，测试了规模为 $N=20, 50, 100$ 的实例。
*   **训练数据来源（Benchmark）**：使用了四种经典启发式算法生成训练数据：
    1.  最近邻算法（Nearest Neighbor, NN）
    2.  最近插入法（Nearest Insertion, NI）
    3.  最远插入法（Farthest Insertion, FI）
    4.  模拟退火算法（Simulated Annealing, SA）
*   **对比方法**：
    *   原始启发式算法（Original Heuristics）
    *   行为克隆（Behavior Cloning, BC）：即不带 RTG 条件的监督学习。
    *   不同 RTG 设定下的 DT 变体。

### 4. 资源与算力
*   **硬件环境**：单台服务器，配备 1 张 **NVIDIA GeForce RTX 4090 (24GB)** GPU 和 Intel Xeon Silver 4314 CPU。
*   **训练时长**：
    *   $N=20$：约 19 小时。
    *   $N=50$：约 1 天 3 小时。
    *   $N=100$：约 1 天 23 小时。
*   **推理速度**：单个实例预测耗时在 0.63 到 1.10 秒之间。

### 5. 实验数量与充分性
*   **实验规模**：每个规模（N=20/50/100）准备了 100 万个训练实例，验证集和测试集各 1 万个。
*   **消融实验**：针对 RTG 的预测方式（固定值 vs 预测值）以及 Expectile 回归的超参数 $\alpha$ 进行了详细对比。
*   **充分性评价**：实验覆盖了从简单贪心到复杂随机搜索（SA）的多种数据源，并对比了 BC 基准，验证了 RTG 条件化的必要性。实验设计较为客观，通过标准差展示了结果的稳定性。

### 6. 主要结论与发现
*   **超越启发式**：该方法在所有测试场景下均优于其训练所用的原始启发式算法。特别是在 SA 数据集上，性能提升接近两倍。
*   **RTG 的关键作用**：带 RTG 条件的 DT 显著优于行为克隆（BC），证明了模型不是在简单模仿，而是在学习如何根据目标调整策略。
*   **乐观预测的有效性**：使用 Expectile 回归（$\alpha=0.99$）预测高目标回报，比预测平均回报或设定固定回报能产生更高质量的解。
*   **策略合成能力**：模型能够从次优的轨迹中“缝合”出更优的路径，尤其是在数据多样性较高的 SA 数据集上表现最强。

### 7. 优点
*   **创新性结合**：首次将决策 Transformer 与指针网络结合，解决了离线 RL 在处理变长组合优化动作空间时的适配问题。
*   **无需环境交互**：利用现有的算法知识即可训练，规避了在线 RL 复杂的奖励设计和环境模拟问题。
*   **性能突破**：证明了离线学习可以作为一种通用的策略改进手段，而不仅仅是数据模仿。

### 8. 不足与局限
*   **对数据多样性的依赖**：在动作模式非常单一的数据集（如 NN）上，模型提升有限，说明其性能受限于训练数据的探索程度。
*   **RTG 预测仍显保守**：实验发现，如果在推理时手动给预测的 RTG 增加一个偏移量（Offset），性能还能进一步提升，说明模型自主预测的最优回报仍偏保守。
*   **泛化性验证不足**：目前仅在 TSP 问题上进行了验证，尚未在更复杂的约束优化问题（如带时间窗的车辆路径问题）上测试。

（完）
