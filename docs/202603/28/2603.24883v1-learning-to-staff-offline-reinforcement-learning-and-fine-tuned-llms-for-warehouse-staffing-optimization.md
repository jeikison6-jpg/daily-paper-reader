---
title: "Learning to Staff: Offline Reinforcement Learning and Fine-Tuned LLMs for Warehouse Staffing Optimization"
title_zh: 学习人员配置：用于仓库人员配置优化的离线强化学习与微调大语言模型
authors: "Kalle Kujanpää, Yuying Zhu, Kristina Klinkner, Shervin Malmasi"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24883v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 离线强化学习用于仓库人员配置优化
tldr: "本研究探讨了优化半自动化仓库分拣系统实时人员配置的机器学习方法。研究对比了两种路径：一是利用离线强化学习训练基于Transformer的策略，在详细历史数据上实现了2.4%的吞吐量提升；二是利用微调后的大语言模型处理抽象的人类可读状态描述，通过SFT和DPO技术达到了匹配或超越历史基准的性能。这为AI辅助运营决策提供了多样化的可行方案。"
selection_source: fresh_fetch
motivation: 旨在解决半自动化仓库中实时人员配置优化的问题，以提高运营效率并降低成本。
method: 对比了基于Transformer的离线强化学习方法与经过监督微调及直接偏好优化的LLM方法。
result: "离线强化学习使吞吐量提升了2.4%，而微调后的LLM在抽象决策任务中表现出与历史基准相当或更优的性能。"
conclusion: 离线强化学习适用于处理复杂任务架构，而LLM则在处理人类可读输入及结合管理者偏好方面具有独特优势。
---

## 摘要
我们研究了用于优化半自动化仓库分拣系统中实时人员配置决策的机器学习方法。运营决策可以在不同的抽象层级上得到支持，并具有不同的权衡。我们在匹配的模拟环境中评估了两种方法。首先，我们利用详细的历史状态表示，通过离线强化学习训练了定制的基于 Transformer 的策略，在学习型模拟器中相比历史基准实现了 2.4% 的吞吐量提升。在高业务量的仓库运营中，这种规模的改进意味着显著的成本节约。其次，我们探索了在抽象的、人类可读的状态描述上运行的大语言模型（LLM）。这些模型非常适合仓库经理利用高层级运营摘要所做的决策。我们系统地比较了提示技术、自动提示优化和微调策略。虽然仅靠提示被证明是不够的，但监督微调结合基于模拟器生成偏好的直接偏好优化（DPO），在手工构建的模拟器中实现了与历史基准持平或略高的性能。我们的研究结果表明，这两种方法都为人工智能辅助的运营决策提供了可行路径。离线强化学习在特定任务架构方面表现出色，而大语言模型支持人类可读的输入，并可与能够纳入经理偏好的迭代反馈循环相结合。

## Abstract
We investigate machine learning approaches for optimizing real-time staffing decisions in semi-automated warehouse sortation systems. Operational decision-making can be supported at different levels of abstraction, with different trade-offs. We evaluate two approaches, each in a matching simulation environment. First, we train custom Transformer-based policies using offline reinforcement learning on detailed historical state representations, achieving a 2.4% throughput improvement over historical baselines in learned simulators. In high-volume warehouse operations, improvements of this size translate to significant savings. Second, we explore LLMs operating on abstracted, human-readable state descriptions. These are a natural fit for decisions that warehouse managers make using high-level operational summaries. We systematically compare prompting techniques, automatic prompt optimization, and fine-tuning strategies. While prompting alone proves insufficient, supervised fine-tuning combined with Direct Preference Optimization on simulator-generated preferences achieves performance that matches or slightly exceeds historical baselines in a hand-crafted simulator. Our findings demonstrate that both approaches offer viable paths toward AI-assisted operational decision-making. Offline RL excels with task-specific architectures. LLMs support human-readable inputs and can be combined with an iterative feedback loop that can incorporate manager preferences.

---

## 论文详细总结（自动生成）

### 论文总结：学习人员配置——用于仓库人员配置优化的离线强化学习与微调大语言模型

#### 1. 核心问题与整体含义（研究动机和背景）
本文聚焦于半自动化仓库分拣系统中的**实时人员配置（Staffing）优化**。在复杂的物流环境中，经理需要根据吞吐量、缓冲区水平和队列长度，周期性地重新分配工人以消除瓶颈。
*   **挑战**：该问题具有高度非线性动力学（缓冲区溢出或排空会导致上下游停滞）、动作空间呈指数级增长、决策具有延迟效应，且每次人员调动都有物理移动成本。
*   **动机**：传统的分析模型难以捕捉这些复杂交互。研究旨在探索两种不同抽象层级的 AI 辅助方案：一种是基于高维细节状态的**离线强化学习（Offline RL）**，另一种是基于抽象、人类可读描述的**大语言模型（LLM）**，以支持并优化人类经理的决策。

#### 2. 方法论：核心思想与关键技术
论文提出了两条并行的技术路径：

*   **离线强化学习路径 (TGNN 架构)**：
    *   **核心思想**：利用 Transformer-GNN (TGNN) 处理高维时空特征。
    *   **技术细节**：为了降低计算复杂度，TGNN 采用因子化注意力机制，先进行空间维度（位置与线路）的图注意力处理，再进行时间维度的注意力处理。
    *   **动作分解**：将巨大的联合动作空间分解为每个工人的独立决策（留在原位或移动到新位置），通过设置移动概率阈值（<0.1）来控制调动成本。
    *   **算法**：采用 Actor-Critic 框架，结合行为正则化（Behavioral Regularization）以防止策略偏离历史数据分布，并对比了微调行为克隆（BC-FT）。

*   **微调大语言模型路径 (SFT + DPO)**：
    *   **核心思想**：将系统状态转化为结构化文本，利用 LLM 的推理能力生成 JSON 格式的调配建议。
    *   **技术细节**：
        *   **提示工程**：测试了 CoT、Self-Refine 及自动提示优化（OPRO）。
        *   **监督微调 (SFT)**：在历史人类决策数据上进行指令微调。
        *   **迭代直接偏好优化 (Iterative DPO)**：利用模拟器生成成对的动作反馈（动作 A 比动作 B 吞吐量高），通过 DPO 训练模型学习“什么是不该做的”，并加入监督损失项以稳定训练。

#### 3. 实验设计
*   **数据集**：来自亚马逊真实仓库的历史轨迹，包含 31,366 个训练班次（约 50 万次状态转换）和 8,228 个测试班次，跨越多个月和多个物理站点。
*   **场景与模拟器**：
    *   **学习型模拟器**：用于评估 Offline RL，擅长预测缓冲区状态。
    *   **手工规则模拟器**：用于评估 LLM，基于因果规则构建，吞吐量预测更准。
*   **Benchmark（基准）**：回放历史人类决策（Replayed Human Decisions）和“不移动”基准。
*   **对比方法**：
    *   RL 侧：BC、BC-FT、Offline Actor-Critic。
    *   LLM 侧：Zero-shot、CoT、OPRO、SFT、DPO、SFT+DPO。

#### 4. 资源与算力
*   **算力优化**：TGNN 架构通过时空分解，将训练复杂度从平方级降至线性，使得模型可以在**单张 GPU** 上运行，训练时间缩短了 95%。
*   **模型规模**：LLM 实验中，提示优化使用了 **Qwen2.5-32B-Instruct**，而微调实验由于计算资源限制（Computational Constraints）使用了 **Qwen2.5-14B-Instruct**。
*   **具体型号**：文中未明确给出具体的 GPU 型号（如 A100 或 H100），但强调了在单卡环境下的可行性。

#### 5. 实验数量与充分性
*   **实验规模**：使用了超过 4 万小时的真实运营数据，样本量充足。
*   **消融与对比**：针对提示工程做了多组对比（CoT, Self-Consistency, Reflexion 等），针对微调做了迭代次数的消融（1 次 vs 2 次 DPO）。
*   **客观性**：所有结果均报告了 95% 置信区间。为了防止过拟合，RL 结果在两个不同的学习型模拟器上取平均值。
*   **局限性**：由于 RL 和 LLM 运行在不同抽象层级的模拟器上，两者之间缺乏直接的横向对比。

#### 6. 主要结论与发现
*   **Offline RL 表现强劲**：Actor-Critic 方法比历史人类决策提升了 **2.4%** 的吞吐量。
*   **LLM 仅靠提示是不够的**：即使是经过优化的 Prompt，其表现仍低于人类基准（约 -2.0%）。
*   **微调是 LLM 的关键**：SFT 结合迭代 DPO 后，LLM 能够匹配并略微超过人类基准（**+0.6%**）。
*   **模仿学习的价值**：简单的微调行为克隆（BC-FT）在稳定性上优于复杂的 RL 算法，且能达到接近的效果，更适合初步部署。

#### 7. 优点（亮点）
*   **架构创新**：TGNN 有效解决了高维时空数据在 Transformer 中的计算瓶颈。
*   **闭环设计**：LLM 的迭代 DPO 框架模拟了“经理反馈循环”，为未来引入真实人类反馈奠定了基础。
*   **实用性强**：不仅追求性能，还考虑了决策的可解释性（LLM 路径）和部署的稳定性（BC-FT 路径）。

#### 8. 不足与局限
*   **模拟器偏差**：实验高度依赖模拟器，虽然经过校准，但仍可能存在“模拟器过拟合”风险（即策略学习到了模拟器的特定缺陷而非真实物理规律）。
*   **无法直接对比**：由于状态抽象程度不同，无法断定 RL 和 LLM 哪种架构在同等条件下更优。
*   **合成偏好风险**：DPO 使用的是模拟器生成的偏好，而非真实经理的偏好，后者可能包含吞吐量以外的复杂考量（如员工疲劳度）。
*   **分布偏移**：未深入探讨当仓库设施发生重大变化（分布偏移）时模型的泛化能力。

（完）
