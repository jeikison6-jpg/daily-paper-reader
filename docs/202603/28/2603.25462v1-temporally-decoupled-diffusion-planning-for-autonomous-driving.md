---
title: Temporally Decoupled Diffusion Planning for Autonomous Driving
title_zh: 自动驾驶的时间解耦扩散规划
authors: "Xiang Li, Bikun Wang, John Zhang, Jianjun Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25462v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 用于自动驾驶轨迹生成和运动规划的扩散模型
tldr: 针对自动驾驶中长短期规划需求不同的挑战，本文提出时间解耦扩散模型（TDDM）。该模型采用“噪声即掩码”范式，将轨迹划分为具有独立噪声水平的片段，利用弱噪声的远期目标作为上下文来引导强噪声的近期避障规划。通过引入TD-AdaLN架构和非对称时间引导策略，TDDM在nuPlan基准测试中表现优异，尤其在复杂场景下展现出极强的鲁棒性。
selection_source: fresh_fetch
motivation: 现有扩散模型将轨迹视为整体，忽略了近期规划受瞬时动力学约束而远期规划受导航目标驱动的时间异构依赖关系。
method: 提出TDDM模型，通过为不同轨迹片段分配独立噪声水平，并结合TD-AdaLN层和非对称时间分类器自由引导，实现远期先验对近期生成的有效约束。
result: 在nuPlan基准测试中，TDDM的性能接近或超过了现有最先进基准，特别是在极具挑战性的Test14-hard子集中表现尤为出色。
conclusion: 时间解耦的扩散规划方法通过有效平衡即时安全性与长期导航目标，显著提升了自动驾驶在复杂城市环境中的决策质量。
---

## 摘要
动态城市环境中的运动规划需要平衡即时安全与长期目标。虽然扩散模型能有效捕捉多模态决策，但现有方法将轨迹视为统一实体，忽略了异构的时间依赖性，即短期规划受瞬时动力学约束，而长期规划受导航目标约束。为解决这一问题，我们提出了时间解耦扩散模型 (TDDM)，通过“噪声即掩码”范式重新构建轨迹生成。通过将轨迹划分为具有独立噪声水平的分段，我们将高噪声隐式地视为信息缺失，将弱噪声视为上下文线索。这促使模型利用保存较好的时间上下文中的内部相关性，来重建受损的短期状态。在架构上，我们引入了时间解耦自适应层归一化 (TD-AdaLN) 以注入特定分段的时间步。在推理过程中，我们的非对称时间无分类器引导 (Asymmetric Temporal Classifier-Free Guidance) 利用弱噪声的远期先验来引导即时路径生成。在 nuPlan 基准测试上的评估表明，TDDM 达到或超过了最先进的基准水平，特别是在极具挑战性的 Test14-hard 子集中表现出色。

## Abstract
Motion planning in dynamic urban environments requires balancing immediate safety with long-term goals. While diffusion models effectively capture multi-modal decision-making, existing approaches treat trajectories as monolithic entities, overlooking heterogeneous temporal dependencies where near-term plans are constrained by instantaneous dynamics and far-term plans by navigational goals. To address this, we propose Temporally Decoupled Diffusion Model (TDDM), which reformulates trajectory generation via a noise-as-mask paradigm. By partitioning trajectories into segments with independent noise levels, we implicitly treat high noise as information voids and weak noise as contextual cues. This compels the model to reconstruct corrupted near-term states by leveraging internal correlations with better-preserved temporal contexts. Architecturally, we introduce a Temporally Decoupled Adaptive Layer Normalization (TD-AdaLN) to inject segment-specific timesteps. During inference, our Asymmetric Temporal Classifier-Free Guidance utilizes weakly noised far-term priors to guide immediate path generation. Evaluations on the nuPlan benchmark show TDDM approaches or exceeds state-of-the-art baselines, particularly excelling in the challenging Test14-hard subset.

---

## 论文详细总结（自动生成）

以下是对论文《Temporally Decoupled Diffusion Planning for Autonomous Driving》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注自动驾驶中的**运动规划**问题。在复杂的城市环境中，规划器需要同时兼顾**即时安全性**（如避障、瞬时动力学）和**长期导航目标**（如路径跟随）。
*   **现有挑战**：目前的扩散模型（Diffusion Models）通常将整个轨迹（如未来8秒）视为一个不可分割的整体进行去噪。这种“一刀切”的方法忽略了轨迹内部不同时间段的异构依赖关系：近期规划更依赖当前环境动力学，而远期规划更依赖全局导航意图。
*   **研究动机**：借鉴视频生成领域（如 Diffusion Forcing）的思路，将轨迹在时间维度上解耦，通过独立控制不同时间段的噪声水平，使模型能够利用远期目标作为“线索”来引导近期路径的生成。

### 2. 方法论：核心思想与关键技术
论文提出了**时间解耦扩散模型 (TDDM)**，其核心逻辑是将轨迹生成重构为一种“噪声即掩码”的范式。
*   **轨迹令牌化与独立噪声**：将轨迹划分为 $N$ 个时间片段（Tokens）。在训练时，为不同片段分配独立的扩散时间步（Timesteps）。高噪声片段被视为“信息真空”，而弱噪声片段则作为“上下文参考”，迫使模型学习片段间的时空相关性。
*   **TD-AdaLN 架构**：在 Diffusion Transformer (DiT) 基础上，设计了**时间解耦自适应层归一化 (TD-AdaLN)**。该模块允许模型为每个轨迹片段注入特定的时间步编码，从而在架构层面支持异构的去噪过程。
*   **非对称时间无分类器引导 (Asymmetric Temporal CFG)**：在推理阶段，同时运行两条路径：
    1.  **无条件路径**：标准的全局去噪。
    2.  **条件路径**：将远期片段保持在极低噪声水平（作为已知先验），仅对近期片段进行去噪。
    通过两者的融合，实现以“远期目标”引导“近期生成”的轨迹补全任务。
*   **基于锚点的词汇表**：使用 K-Means 聚类生成的轨迹锚点作为扩散过程的起点，提高生成效率。

### 3. 实验设计
*   **数据集**：使用大规模自动驾驶数据集 **nuPlan**。
*   **基准测试 (Benchmarks)**：
    *   **Val14**：常规验证集（1118个场景）。
    *   **Test14-hard**：长尾、高风险挑战场景（272个场景）。
    *   **Test14-random**：随机采样场景。
*   **评估模式**：包括非反应式（Non-reactive）和反应式（Reactive）闭环评估。
*   **对比方法**：
    *   规则/混合方法：IDM, PDM-Closed, PDM-Hybrid。
    *   学习方法：PlanTF, PLUTO, UrbanDriver, GameFormer。
    *   扩散模型基准：Diffusion Planner。

### 4. 资源与算力
*   **算力设备**：使用了 **4 张 NVIDIA 3080 GPU**。
*   **训练细节**：在 200k 个 nuPlan 场景子集上训练了 **500 个 Epoch**。
*   **超参数**：Batch size 为 320，使用 AdamW 优化器，学习率为 $5 \times 10^{-4}$。
*   **推理效率**：使用 DPM-Solver++ 仅需 **2 个去噪步**即可完成推理。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流基准上进行了详尽的定量对比，并提供了定性可视化分析。
*   **消融实验**：针对轨迹令牌化数量 ($N$)、TD-AdaLN 模块、独立噪声训练、CFG 引导比例 ($w$) 等关键组件进行了多组消融实验。
*   **充分性评价**：实验设计较为客观、公平。特别是在仅使用 20% 训练数据（200k vs 1M）的情况下，与使用全量数据的 SOTA 方法进行对比，证明了其极高的**数据效率**。

### 6. 主要结论与发现
*   **性能优异**：TDDM 在 nuPlan 上的表现接近或超过了现有的最先进学习型规划器。
*   **长尾场景鲁棒性**：在最困难的 **Test14-hard** 子集中，TDDM 的得分（77.95）显著高于对比的扩散模型基准（75.67），证明了时间解耦对处理复杂交互场景的有效性。
*   **一致性提升**：非对称引导策略有效解决了传统扩散规划器中常见的“近视”或决策不连贯问题。

### 7. 优点
*   **创新范式**：首次将时间解耦的概念引入自动驾驶扩散规划，打破了轨迹整体去噪的局限。
*   **架构适配**：TD-AdaLN 的设计巧妙地解决了 Transformer 内部处理异构时间步信息的难题。
*   **高效性**：通过锚点机制和快速求解器，实现了极少的去噪步数，具备实时应用的潜力。

### 8. 不足与局限
*   **反应式评估略逊**：在完全反应式（Reactive）模式下，虽然表现优秀，但仍略低于某些顶尖的规则/学习混合方法（如 PDM-Closed）。
*   **依赖锚点**：模型仍依赖预定义的轨迹词汇表（Anchors），这可能限制了在极端罕见路径上的生成灵活性。
*   **交互建模**：目前主要关注自车轨迹的时间解耦，对多车之间复杂的博弈交互建模尚有提升空间。

（完）
