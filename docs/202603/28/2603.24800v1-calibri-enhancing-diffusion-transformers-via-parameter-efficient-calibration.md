---
title: "Calibri: Enhancing Diffusion Transformers via Parameter-Efficient Calibration"
title_zh: Calibri：通过参数高效校准增强扩散 Transformer
authors: "Danil Tokhchukov, Aysel Mirzoeva, Andrey Kuznetsov, Konstantin Sobolev"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24800v1"
tags: ["query:rl-vg"]
score: 9.0
evidence: 扩散Transformer的黑盒奖励优化
tldr: 本研究揭示了 Diffusion Transformers (DiTs) 的潜在优化空间，发现仅通过引入单个学习缩放参数即可显著提升性能。为此，作者提出了 Calibri，一种参数高效的校准方法。Calibri 将 DiT 组件校准视为黑盒奖励优化问题，利用进化算法仅调整约 100 个参数。实验表明，该方法在多种文生图模型上均能提升生成质量，并能在保持高质量输出的同时减少推理步数。
selection_source: fresh_fetch
motivation: 旨在通过深入分析去噪过程，挖掘 DiT 块中单个缩放参数对提升生成性能的巨大潜力。
method: 提出 Calibri 方法，通过进化算法优化极少量（约 100 个）参数，实现对 DiT 组件的黑盒奖励校准。
result: 实验证明 Calibri 在多种模型上一致提升了生成质量，并显著降低了推理所需的迭代次数。
conclusion: 该研究表明轻量级的参数校准是提升扩散 Transformer 模型效率与质量的一种极具潜力且高效的途径。
---

## 摘要
在本文中，我们揭示了扩散 Transformer (DiTs) 在显著增强生成任务方面的潜在能力。通过对去噪过程的深入分析，我们证明了引入单个可学习的缩放参数即可显著提升 DiT 模块的性能。基于这一见解，我们提出了 Calibri，这是一种参数高效的方法，通过优化校准 DiT 组件来提升生成质量。Calibri 将 DiT 校准建模为一个黑盒奖励优化问题，并利用进化算法高效求解，仅需修改约 100 个参数。实验结果表明，尽管设计轻量，Calibri 在各种文本生成图像模型中均能持续提升性能。值得注意的是，Calibri 还在保持高质量输出的同时，减少了图像生成所需的推理步数。

## Abstract
In this paper, we uncover the hidden potential of Diffusion Transformers (DiTs) to significantly enhance generative tasks. Through an in-depth analysis of the denoising process, we demonstrate that introducing a single learned scaling parameter can significantly improve the performance of DiT blocks. Building on this insight, we propose Calibri, a parameter-efficient approach that optimally calibrates DiT components to elevate generative quality. Calibri frames DiT calibration as a black-box reward optimization problem, which is efficiently solved using an evolutionary algorithm and modifies just ~100 parameters. Experimental results reveal that despite its lightweight design, Calibri consistently improves performance across various text-to-image models. Notably, Calibri also reduces the inference steps required for image generation, all while maintaining high-quality outputs.

---

## 论文详细总结（自动生成）

以下是对论文《Calibri: Enhancing Diffusion Transformers via Parameter-Efficient Calibration》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的扩散 Transformer (DiT) 架构（如 SD3、FLUX）虽然由重复的块组成，但各层对生成结果的贡献并不均衡。
*   **研究动机**：作者发现，简单地禁用某些 DiT 块甚至能提升生成质量，这暗示了标准架构的权重分配并非最优。因此，论文旨在探索是否可以通过一种极低参数量（Post-hoc Calibration）的方法，在不微调全量模型的前提下，优化各组件的贡献，从而提升图像生成的质量与效率。

### 2. 方法论
*   **核心思想**：在 DiT 块的输出端引入可学习的标量缩放因子（Scaling Parameters），通过最大化黑盒奖励函数（Reward Function）来寻找最优的权重配置。
*   **关键技术细节**：
    *   **校准粒度**：提出了三种粒度：**Block Scaling**（每个块一个参数）、**Layer Scaling**（Attention 和 MLP 层分别校准）以及 **Gate Scaling**（针对多模态 DiT，对视觉和文本门控分别校准）。
    *   **优化算法**：采用 **CMA-ES（协方差矩阵自适应进化策略）**。这是一种无梯度优化算法，通过迭代更新多元高斯分布的均值和协方差，在搜索空间中寻找使奖励模型（如 HPSv3）得分最高的参数组合。
    *   **Calibri Ensemble**：支持同时校准多个模型（如条件与无条件模型）的集成，进一步增强生成性能。
    *   **参数量**：仅需优化约 $10^2$（约 100 个）参数，远低于传统的全量微调或 LoRA。

### 3. 实验设计
*   **基准模型（Backbones）**：FLUX.1-dev、Stable Diffusion 3.5 Medium (SD-3.5M)、Qwen-Image。
*   **数据集与评测指标**：
    *   使用 **T2I-compbench++** 的提示词进行训练（搜索参数）和测试。
    *   **客观指标**：HPSv3（人类偏好）、ImageReward、Q-Align（图像质量）、PickScore。
    *   **主观评测**：开展了涉及 200 名用户、5600 次评估的大规模人类偏好研究。
*   **对比方法**：原始基准模型、经过强化学习对齐的 **Flow-GRPO** 模型。

### 4. 资源与算力
*   **算力消耗**：实验使用了 **NVIDIA H100 GPU**。
*   **训练时长**：
    *   **FLUX (Block)**：约 32 H100 GPU 小时（200 次迭代）。
    *   **SD-3.5M (Gate)**：约 356 H100 GPU 小时（2280 次迭代）。
    *   **Qwen-Image (Gate)**：约 286 H100 GPU 小时（630 次迭代）。
*   **说明**：虽然搜索过程需要一定算力，但这是一次性的离线成本，推理阶段不增加额外计算负担。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了当前最前沿的三个 DiT 变体，并进行了消融实验（对比不同校准粒度、不同奖励模型作为目标）。
*   **充分性**：实验设计较为全面，不仅有自动化指标，还通过人类评估验证了改进的真实性（排除“奖励作弊”风险）。对比 Flow-GRPO 的实验展示了该方法在极低参数量下达到甚至超过复杂强化学习方法的效果，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：Calibri 在所有测试模型上均显著提升了 HPSv3 和 ImageReward 等指标。
*   **效率增强**：Calibri 能够显著减少推理步数（NFE）。例如，FLUX 在 15 步时的表现即可优于原始模型 30 步的表现，推理速度提升 2-3.3 倍。
*   **参数极简**：仅更新约 200 个参数的效果可类比更新 1800 万参数的 Flow-GRPO。
*   **兼容性**：Calibri 可以与现有的对齐方法（如 GRPO）叠加使用，进一步压榨模型潜力。

### 7. 优点
*   **极高效率**：参数量极小，无需存储庞大的微调权重，易于部署。
*   **推理加速**：通过优化权重分布，使模型在更少的采样步数下收敛到高质量结果。
*   **即插即用**：作为一种后校准技术，不需要重新训练模型，对架构改动极小。

### 8. 不足与局限
*   **奖励模型依赖**：Calibri 的上限取决于所使用的奖励模型。如果奖励模型对某些伪影（如多出的手指）不敏感，校准后的模型也可能保留这些缺陷。
*   **搜索成本**：虽然推理快，但使用 CMA-ES 进行黑盒搜索的过程仍需消耗数百个 GPU 小时，对于算力受限的用户仍有门槛。
*   **多样性风险**：与所有基于奖励优化的方法类似，过度优化可能导致生成图像的模式坍塌或多样性略微下降（尽管文中称其表现优于 Flow-GRPO）。

（完）
