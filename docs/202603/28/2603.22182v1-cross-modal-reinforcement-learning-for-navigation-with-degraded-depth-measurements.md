---
title: Cross-Modal Reinforcement Learning for Navigation with Degraded Depth Measurements
title_zh: 针对深度测量退化情况下的跨模态强化学习导航
authors: "Omkar Sawant, Luca Zanatta, Grzegorz Malczyk, Kostas Alexis"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22182v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 基于强化学习的导航策略
tldr: 针对深度传感器在恶劣光照或反射表面下失效的问题，本文提出一种跨模态强化学习框架。通过跨模态 Wasserstein 自动编码器（CMWAE）学习深度与灰度图像的共享潜空间表示，使系统能在深度测量退化时利用灰度图推断深度特征。结合强化学习策略，实现了在非结构化环境中的鲁棒避障导航，并在实机实验中验证了其有效性。
selection_source: fresh_fetch
motivation: 旨在解决深度传感器在复杂环境（如强光、反射）中因测量失效而导致机器人导航性能下降的问题。
method: 提出一种跨模态 Wasserstein 自动编码器来学习深度与灰度图的共享表示，并将其集成到强化学习策略中。
result: 实验证明该方法在深度信息严重退化的情况下仍能保持鲁棒的导航表现，并具备良好的跨场景迁移能力。
conclusion: 通过跨模态一致性学习互补特征，可以显著提升机器人在传感器受损环境下的自主导航可靠性。
---

## 摘要
本文提出了一种跨模态学习框架，利用深度图像和灰度图像的互补信息来实现鲁棒导航。我们引入了一种跨模态 Wasserstein 自动编码器（Cross-Modal Wasserstein Autoencoder），通过强制跨模态一致性来学习共享的潜在表示，使系统能够在深度测量受损时从灰度观测中推断出与深度相关的特征。学习到的表示与基于强化学习的策略相结合，用于在光照不足或反射表面等不利条件下导致深度传感器退化的非结构化环境中实现无碰撞导航。仿真和真实世界实验表明，我们的方法在深度显著退化的情况下仍能保持鲁棒性能，并成功迁移到真实环境中。

## Abstract
This paper presents a cross-modal learning framework that exploits complementary information from depth and grayscale images for robust navigation. We introduce a Cross-Modal Wasserstein Autoencoder that learns shared latent representations by enforcing cross-modal consistency, enabling the system to infer depth-relevant features from grayscale observations when depth measurements are corrupted. The learned representations are integrated with a Reinforcement Learning-based policy for collision-free navigation in unstructured environments when depth sensors experience degradation due to adverse conditions such as poor lighting or reflective surfaces. Simulation and real-world experiments demonstrate that our approach maintains robust performance under significant depth degradation and successfully transfers to real environments.

---

## 论文详细总结（自动生成）

以下是对论文《Cross-Modal Reinforcement Learning for Navigation with Degraded Depth Measurements》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：无人机（UAV）在非结构化环境（如隧道、灾难现场）导航时，深度传感器（如RGB-D相机）常因光照不足、反射表面或传感器故障导致深度图退化（缺失或错误），导致传统导航算法失效。
*   **研究动机**：虽然深度信息可能受损，但灰度/RGB图像通常仍包含关键的结构线索。本文旨在通过跨模态学习，利用灰度图补全退化的深度特征，提升机器人在极端环境下的避障鲁棒性。

### 2. 方法论
*   **核心思想**：构建一个**跨模态 Wasserstein 自动编码器（CMWAE）**，将灰度图和深度图映射到共享的潜在空间（Latent Space），使系统具备从灰度观测中“推断”几何结构的能力。
*   **关键技术细节**：
    *   **CMWAE 架构**：包含两个分支（灰度分支 $E_g$ 和深度分支 $E_d$），均采用 AlexNet 骨干网络。通过共享投影层 $E_s$ 生成融合潜向量 $z$。
    *   **解码与重构**：解码器 $D_d$ 仅尝试重构深度图。这种非对称设计强制网络从灰度特征中提取几何信息。
    *   **损失函数**：结合了重构损失（MSE）和基于最大均值差异（MMD）的 Wasserstein 距离正则项，以确保潜空间分布的一致性和泛化性。
    *   **强化学习策略**：使用 **PPO（Proximal Policy Optimization）** 算法。观测值包括潜向量 $z$、目标相对位置、机器人状态（速度、姿态）及历史动作。输出为速度和偏航率指令。
    *   **训练策略**：在训练 CMWAE 时，人为对深度图施加随机遮挡（20%-50% 的区域失效），迫使模型学习跨模态补偿。

### 3. 实验设计
*   **数据集/场景**：
    *   **感知训练**：使用 TartanAir 数据集（包含同步的 RGB 和深度图）进行 CMWAE 的预训练。
    *   **仿真环境**：在 Aerial Gym 模拟器中进行大规模并行强化学习训练，环境包含随机生成的墙壁、杆状物和面板。
*   **Benchmark 与对比方法**：
    *   对比了 **DCE-RL（Deep Collision Encoding）**，这是一种仅依赖深度信息的 SOTA 强化学习导航方法。
    *   设置了“清洁（Clean）”和“退化（Corrupted）”两种测试条件。
*   **实机实验**：在 28 米长的走廊中，通过关闭 Intel RealSense D455 的红外投影仪来模拟深度传感器失效，测试算法的 Sim2Real 迁移能力。

### 4. 资源与算力
*   **算力设备**：
    *   **部署端**：实机实验使用了 **NVIDIA Jetson Orin NX (16GB)** 进行实时推理。
    *   **训练端**：文中提到使用了支持高度并行化的 Aerial Gym 模拟器，但未明确给出训练所用的具体 GPU 型号、数量及总训练时长。
*   **通信框架**：基于 ROS 和 PX4 自动驾驶仪。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **感知评估**：对比了两种不同的遮挡方案（S1 和 S2），并在 0% 到 50% 的不同退化水平下测试了 MSE 和 SSIM 指标。
    *   **策略评估**：在仿真中进行了 20 个难度等级（障碍物密度递增）的课程学习测试。
    *   **实机验证**：在 7 种不同的障碍物布局环境下进行了多次飞行实验。
*   **充分性评价**：实验设计较为全面，涵盖了从感知重构质量到闭环控制性能的评估，并对比了基准算法，验证了从仿真到现实的迁移效果，具有较强的说服力。

### 6. 主要结论与发现
*   **跨模态优势**：在深度图完好时，纯深度模型（DCE-RL）略优（因为潜空间带宽全部用于深度）；但在深度图退化时，DCE-RL 彻底失效（成功率为 0%），而 CMWAE 仍能保持较高的导航成功率（如在 20 级难度下仍有约 33% 成功率）。
*   **重构能力**：CMWAE 能够通过灰度图有效补全深度图中的缺失空洞，保留关键的避障几何特征。
*   **实机表现**：在深度图平均退化 25%-30%（峰值达 60%）的真实环境中，无人机能够成功避开所有障碍物到达目标点。

### 7. 优点
*   **鲁棒性强**：专门针对传感器失效场景设计，解决了单一模态导航的脆弱性问题。
*   **潜空间对齐**：引入 Wasserstein 距离和 MMD 正则化，相比传统 VAE 提供了更稳定、质量更高的潜在表示。
*   **计算高效**：将 RGB 转为灰度处理，降低了计算开销，适合在 Jetson 等嵌入式平台上运行。

### 8. 不足与局限
*   **性能权衡**：由于潜空间需要同时编码两种模态，在传感器完全正常的情况下，其表现略逊于专门针对单一模态优化的模型。
*   **环境复杂度限制**：实验场景主要由静态几何体（面板、杆）组成，尚未在具有动态障碍物或极度复杂的自然环境（如茂密森林）中进行验证。
*   **模态扩展性**：目前仅融合了灰度与深度，未探讨加入热成像或雷达等更多模态对极端恶劣环境（如浓烟）的进一步提升。

（完）
