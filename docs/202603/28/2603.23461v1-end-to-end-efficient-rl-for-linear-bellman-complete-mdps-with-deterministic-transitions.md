---
title: End-to-End Efficient RL for Linear Bellman Complete MDPs with Deterministic Transitions
title_zh: 具有确定性转移的线性 Bellman 完备 MDP 的端到端高效强化学习
authors: "Zakaria Mhammedi, Alexander Rakhlin, Nneka Okolo"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23461v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: MDP中具有线性函数逼近的强化学习
tldr: 本研究针对满足线性Bellman完备性的马尔可夫决策过程（MDP），探讨了线性函数近似下的强化学习。针对以往算法在处理大动作空间时效率低下或依赖强预言机假设的问题，本文提出了一种针对确定性转移、随机初始状态和奖励的高效算法。该算法在有限动作空间下实现了端到端高效，在无限动作空间下仅需标准argmax预言机，且样本和计算复杂度均呈多项式级别，显著提升了计算效率。
selection_source: fresh_fetch
motivation: 旨在解决线性Bellman完备MDP中，现有高效算法受限于动作空间大小或需强预言机假设的局限性。
method: 提出一种适用于确定性转移MDP的新算法，利用线性函数近似并结合标准argmax预言机处理大规模动作空间。
result: 成功实现了在样本和计算复杂度上均对时界、特征维度和精度要求呈多项式增长的ε-最优策略学习。
conclusion: 该研究证明了在确定性转移下的线性Bellman完备MDP中，可以实现兼具统计与计算高效性的强化学习方案。
---

## 摘要
我们研究了在满足“线性 Bellman 完备性”的马尔可夫决策过程（MDPs）中，使用线性函数近似的强化学习（RL）。这是一个基础设定，其中任何线性价值函数的 Bellman 备份仍保持线性。虽然在统计上是可处理的，但先前的计算高效算法要么局限于较小的动作空间，要么需要对特征空间进行强预言机（oracle）假设。我们为具有确定性转移、随机初始状态和随机奖励的线性 Bellman 完备 MDP 提供了一种计算高效的算法。对于有限动作空间，我们的算法是端到端高效的；对于大规模或无限动作空间，我们仅需要一个标准的动作 argmax 预言机。我们的算法学习一个 ε-最优策略，其样本和计算复杂度在时界（horizon）、特征维度和 1/ε 上呈多项式级。

## Abstract
We study reinforcement learning (RL) with linear function approximation in Markov Decision Processes (MDPs) satisfying \emph{linear Bellman completeness} -- a fundamental setting where the Bellman backup of any linear value function remains linear. While statistically tractable, prior computationally efficient algorithms are either limited to small action spaces or require strong oracle assumptions over the feature space. We provide a computationally efficient algorithm for linear Bellman complete MDPs with \emph{deterministic transitions}, stochastic initial states, and stochastic rewards. For finite action spaces, our algorithm is end-to-end efficient; for large or infinite action spaces, we require only a standard argmax oracle over actions. Our algorithm learns an $\varepsilon$-optimal policy with sample and computational complexity polynomial in the horizon, feature dimension, and $1/\varepsilon$.

---

## 论文详细总结（自动生成）

以下是对论文《End-to-End Efficient RL for Linear Bellman Complete MDPs with Deterministic Transitions》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在线性函数近似的强化学习中，**线性 Bellman 完备性（Linear Bellman Completeness）**是一个基础假设（即任何线性价值函数的 Bellman 备份仍是线性的）。虽然该设定在统计上是可处理的（样本复杂度可控），但在计算上一直存在挑战。
*   **研究动机**：现有的计算高效算法要么仅限于极小的动作空间（复杂度随动作数量指数增长），要么需要极强的预言机假设（如对整个特征空间进行线性优化）。
*   **研究背景**：本文聚焦于**确定性转移（Deterministic Transitions）**、随机初始状态和随机奖励的特定设定，旨在解决在大动作空间下，如何实现样本复杂度和计算复杂度均为多项式级别的端到端高效算法。

### 2. 方法论：核心思想与算法流程
论文提出了两种主要方案，分别针对有限动作空间和无限/大动作空间：

*   **核心思想**：采用“先探索、后规划”的两阶段框架。
    *   **阶段 I（探索）**：逐层构建策略覆盖（Policy Cover）。通过构建“重心生成器（Barycentric Spanner）”，找到一组策略，其诱导的特征向量能近似张成所有可达特征的空间。
    *   **阶段 II（利用）**：利用策略覆盖收集数据，通过岭回归（Ridge Regression）估计奖励参数，然后在估计的确定性代理奖励上进行规划。
*   **关键技术细节**：
    *   **有限动作空间算法**：结合了 **OCP（乐观约束传播）** 算法和鲁棒重心生成器。OCP 用于在确定性系统中高效处理一般函数近似。
    *   **大动作空间算法**：这是本文的核心贡献。引入了 **FQI（拟合 Q 迭代）** 和 **SubspaceCover（子空间覆盖）** 子程序。
    *   **SubspaceCover 机制**：利用确定性动力学，通过迭代搜索不在当前已知子空间内的特征方向。它不需要复杂的全局优化预言机，仅需一个标准的**动作 argmax 预言机**（即在给定状态和向量下，找到使特征与向量内积最大的动作）。
    *   **规避 Q 参数边界限制**：通过先估计瞬时奖励参数，将随机奖励问题转化为确定性奖励规划，从而避开了以往研究中对 Q 函数参数范数必须有界的严格限制（该限制会导致算法失去通用性）。

### 3. 实验设计
*   **性质说明**：本论文是一篇**纯理论证明型论文**，重点在于算法的复杂性分析和数学证明。
*   **场景/基准**：论文没有使用标准的经验性数据集（如 Atari 或 MuJoCo），而是通过数学推导证明了算法在满足假设的线性 Bellman 完备 MDP 上的表现。
*   **对比对象**：理论上对比了之前的算法，如 FRANCIS（需要强可达性假设）、ELEANOR（计算不可行）以及 Golowich & Moitra (2024a) 的算法（动作空间指数级复杂度）。

### 4. 资源与算力
*   **算力说明**：文中**未提及**任何具体的 GPU 型号、数量或训练时长。
*   **原因**：由于是理论研究，其核心贡献在于证明了算法的**计算复杂度是多项式级**的（即 $poly(d, H, 1/\epsilon)$），而非通过实验跑出的数值结果。

### 5. 实验数量与充分性
*   **充分性评价**：从理论研究的角度看，论文的证明非常充分。它涵盖了从有限动作到无限动作的扩展，并详细推导了每一阶段的误差界限和成功概率。
*   **客观性**：论文通过严格的数学引理（如子空间发现引理、岭回归置信椭圆等）确保了结论的客观性。

### 6. 主要结论与发现
*   **计算可行性**：证明了在确定性转移下，线性 Bellman 完备 MDP 的强化学习可以在多项式时间内完成，即使动作空间很大。
*   **预言机弱化**：成功将所需的预言机从“特征空间线性优化预言机”弱化为更标准的“动作 argmax 预言机”。
*   **端到端效率**：算法实现了从数据收集到策略输出的全流程高效性，样本复杂度和计算复杂度均与特征维度 $d$、时界 $H$ 和精度 $1/\epsilon$ 呈多项式关系。

### 7. 优点
*   **理论突破**：解决了线性 Bellman 完备设定下大动作空间的计算效率难题，填补了统计与计算之间的鸿沟。
*   **假设精简**：不需要 Q 函数参数有界的假设，这使得算法能够涵盖线性 MDP 等更广泛的模型。
*   **技术创新**：提出的 `SubspaceCover` 算法为在确定性系统中发现隐藏特征方向提供了一种高效的新思路。

### 8. 不足与局限
*   **转移确定性限制**：算法严格依赖于**确定性转移**假设。在完全随机的转移环境下，该方法无法直接应用，而随机转移是强化学习中更普遍且更困难的设定。
*   **应用范围**：虽然论文提到该技术可能适用于大语言模型（LLM）微调（因为 token 生成是确定性的），但目前仅停留在理论设想阶段，缺乏实际系统的验证。
*   **预言机依赖**：在大动作空间下仍需 argmax 预言机，虽然这是标准假设，但在某些复杂的连续动作空间中，实现该预言机本身可能依然困难。

（完）
