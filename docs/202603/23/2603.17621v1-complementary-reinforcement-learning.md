---
title: Complementary Reinforcement Learning
title_zh: 互补强化学习
authors: "Dilxat Muhtar, Jiashun Liu, Wei Gao, Weixun Wang, Shaopan Xiong, Ju Huang, Siran Yang, Wenbo Su, Jiamang Wang, Ling Pan, Bo Zheng"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17621v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 基于大语言模型智能体的强化学习算法
tldr: "针对LLM智能体在强化学习中样本效率低且难以有效利用历史经验的问题，本文受神经科学启发提出“互补强化学习”（Complementary RL）。该方法通过在优化循环中实现经验提取器与策略执行器的协同演化，使提取器能根据执行器的成长动态调整经验管理策略。实验表明，该方法在单任务中性能提升10%，并在多任务场景下展现出良好的扩展性，为高效经验驱动学习提供了新范式。"
selection_source: fresh_fetch
motivation: 现有方法中提取的经验往往是静态的，无法随执行器能力的提升而同步演化，导致经验与执行器需求之间出现失配。
method: 提出一种协同演化框架，执行器通过稀疏奖励优化，而经验提取器则根据其提供的经验是否能促进执行器成功来动态调整策略。
result: "在单任务场景下相比不学习经验的基线提升了10%的性能，并在多任务设置中展现出稳健的可扩展性。"
conclusion: Complementary RL 成功实现了经验管理与策略优化的深度耦合，确立了高效经验驱动型智能体学习的新范式。
---

## 摘要
强化学习（RL）已成为训练基于大语言模型（LLM）智能体的强大范式，但仍受限于低样本效率。这不仅源于稀疏的结果反馈，还源于智能体无法跨回合利用先验经验。虽然利用历史经验增强智能体提供了一种有前景的补救方案，但现有方法存在一个关键缺陷：从历史中提取的经验要么是静态存储的，要么无法与不断进步的执行器（actor）协同演化，导致经验与执行器不断发展的能力之间出现渐进式失配，从而在训练过程中降低了其效用。受神经科学中互补学习系统的启发，我们提出了互补强化学习（Complementary RL），旨在强化学习优化循环中实现经验提取器与策略执行器的无缝协同演化。具体而言，执行器通过基于结果的稀疏奖励进行优化，而经验提取器则根据其提取的经验是否能显著促进执行器的成功来进行优化，从而使其经验管理策略与执行器不断增长的能力保持同步演化。实验结果表明，Complementary RL 优于不从经验中学习的基于结果的智能体强化学习基准，在单任务场景中实现了 10% 的性能提升，并在多任务设置中展现出稳健的可扩展性。这些结果确立了 Complementary RL 作为高效经验驱动型智能体学习范式的地位。

## Abstract
Reinforcement Learning (RL) has emerged as a powerful paradigm for training LLM-based agents, yet remains limited by low sample efficiency, stemming not only from sparse outcome feedback but also from the agent's inability to leverage prior experience across episodes. While augmenting agents with historical experience offers a promising remedy, existing approaches suffer from a critical weakness: the experience distilled from history is either stored statically or fail to coevolve with the improving actor, causing a progressive misalignment between the experience and the actor's evolving capability that diminishes its utility over the course of training. Inspired by complementary learning systems in neuroscience, we present Complementary RL to achieve seamless co-evolution of an experience extractor and a policy actor within the RL optimization loop. Specifically, the actor is optimized via sparse outcome-based rewards, while the experience extractor is optimized according to whether its distilled experiences demonstrably contribute to the actor's success, thereby evolving its experience management strategy in lockstep with the actor's growing capabilities. Empirically, Complementary RL outperforms outcome-based agentic RL baselines that do not learn from experience, achieving 10% performance improvement in single-task scenarios and exhibits robust scalability in multi-task settings. These results establish Complementary RL as a paradigm for efficient experience-driven agent learning.

---

## 论文详细总结（自动生成）

以下是对论文《Complementary Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：基于大语言模型（LLM）的智能体在强化学习（RL）中面临**样本效率低下**的问题。这主要归因于：1) 奖励信号稀疏且仅基于结果；2) 智能体无法有效利用跨回合的历史经验。
*   **研究动机**：现有的经验增强方法（如反射、经验池）多将经验视为**静态资源**，或者使用非自适应的提取器。随着训练进行，执行器（Actor）能力提升，而静态经验会产生“分布错位”（Distributional Misalignment），导致经验不仅无法提供帮助，甚至可能干扰学习。
*   **整体含义**：受神经科学中“互补学习系统”（CLS）启发，论文提出了一种让**策略执行器**与**经验提取器**在RL优化循环中**协同演化**的范式，使经验管理策略能随智能体能力的增长而动态调整。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：构建一个双模型协同系统。执行器（Actor）负责环境交互并根据经验进行决策；提取器（Extractor）负责从轨迹中提炼、维护经验池。两者均通过RL进行优化，形成闭环。
*   **关键技术细节**：
    *   **协同演化架构**：执行器 $\pi_\theta$ 使用基于结果的奖励优化；提取器 $\pi_\phi$ 根据其生成的经验是否促成执行器成功来获得奖励，并使用 **CISPO 算法**进行优化，以确保更新的稳定性。
    *   **经验池管理（ExperienceManager）**：引入集中式管理器，支持异步的经验提炼（Add）、更新（Update）和定期合并（Merge）。合并操作能有效消除冗余和冲突。
    *   **执行器优化策略（Split GRPO）**：为了防止执行器过度依赖外部经验而丧失自身能力，将采样轨迹分为“有经验引导”和“无经验引导”两组。提出**分组优势估计**，在各组内独立归一化优势值，确保训练信号的完整性。
    *   **主动检索（Search-and-Ask）**：允许执行器在决策过程中主动向经验池发起上下文相关的查询，获取更具针对性的指导。

### 3. 实验设计
*   **实验场景**：涵盖了四个具有挑战性的开放式环境：
    1.  **MiniHack**：基于网格的导航与解谜任务。
    2.  **WebShop**：模拟真实网络购物交互。
    3.  **ALFWorld**：具身智能体文本交互环境。
    4.  **SWE-Bench**：解决真实的 GitHub 软件工程问题。
*   **对比基准（Benchmarks）**：
    *   **Baseline**：不使用经验的纯 RL 训练。
    *   **Offline Exp.**：使用预先构建的静态经验池。
    *   **Static Online Exp.**：动态维护经验池但提取器不参与演化（固定参数）。
    *   **Exp. Only**：仅优化提取器而固定执行器。

### 4. 资源与算力
*   **模型使用**：执行器主要采用 **Qwen2.5-7B-Instruct**，提取器采用 **Qwen3-4B-Thinking**（部分实验使用了 30B 模型）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到使用了 Megatron 作为训练引擎，vLLM 作为推理引擎，并设计了全异步框架以最大化吞吐量，实验表明该框架在扩展到 1024 个并行环境时几乎不增加额外的耗时。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了单任务（4个环境独立训练）和多任务（3任务及6任务混合）实验。
    *   针对核心组件（Merge、Search-and-Ask、分组策略等）进行了详细的消融实验。
    *   探讨了提取器模型规模（4B vs 30B）对结果的影响。
*   **充分性与公平性**：实验设置了统一的超参数，对比了多种经验利用范式，并提供了测试时“带经验”与“不带经验”的性能对比，验证了经验是否被“内化”。整体实验设计较为客观、充分，覆盖了从简单导航到复杂代码修复的多种维度。

### 6. 主要结论与发现
*   **协同演化至关重要**：静态或非自适应的经验在训练后期会失效，而协同演化的 Complementary RL 在单任务中比基线提升了约 **10%** 的成功率。
*   **效率提升**：智能体不仅成功率更高，且完成任务所需的步数显著减少（如在 MiniHack 中减少 1.5 倍步数），证明经验引导了更有效的决策。
*   **多任务扩展性**：在多任务混合训练中，该方法展现出强大的鲁棒性，且能提取出跨任务的通用策略（如“如何处理陷入循环”的通用经验）。
*   **内化效应**：即使在测试时不提供经验，经过协同演化训练的执行器性能仍优于基线，说明经验已部分转化为模型参数。

### 7. 优点（亮点）
*   **生物启发式创新**：将神经科学的互补学习理论成功引入 LLM 智能体训练，解决了经验失效的痛点。
*   **异步系统设计**：通过 `ExperienceManager` 解决了经验提炼与策略训练之间的同步阻塞问题，保证了工业级的大规模训练效率。
*   **算法稳定性**：提出的分组优势估计（Split GRPO）和提取器优化（CISPO）有效缓解了 RL 训练中的分布偏移和过拟合问题。

### 8. 不足与局限
*   **自适应蒸馏的风险**：在与自蒸馏（Self-Distillation）结合的实验中，后期出现了性能崩溃，说明经验内化与强化学习目标的平衡仍需更精细的调优。
*   **计算开销**：虽然异步设计减少了延迟，但双模型架构（Actor + Extractor + Embedding）相比单模型 RL 仍需消耗更多的显存和推理资源。
*   **任务覆盖**：虽然涵盖了 SWE-Bench，但对于极长序列或极高复杂度的推理任务，经验池的检索精度和上下文窗口限制可能成为瓶颈。

（完）
