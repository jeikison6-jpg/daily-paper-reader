---
title: "VectorWorld: Efficient Streaming World Model via Diffusion Flow on Vector Graphs"
title_zh: VectorWorld：基于矢量图扩散流的高效流式世界模型
authors: "Chaokang Jiang, Desen Zhou, Jiuming Liu, Kevin Li Sun"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17652v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 用于自动驾驶策略的生成式世界模型
tldr: VectorWorld是一个针对自动驾驶闭环评估的高效流式世界模型。针对现有模型初始化不匹配、实时性差及长时仿真动力学失效等问题，该模型通过生成以自我为中心的矢量图瓦片，结合运动感知VAE、单步扩散流补全和物理对齐的NPC策略，实现了高保真、实时的长距离（1km+）闭环仿真。
selection_source: fresh_fetch
motivation: 现有生成式世界模型在闭环评估中面临初始化不匹配、采样延迟高以及长时仿真中动力学不可行性累积的问题。
method: 提出VectorWorld模型，利用运动感知VAE对齐初始化，采用单步扩散流进行实时矢量图补全，并引入物理对齐的ΔSim策略稳定长时仿真。
result: 在Waymo和nuPlan数据集上，该方法显著提升了地图结构保真度和初始化有效性，支持超过1公里的稳定实时闭环回放。
conclusion: VectorWorld通过结合矢量图生成与物理约束策略，为自动驾驶提供了一个高效、稳定且符合动力学要求的流式仿真环境。
---

## 摘要
自动驾驶策略的闭环评估需要超越日志回放的交互式仿真。然而，现有的生成式世界模型在闭环中往往性能下降，原因在于：(i) 无历史信息的初始化与策略输入不匹配；(ii) 多步采样延迟超出了实时性预算；(iii) 长时界下运动学不可行性的累积。我们提出了 VectorWorld，这是一种流式世界模型，能够在展开（rollout）过程中增量生成以自车为中心的 $64 \mathrm{m}\times 64\mathrm{m}$ 车道-智能体矢量图瓦片。VectorWorld 通过运动感知门控 VAE 产生与策略兼容的交互状态，从而使初始化与基于历史信息的策略保持一致。它通过无求解器的一步掩码补全实现了实时外扩（outpainting），该补全采用边缘门控关系 DiT，并使用区间条件 MeanFlow 和基于 JVP 的大步长监督进行训练。为了稳定长时界展开，我们引入了 $\Delta$Sim，这是一种符合物理规律的非自车（NPC）策略，具有离散-连续混合动作和可微运动学 Logit 整形。在 Waymo Open Motion 和 nuPlan 数据集上，VectorWorld 提高了地图结构的保真度和初始化的有效性，并支持稳定、实时的 $1\mathrm{km}+$ 闭环展开。

## Abstract
Closed-loop evaluation of autonomous-driving policies requires interactive simulation beyond log replay. However, existing generative world models often degrade in closed loop due to (i) history-free initialization that mismatches policy inputs, (ii) multi-step sampling latency that violates real-time budgets, and (iii) compounding kinematic infeasibility over long horizons. We propose VectorWorld, a streaming world model that incrementally generates ego-centric $64 \mathrm{m}\times 64\mathrm{m}$ lane--agent vector-graph tiles during rollout. VectorWorld aligns initialization with history-conditioned policies by producing a policy-compatible interaction state via a motion-aware gated VAE. It enables real-time outpainting via solver-free one-step masked completion with an edge-gated relational DiT trained with interval-conditioned MeanFlow and JVP-based large-step supervision. To stabilize long-horizon rollouts, we introduce $Δ$Sim, a physics-aligned non-ego (NPC) policy with hybrid discrete--continuous actions and differentiable kinematic logit shaping. On Waymo open motion and nuPlan, VectorWorld improves map-structure fidelity and initialization validity, and supports stable, real-time $1\mathrm{km}+$ closed-loop rollouts (\href{https://github.com/jiangchaokang/VectorWorld}{code}).

---

## 论文详细总结（自动生成）

### VectorWorld: 基于矢量图扩散流的高效流式世界模型论文总结

#### 1. 核心问题与研究动机
论文指出，现有的生成式自动驾驶世界模型在**闭环评估**中存在三大核心瓶颈：
*   **初始化不匹配（冷启动问题）：** 许多模型生成的是 $t=0$ 的静态快照，而反应式驾驶策略通常依赖历史运动信息，导致仿真开始时出现不稳定的瞬态（如尖锐的加减速）。
*   **采样延迟高：** 基于扩散模型的生成器通常需要多步迭代，无法满足流式外扩（Streaming Outpainting）的实时性要求。
*   **长时界运动学漂移：** 微小的运动学违规会在长距离（公里级）仿真中累积，导致场景崩溃或不可行。

#### 2. 方法论
VectorWorld 采用矢量图（Vector Graph）表示，通过流式生成 $64\mathrm{m} \times 64\mathrm{m}$ 的局部瓦片来构建无限长世界。
*   **交互状态接口（Motion-aware Gated VAE）：** 引入门控机制融合智能体的静态状态和运动历史代码。门控能自动识别静态（如路边停靠）和动态智能体，为下游策略提供“热启动”输入。
*   **边缘门控关系 DiT（Edge-Gated Relational DiT）：** 针对异构矢量图（车道与智能体），在 Transformer 中引入基于边缘特征的注意力偏置（Bias）和值门控（Value Gating），显式约束车道拓扑连接和智能体-车道对齐。
*   **区间条件 MeanFlow 与 JVP 监督：** 为了实现**单步（One-step）**生成，模型采用 MeanFlow 框架，并引入基于雅可比-向量积（JVP）的一阶修正项进行大步长监督，确保在无迭代求解器的情况下仍能保持结构完整性。
*   **$\Delta$Sim 物理对齐策略：** 针对 NPC（非自车）行为，提出混合离散-连续动作空间，并结合可微运动学 Logit 整形和 DKAL 损失函数，在训练和推理阶段同时约束物理可行性。

#### 3. 实验设计
*   **数据集：** Waymo Open Motion 和 nuPlan。
*   **基准方法（Benchmarks）：** 对比了 SLEDGE (L/XL) 和 ScenDream (B/L) 等先进的矢量化场景生成模型。
*   **评估指标：** 
    *   **离线指标：** Fréchet 距离 (FD)、路线长度、端点距离 (EPD)、初始碰撞率、拓扑保真度。
    *   **闭环指标：** 碰撞率、越野率、成功率、路线进度、加加速度（Jerk）。
*   **实验场景：** 包括初始场景生成、流式外扩（Outpainting）以及基于 PPO 策略的压力测试和强化学习训练。

#### 4. 资源与算力
论文在附录 A.13 中明确记录了算力使用情况：
*   **GPU 型号：** NVIDIA H20。
*   **配置：** 
    *   **VAE 训练：** 8 张 GPU，约 60,000 步。
    *   **MeanFlow 生成器：** 8 张 GPU，约 100,000 步（训练时长约 480 GPU 小时）。
    *   **$\Delta$Sim 策略：** 4 张 GPU，约 10,000 步。
*   **推理效率：** 生成单个瓦片仅需约 **6ms**（含 VAE 解码），完全满足实时仿真需求。

#### 5. 实验数量与充分性
实验设计非常全面且具有客观性：
*   **多维度验证：** 涵盖了从静态生成质量到动态闭环稳定性的全流程评估。
*   **消融实验：** 针对 VAE 门控、DiT 关系模块、MeanFlow 训练目标（JVP/双时间通道）以及 $\Delta$Sim 的各个组件均做了详尽的对比实验（表 9-13）。
*   **跨后端验证：** 验证了在 VectorWorld 中训练的策略能够迁移到日志回放和其他仿真器中，证明了模型并非简单的“自洽”，而是具有真实的泛化价值。
*   **长距离测试：** 成功演示了超过 1 公里的稳定闭环仿真，这在生成式模型中较为罕见。

#### 6. 主要结论与发现
*   **单步生成的突破：** 通过 MeanFlow 和 JVP 修正，VectorWorld 实现了在极低延迟下生成高质量、结构一致的矢量地图。
*   **初始化至关重要：** 带有运动历史的“热启动”能显著降低闭环初期的碰撞率和不稳定性（Jerk 从 16.6 降至 9.6）。
*   **物理约束的必要性：** 简单的模仿学习不足以维持长时仿真，物理对齐的策略（$\Delta$Sim）是防止仿真崩溃的关键。
*   **训练价值：** 在 VectorWorld 提供的压力环境下训练，使 PPO 策略在困难场景下的成功率从 25.0% 提升至 56.0%。

#### 7. 优点与亮点
*   **极高的效率：** 6ms 的生成速度使其成为首个真正意义上支持实时流式外扩的矢量世界模型。
*   **结构保真度：** 边缘门控 DiT 有效解决了矢量生成中常见的车道断裂和智能体漂移问题。
*   **系统性解决闭环痛点：** 论文不仅关注生成图像的“好看”，更深入解决了初始化、延迟和长时稳定性等工程部署难题。

#### 8. 不足与局限
*   **地图元素覆盖有限：** 目前主要关注车道中心线，尚未涵盖路缘、人行横道等更精细的语义地图元素。
*   **极端工况物理性：** 虽然引入了运动学约束，但在极端避障等高动态工况下，NPC 的物理真实性仍有提升空间。
*   **细粒度分布偏差：** 在某些特定数据集（如 nuPlan）的细粒度分布指标（如智能体间距 JSD）上，单步生成相比多步扩散仍存在微小差距。

（完）
