---
title: Trust-Region Noise Search for Black-Box Alignment of Diffusion and Flow Models
title_zh: 扩散与流模型黑盒对齐的置信域噪声搜索
authors: "Niklas Schweiger, Daniel Cremers, Karnik Ram"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14504v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 使用信任区域搜索对扩散模型进行黑盒对齐
tldr: 本研究针对扩散和流模型在推理阶段的对齐问题，指出当前方法多受限于奖励函数的可微性或计算开销。为此，作者提出了置信域噪声搜索（TRS）算法，该算法将生成和奖励模型视为黑盒，仅优化初始源噪声。TRS 在全局探索与局部利用间取得了良好平衡，且无需复杂调参。实验证明，TRS 在图像、分子及蛋白质设计等任务中均显著优于现有推理对齐方法，展现了极强的通用性。
selection_source: fresh_fetch
motivation: 现有的扩散模型对齐方法通常依赖可微奖励函数或特定的模型架构，且在计算效率和内存占用上存在局限。
method: 提出一种基于置信域的搜索算法（TRS），通过将生成和奖励模型视为黑盒，仅对初始源噪声进行优化。
result: 在文本转图像、分子设计和蛋白质设计等多个领域，TRS 的表现均显著优于基础模型及其他推理时对齐技术。
conclusion: TRS 提供了一种简单、通用且高效的黑盒对齐方案，能够灵活适应各种生成设置和奖励模型。
---

## 摘要
优化扩散和流模型的噪声样本是在推理阶段将这些模型与目标奖励对齐的一种日益流行的方法。然而，我们观察到这些方法通常受限于可微或低成本的奖励模型、底层预训练生成模型的公式，或者在内存/计算上效率低下。与此不同，我们提出了一种简单的基于置信域的搜索算法（TRS），该算法将预训练的生成模型和奖励模型视为黑盒，仅优化源噪声。我们的方法在全局探索和局部利用之间实现了良好的平衡，具有通用性，且只需极少的超参数调整即可轻松适应各种生成设置和奖励模型。我们在文本生成图像、分子和蛋白质设计任务中评估了 TRS，与基础生成模型以及其他优化源噪声样本（在扩散模型中甚至优化整个逆向采样噪声轨迹）的推理阶段对齐方法相比，获得了显著改进的输出样本。我们的源代码已公开。

## Abstract
Optimizing the noise samples of diffusion and flow models is an increasingly popular approach to align these models to target rewards at inference time. However, we observe that these approaches are usually restricted to differentiable or cheap reward models, the formulation of the underlying pretrained generative model, or are memory/compute inefficient. We instead propose a simple trust-region based search algorithm (TRS) which treats the pre-trained generative and reward models as a black-box and only optimizes the source noise. Our approach achieves a good balance between global exploration and local exploitation, and is versatile and easily adaptable to various generative settings and reward models with minimal hyperparameter tuning. We evaluate TRS across text-to-image, molecule and protein design tasks, and obtain significantly improved output samples over the base generative models and other inference-time alignment approaches which optimize the source noise sample, or even the entire reverse-time sampling noise trajectories in the case of diffusion models. Our source code is publicly available.

---

## 论文详细总结（自动生成）

这是一份关于论文《Trust-Region Noise Search for Black-Box Alignment of Diffusion and Flow Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
预训练的扩散模型（Diffusion）和流匹配模型（Flow Matching）虽然能生成高质量数据，但在特定下游任务中（如生成具有高结合亲和力的分子或符合复杂描述的图像）往往需要进行**推理阶段对齐（Inference-time Alignment）**。
*   **现有挑战**：
    *   **梯度方法**：需要奖励函数可微，且反向传播穿过整个采样轨迹，显存开销巨大，且容易使生成结果偏离原始数据流形。
    *   **序列搜索方法**：依赖中间奖励估计，难以处理昂贵的奖励函数，且并行效率低。
    *   **黑盒搜索方法**：虽然通用，但在全局探索（寻找新区域）与局部利用（精细优化）之间难以平衡。
*   **研究动机**：提出一种简单、高效、不依赖梯度的算法，通过仅优化初始源噪声（Source Noise）来实现模型与奖励的对齐。

### 2. 方法论：置信域噪声搜索（TRS）
TRS 算法的核心思想是将生成模型和奖励模型视为一个整体黑盒函数，通过在噪声空间中维护多个自适应的“置信域”来寻找最优初始噪声 $x_0$。
*   **核心流程**：
    1.  **预热阶段**：从先验分布中随机采样一批噪声，评估奖励，选择表现最好的 $k$ 个点作为初始置信域的中心。
    2.  **结构化采样**：在每个置信域中心附近生成扰动。为了处理高维空间，引入了**随机坐标掩码（Stochastic Coordinate Mask）**，即每次只扰动部分维度。
    3.  **自适应调整**：根据奖励反馈更新置信域的边长 $\ell$。如果连续多次改进，则扩大区域（探索）；如果连续失败，则缩小区域（利用）。
    4.  **全局重中心化**：每轮迭代后，将所有置信域重新定位到当前全局最优的 $k$ 个点附近。这种机制允许算法从表现不佳的区域“跳跃”到更有潜力的区域。
*   **关键技术**：支持 Sobol 序列（低维）和高斯扰动（高维），确保在不同维度的噪声空间（如 SDXL 的 65,536 维）中均能有效搜索。

### 3. 实验设计
论文在三个极具代表性的领域进行了验证：
*   **文本转图像 (T2I)**：
    *   **模型**：Stable Diffusion v1.5, SDXL-Lightning。
    *   **数据集**：DrawBench（包含 200 个复杂提示词）。
    *   **奖励模型**：ImageReward, HPSv2, Aesthetic Predictor。
*   **分子生成**：
    *   **模型**：EquiFM（基于 ODE 的流匹配模型）。
    *   **场景**：多属性目标匹配（如同时优化极化率、偶极矩等 3-6 个化学属性）。
*   **蛋白质设计**：
    *   **模型**：Proteina（3D 骨架生成）。
    *   **奖励模型**：设计位点奖励（基于 ProteinMPNN 和 ESMFold 的自一致性 RMSD）。
*   **对比方法**：包括梯度方法（OC-Flow）、树搜索（DTS*）、高斯过程搜索（Fast Direct）以及基础黑盒方法（Random Search, Zero-Order Search）。

### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA A100-SXM4-40GB** GPU 上运行。
*   **效率对比**：论文详细记录了运行时间（Runtime）和显存占用（VRAM）。
    *   在 SD1.5 任务中，TRS 比 DTS* 快约 4 倍。
    *   TRS 的显存占用远低于基于梯度的 OC-Flow（尤其在处理高分辨率图像时）。
    *   对于蛋白质设计等昂贵奖励任务，TRS 通过高效的批处理（Batching）显著降低了总耗时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了图像（200 提示词）、分子（200 个随机目标组合）、蛋白质（不同长度 nres=50/100）的大量测试。
*   **消融实验**：非常充分，涵盖了中心选择策略、预热预算比例、置信域数量 $k$、扰动掩码概率、以及 ODE 与 SDE 采样动力学的对比。
*   **公平性**：所有方法均在相同的函数评价次数（NFE）预算下进行对比，确保了计算成本的对等。

### 6. 主要结论与发现
*   **性能领先**：TRS 在所有任务和所有奖励模型下均取得了最高的平均奖励值，显著优于现有的搜索和梯度方法。
*   **稳定性**：在分子生成中，TRS 在大幅提升属性匹配度的同时，保持了极高的分子稳定性和有效性，而梯度方法往往会导致生成结果脱离流形（有效性下降）。
*   **缩放效应**：随着计算预算（NFE）的增加，TRS 的表现提升比其他方法更持久，不易陷入早期饱和。
*   **采样偏好**：TRS 在确定性 ODE 采样下表现最佳，因为其局部搜索逻辑与确定性轨迹更契合。

### 7. 优点
*   **通用性极强**：完全黑盒，不要求奖励函数可微，不要求生成模型有特定架构。
*   **内存友好**：无需存储计算图或中间轨迹，适合在大模型（如 SDXL）上部署。
*   **自适应平衡**：通过置信域机制自动切换探索与利用，比简单的随机搜索或零阶优化更智能。
*   **即插即用**：超参数在不同任务间表现稳健，无需针对每个新奖励模型进行繁琐调参。

### 8. 不足与局限
*   **奖励模型依赖**：TRS 的上限受限于奖励模型的准确性。如果奖励模型存在“漏洞”（Reward Hacking），TRS 会生成视觉上奇怪但得分很高的样本（论文在附录中展示了此类失败案例）。
*   **多样性权衡**：由于算法最终会收敛到局部最优区域，生成的样本在多样性上可能不如原始模型，虽然论文尝试通过多区域搜索来缓解，但本质上仍是单点优化逻辑。
*   **采样限制**：在随机性极强的 SDE 采样下，TRS 的局部搜索信号会被噪声淹没，效果提升不如 ODE 采样明显。

（完）
