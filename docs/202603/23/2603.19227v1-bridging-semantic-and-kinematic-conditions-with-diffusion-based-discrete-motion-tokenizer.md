---
title: Bridging Semantic and Kinematic Conditions with Diffusion-based Discrete Motion Tokenizer
title_zh: 利用基于扩散模型的离散运动分词器连接语义与运动学条件
authors: "Chenyang Gu, Mingyuan Zhang, Haozhe Xie, Zhongang Cai, Lei Yang, Ziwei Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19227v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 基于扩散的运动合成与规划
tldr: 针对人体动作生成中语义理解与运动学控制难以兼顾的问题，本文提出了一种三阶段框架（感知、规划、控制）。核心创新是MoTok，一种基于扩散模型的离散动作分词器，它通过扩散解码器将语义抽象与精细重建解耦，实现了在极低token数量下的高保真重建。该方法在规划阶段处理粗略约束，在控制阶段利用扩散优化处理精细约束，显著提升了动作生成的准确性与质量。
selection_source: fresh_fetch
motivation: 旨在结合离散token生成器在语义处理上的优势与连续扩散模型在运动学控制上的长处，解决现有方法在复杂约束下保真度下降的问题。
method: 提出包含感知、规划、控制的三阶段框架，并引入MoTok分词器，利用扩散解码器将动作恢复任务从语义编码中分离出来。
result: 在HumanML3D数据集上，该方法仅使用六分之一的token数量，便将轨迹误差从0.72cm降至0.08cm，FID从0.083显著提升至0.029。
conclusion: 该研究证明了通过解耦语义抽象与运动学细节，可以有效解决离散生成中的控制冲突，实现更高质量且受控的动作合成。
---

## 摘要
先前的运动生成主要遵循两种范式：擅长运动学控制的连续扩散模型，以及在语义调节方面有效的基于离散 token 的生成器。为了结合两者的优势，我们提出了一个三阶段框架，包括条件特征提取（感知）、离散 token 生成（规划）和基于扩散的运动合成（控制）。该框架的核心是 MoTok，这是一种基于扩散的离散运动分词器，它通过将运动恢复任务委托给扩散解码器，实现了语义抽象与细粒度重建的解耦，从而在保持运动保真度的同时实现了紧凑的单层 token。对于运动学条件，粗粒度约束在规划阶段引导 token 生成，而细粒度约束则在控制阶段通过基于扩散的优化来强制执行。这种设计防止了运动学细节干扰语义 token 的规划。在 HumanML3D 数据集上，我们的方法在仅使用六分之一 token 的情况下，显著提高了相对于 MaskControl 的可控性和保真度，将轨迹误差从 0.72 cm 降低到 0.08 cm，并将 FID 从 0.083 降低到 0.029。与在更强运动学约束下性能会下降的先前方法不同，我们的方法提升了保真度，将 FID 从 0.033 降低到 0.014。

## Abstract
Prior motion generation largely follows two paradigms: continuous diffusion models that excel at kinematic control, and discrete token-based generators that are effective for semantic conditioning. To combine their strengths, we propose a three-stage framework comprising condition feature extraction (Perception), discrete token generation (Planning), and diffusion-based motion synthesis (Control). Central to this framework is MoTok, a diffusion-based discrete motion tokenizer that decouples semantic abstraction from fine-grained reconstruction by delegating motion recovery to a diffusion decoder, enabling compact single-layer tokens while preserving motion fidelity. For kinematic conditions, coarse constraints guide token generation during planning, while fine-grained constraints are enforced during control through diffusion-based optimization. This design prevents kinematic details from disrupting semantic token planning. On HumanML3D, our method significantly improves controllability and fidelity over MaskControl while using only one-sixth of the tokens, reducing trajectory error from 0.72 cm to 0.08 cm and FID from 0.083 to 0.029. Unlike prior methods that degrade under stronger kinematic constraints, ours improves fidelity, reducing FID from 0.033 to 0.014.

---

## 论文详细总结（自动生成）

### 论文总结：利用基于扩散模型的离散运动分词器连接语义与运动学条件 (MoTok)

#### 1. 核心问题与整体含义
人体动作生成领域目前存在两大主流范式：**连续扩散模型**（擅长精细的运动学控制，但推理慢且语义对齐难）和**离散 Token 生成器**（擅长处理复杂的语义条件，但往往将高层语义与底层细节混杂在一起）。
**核心问题**在于：现有的离散分词器为了保证重建精度，通常需要极高的 Token 采样率或复杂的层次化编码，这不仅增加了下游生成器的计算负担，还导致底层运动学约束（如手部轨迹）与高层语义指令（如文字描述）在 Token 空间内发生冲突。本论文旨在通过一种“感知-规划-控制”的解耦框架，实现高效、高保真的受控动作生成。

#### 2. 方法论
论文提出了一个三阶段框架，其核心是 **MoTok**（基于扩散的离散运动分词器）：
*   **核心思想**：将“语义抽象”与“细粒度重建”解耦。让离散 Token 仅负责捕捉动作的语义结构，而将复杂的运动细节恢复任务交给一个**条件扩散解码器**。
*   **关键技术细节**：
    *   **MoTok 分词器**：包含卷积编码器（下采样动作序列）、向量量化器（映射到离散 Codebook）和扩散解码器。扩散解码器利用 AdaIN 机制将 Token 特征注入到去噪过程中。
    *   **三阶段流程**：
        1.  **感知 (Perception)**：将文字编码为全局条件，将轨迹/关键点编码为局部条件。
        2.  **规划 (Planning)**：在离散 Token 空间内生成动作序列。支持自回归 (AR) 或离散扩散 (DDM) 两种生成器。
        3.  **控制 (Control)**：通过 MoTok 的扩散解码器合成最终动作，并在去噪过程中通过梯度优化强制执行精细的运动学约束。
    *   **粗到细约束 (Coarse-to-Fine)**：运动学约束在规划阶段作为“粗略引导”，在控制阶段作为“精细约束”，避免了底层细节干扰高层语义规划。

#### 3. 实验设计
*   **数据集**：主要使用 **HumanML3D** 和 **KIT-ML**，这是目前人体动作生成领域最权威的两个文本-动作对齐数据集。
*   **Benchmark 与对比方法**：
    *   **文本到动作生成**：对比了 MoMask, T2M-GPT, MotionDiffuse, ReMoDiffuse 等。
    *   **可控动作生成**：对比了 MaskControl, OmniControl, InterControl, CrowdMoGen 等。
*   **评估指标**：包括 FID（动作保真度）、R-Precision（语义对齐度）、多样性、轨迹误差 (Traj. Err.) 和脚部滑动率 (Foot Skating) 等。

#### 4. 资源与算力
*   **训练配置**：论文提到在 **8 张 GPU** 上进行训练（未明确具体型号，但通常为 A100 或 H100 级别）。
*   **训练时长**：所有模型训练 24 个 epoch，每个 epoch 约 2000 次迭代。
*   **推理效率**：在 **H100** 上进行了推理速度测试。MoTok 生成单个序列耗时约 2.63s，而对比方法 MaskControl 耗时达 32.79s，证明了 MoTok 在效率上的巨大优势。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括在两个主流数据集上的性能对比、针对不同控制目标（盆骨、随机关节、多关节）的鲁棒性测试。
*   **消融实验**：针对解码器架构（纯卷积 vs. 扩散头 vs. 扩散卷积）、Token 维度、下采样率（2x 到 16x）、卷积核大小以及控制信号注入位置进行了多维度消融。
*   **客观性**：实验采用了 20 次运行取平均值及 95% 置信区间的标准做法，对比基准涵盖了近年来的 SOTA 方法，实验设计客观且公平。

#### 6. 主要结论与发现
*   **极高的压缩率**：MoTok 仅需 **1/6 的 Token 数量**（相对于 MaskControl），就能达到甚至超越现有方法的性能。
*   **保真度不降反升**：传统方法在加入强运动学约束时，动作往往会变得僵硬（FID 变差），而 MoTok 在约束增强时，FID 反而从 0.033 提升至 0.014，实现了语义与运动学的完美融合。
*   **误差大幅降低**：在轨迹控制任务中，将平均误差从 0.72 cm 降低到了 0.08 cm。

#### 7. 优点
*   **架构优雅**：通过解耦重建与抽象，解决了离散模型在处理连续约束时的“先天不足”。
*   **通用性强**：统一的接口使其能够无缝适配自回归和离散扩散两种主流生成器。
*   **效率与质量兼得**：大幅减少了下游生成器的序列长度，降低了计算开销，同时提升了生成动作的自然度。

#### 8. 不足与局限
*   **推理延迟**：虽然比 MaskControl 快，但由于后端使用了扩散解码器，其推理速度仍慢于纯卷积解码的 VQ-VAE 方法。
*   **应用限制**：目前主要集中在人体动作生成，尚未在更复杂的物体交互或多智能体协同场景中进行大规模验证。
*   **超参数敏感性**：消融实验显示，CFG Scale 和下采样率对最终效果影响较大，针对不同任务可能需要精细调优。

（完）
