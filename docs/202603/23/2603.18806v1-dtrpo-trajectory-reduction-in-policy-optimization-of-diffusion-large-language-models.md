---
title: "dTRPO: Trajectory Reduction in Policy Optimization of Diffusion Large Language Models"
title_zh: dTRPO：扩散大语言模型策略优化中的轨迹缩减
authors: "Wenxuan Zhang, Lemeng Wu, Changsheng Zhao, Ernie Chang, Mingchen Zhuge, Zechun Liu, Andy Su, Hanxian Huang, Jun Chen, Chong Zhou, Raghuraman Krishnamoorthi, Vikas Chandra, Mohamed Elhoseiny, Wei Wen"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18806v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 针对扩散大语言模型的离线策略优化
tldr: 针对扩散大语言模型（dLLM）在对齐人类偏好时轨迹概率计算成本高的问题，本文提出了dTRPO。该方法通过理论证明，利用新去掩码标记的概率比作为中间状态的无偏估计，并结合重掩码状态的单次前向传播来估算全轨迹概率。实验表明，dTRPO在7B模型上显著提升了推理和指令遵循能力，同时大幅提高了训练和生成效率。
selection_source: fresh_fetch
motivation: 旨在降低扩散大语言模型在策略优化过程中计算全轨迹概率的高昂成本，以实现大规模离线训练。
method: 提出dTRPO，通过新去掩码标记的概率比和重掩码状态的单次前向传播来简化轨迹概率估算。
result: "在7B模型上，dTRPO在STEM、编程和指令遵循任务中分别实现了最高9.6%、4.3%和3.0%的性能提升。"
conclusion: dTRPO通过轨迹缩减策略有效提升了扩散大语言模型的对齐性能与训练效率，为该领域的策略优化提供了高效方案。
---

## 摘要
扩散大语言模型（dLLMs）为语言生成引入了一种新范式，这反过来也为将其与人类偏好对齐带来了新挑战。在这项工作中，我们旨在通过降低轨迹概率计算的成本来改进 dLLMs 的策略优化，从而实现大规模离线策略训练。我们证明了：(i) 在参考策略正则化下，新取消掩码（unmasked）的 token 的概率比是中间扩散状态概率比的无偏估计；(ii) 完整轨迹的概率可以通过对重新掩码（re-masked）的最终状态进行单次前向传递来有效估计。通过将这两种轨迹缩减策略整合到策略优化目标中，我们提出了轨迹缩减策略优化（dTRPO）。我们在指令遵循和推理基准测试中，对 7B 参数规模的 dLLMs 评估了 dTRPO。结果表明，它显著提升了最先进 dLLMs 的核心性能，在 STEM 任务上获得了高达 9.6% 的提升，在编程任务上提升高达 4.3%，在指令遵循任务上提升高达 3.0%。此外，由于其离线和单次前向传递的特性，dTRPO 表现出强大的训练效率，并通过高质量的输出提升了生成效率。

## Abstract
Diffusion Large Language Models (dLLMs) introduce a new paradigm for language generation, which in turn presents new challenges for aligning them with human preferences. In this work, we aim to improve the policy optimization for dLLMs by reducing the cost of the trajectory probability calculation, thereby enabling scaled-up offline policy training. We prove that: (i) under reference policy regularization, the probability ratio of the newly unmasked tokens is an unbiased estimate of that of intermediate diffusion states, and (ii) the probability of the full trajectory can be effectively estimated with a single forward pass of a re-masked final state. By integrating these two trajectory reduction strategies into a policy optimization objective, we propose Trajectory Reduction Policy Optimization (dTRPO). We evaluate dTRPO on 7B dLLMs across instruction-following and reasoning benchmarks. Results show that it substantially improves the core performance of state-of-the-art dLLMs, achieving gains of up to 9.6% on STEM tasks, up to 4.3% on coding tasks, and up to 3.0% on instruction-following tasks. Moreover, dTRPO exhibits strong training efficiency due to its offline, single-forward nature, and achieves improved generation efficiency through high-quality outputs.

---

## 论文详细总结（自动生成）

以下是对论文《dTRPO: Trajectory Reduction in Policy Optimization of Diffusion Large Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：扩散大语言模型（dLLMs）作为自回归模型（ARMs）的竞争者，具有双向上下文感知和并行解码等优势。然而，在将模型与人类偏好对齐（Post-training）方面，dLLMs 面临巨大挑战。
*   **核心问题**：ARMs 的生成概率可以自然地分解为 token 级别的乘积，仅需一次前向传播即可计算。而 dLLMs 通过多步扩散过程生成，计算完整的轨迹概率通常需要大量的模型前向传播，导致训练成本极高且数值不稳定。
*   **研究动机**：旨在通过“轨迹缩减”（Trajectory Reduction）技术，降低 dLLMs 策略优化的计算开销，实现高效、可扩展的离线偏好对齐训练。

### 2. 方法论：核心思想与关键技术
dTRPO 的核心思想是通过理论证明简化轨迹概率比的计算：
*   **状态缩减（State Reduction）**：证明了可以通过在每个扩散块（Block）中随机采样一个时间步来估计整个轨迹的对数概率。这是一种无偏估计，将计算复杂度从 $T$ 次前向传播降低到 $N_B$（块数）次。
*   **比例缩减（Ratio Reduction）**：证明了在计算当前策略与参考策略的概率比时，扩散过程中的调度相关系数（如掩码率 $\alpha_t$）会相互抵消。最终，轨迹概率比仅取决于**新去掩码（newly unmasked）token** 的分类概率比。
*   **dTRPO 目标函数**：结合上述两点，将 DPO（直接偏好优化）引入 dLLMs。其损失函数仅需对比偏好样本对中新去掩码 token 的预测概率。
*   **关键技术细节**：
    *   **块注意力（Block Attention）**：设计了一种特殊的注意力掩码，使得在一次前向传播中，不同块的 token 可以观察到不同的掩码状态，从而实现单次前向传播完成概率估算。
    *   **推理对齐调度**：在训练中采用与推理时一致的“Top-K 置信度去掩码”策略，增强了训练与部署的一致性。

### 3. 实验设计
*   **基准模型**：以 Fast-dLLM-v2-7B（基于 Qwen2.5-7B-Instruct 转换而来）为骨干网络。
*   **数据集**：使用 SmolTalk2 偏好数据集，并混合了数学（Argilla）和代码（Vezora）偏好数据，共 500K 条样本。
*   **对比方法**：
    *   开源 dLLMs：LLaDA, Dream, Fast-dLLM-v2。
    *   对齐算法变体：ELBO（变分下界）、VRPO、带有均值场近似的 DPO（DPO w/ MF）。
*   **评估基准**：STEM（GPQA, GSM8K, MATH）、代码（MBPP+, LCBv6, HumanEval+）、指令遵循（IFEval, Arena-Hard, MT-Bench）。

### 4. 资源与算力
*   **硬件设备**：使用了 **64 台 NVIDIA A100 (80GB) GPU**。
*   **训练时长**：总训练时间约为 **5 小时**（1 个 epoch）。
*   **效率提升**：每个训练样本仅需 4 次前向传播（策略模型和参考模型各 2 次），与 ARMs 的 DPO 训练成本相当，远低于需要在线采样的强化学习方法。

### 5. 实验数量与充分性
*   **实验规模**：在 9 个主流基准测试集上进行了零 shot 评估，涵盖了推理、编程和对话能力。
*   **消融实验**：非常充分。作者针对去掩码策略（Top-K vs Random）、采样步数、投影函数（DPO, IPO, RSO, APO）、骨干网络（扩展到 LLaDA）以及参数高效微调策略（MLP-only）均做了详细对比。
*   **客观性**：通过与多种 SOTA dLLM 变体在相同数据下对比，证明了 dTRPO 的优越性，实验设计客观公平。

### 6. 主要结论与发现
*   **性能显著提升**：dTRPO 在 STEM 任务上提升高达 9.6%，编程提升 4.3%，指令遵循提升 3.0%。
*   **缩小差距**：有效缩小了 dLLMs 与强力自回归模型（如 Qwen2.5-Instruct）之间的性能差距。
*   **高效性**：dTRPO 是一种纯离线方法，训练效率极高，且由于生成的答案质量更高，推理时的平均响应时间反而有所下降。
*   **通用性**：该方法不仅适用于块状扩散模型，也适用于长块（Long-block）扩散模型（如 LLaDA）。

### 7. 优点
*   **理论严谨**：通过数学证明提供了无偏估计的理论支撑，而非纯启发式设计。
*   **计算极其高效**：将原本复杂的扩散轨迹计算简化为类似 ARMs 的单次前向传播，解决了 dLLMs 训练难的痛点。
*   **推理对齐**：通过在训练中引入推理时的去掩码逻辑，使得模型在实际生成时表现更稳健。

### 8. 不足与局限
*   **性能上限**：虽然缩小了差距，但在某些复杂任务上仍略逊于顶尖的自回归模型（Qwen2.5-Instruct）。
*   **依赖参考模型**：作为离线偏好优化方法，其效果高度依赖于初始参考模型的质量。
*   **评估范围**：主要集中在 7B 规模模型，尚未在更大规模（如 70B+）或更长上下文的场景下验证其扩展性。

（完）
