---
title: Unleashing Guidance Without Classifiers for Human-Object Interaction Animation
title_zh: 释放用于人-物交互动画的无分类器引导
authors: "Ziyin Wang, Sirui Xu, Chuan Guo, Bing Zhou, Jiangshan Gong, Jian Wang, Yu-Xiong Wang, Liang-Yan Gui"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25734v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 无需分类器的引导用于人机交互动画生成
tldr: 针对生成真实人机交互（HOI）动画中依赖手工约束的问题，本文提出 LIGHT 框架。该方法利用扩散强制技术，通过为不同模态分配异步去噪进度，使较清晰的组件在交叉注意力机制下引导较模糊的组件，实现无分类器的引导。实验证明，这种基于进度的引导能显著提升接触真实感和对未知物体的泛化能力，减少了对手工先验的依赖。
selection_source: fresh_fetch
motivation: 现有的扩散模型生成 HOI 动画时过度依赖手工设计的接触先验或运动约束，限制了生成的灵活性和真实感。
method: 提出 LIGHT 框架，通过异步去噪进度让不同模态互为引导，并结合合成几何增强训练以提升接触语义的鲁棒性。
result: 实验表明，该方法在接触保真度、生成真实感以及对新物体和任务的泛化能力上均优于传统方法。
conclusion: 基于去噪进度的引导机制是一种有效的无分类器引导方案，能显著提升复杂人机交互动画的质量。
---

## 摘要
生成逼真的人-物交互（HOI）动画仍然具有挑战性，因为它需要对动态人体动作和多样的物体几何形状进行联合建模。先前的基于扩散的方法通常依赖于手工设计的接触先验或人为施加的运动学约束，以提高接触质量。我们提出了 LIGHT，这是一种数据驱动的替代方案，其中引导源自去噪步调（denoising pace）本身，从而减少了对手工设计先验的依赖。基于扩散强迫（diffusion forcing），我们将表示分解为特定模态的组件，并通过异步去噪调度分配个性化的噪声水平。在这种范式中，较清晰的组件通过交叉注意力引导噪声较大的组件，从而在无需辅助分类器的情况下产生引导。我们发现这种数据驱动的引导本质上具有接触感知能力，并且当训练通过广泛的合成物体几何形状进行增强时，这种引导可以得到进一步加强，从而促进接触语义对几何多样性的不变性。大量实验表明，与传统的无分类器引导相比，步调诱导的引导能更有效地反映接触先验的优势，同时实现更高的接触保真度、更逼真的 HOI 生成以及对未知物体和任务更强的泛化能力。

## Abstract
Generating realistic human-object interaction (HOI) animations remains challenging because it requires jointly modeling dynamic human actions and diverse object geometries. Prior diffusion-based approaches often rely on hand-crafted contact priors or human-imposed kinematic constraints to improve contact quality. We propose LIGHT, a data-driven alternative in which guidance emerges from the denoising pace itself, reducing dependence on manually designed priors. Building on diffusion forcing, we factor the representation into modality-specific components and assign individualized noise levels with asynchronous denoising schedules. In this paradigm, cleaner components guide noisier ones through cross-attention, yielding guidance without auxiliary classifiers. We find that this data-driven guidance is inherently contact-aware, and can be enhanced when training is augmented with a broad spectrum of synthetic object geometries, encouraging invariance of contact semantics to geometric diversity. Extensive experiments show that pace-induced guidance more effectively mirrors the benefits of contact priors than conventional classifier-free guidance, while achieving higher contact fidelity, more realistic HOI generation, and stronger generalization to unseen objects and tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Unleashing Guidance Without Classifiers for Human-Object Interaction Animation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖手工设计的物理约束（如反向运动学 IK）或外部预训练分类器的情况下，生成真实、连贯且符合物理常识的人-物交互（HOI）动画。
*   **研究背景**：现有的扩散模型在生成 HOI 动画时，经常出现手部悬空、物体穿模或接触不稳定的现象。为了解决这些问题，以往的方法通常引入复杂的运动学约束或专门的接触预测模块，但这牺牲了模型的通用性，且增加了设计负担。
*   **研究动机**：探索一种纯数据驱动的引导机制，利用扩散模型自身的去噪节奏（Denoising Pace）来产生隐含的接触感知引导，从而实现更自然、更具泛化能力的交互生成。

### 2. 方法论：LIGHT 框架
*   **核心思想**：基于“扩散强制”（Diffusion Forcing）理论，将交互序列分解为不同的模态（人体、手部、物体），并为它们分配异步的去噪进度。通过让“较清晰”的模态在交叉注意力机制下引导“较模糊”的模态，实现无分类器的引导。
*   **关键技术细节**：
    *   **模态分解（Token Separation）**：将每一帧的表示划分为人体躯干（Body）、手部（Hand）和物体（Object）三个独立的 Token 组。
    *   **异步去噪调度（Asynchronous Schedules）**：在推理时运行两个分支：
        1.  **均匀调度（Uniform Schedule）**：所有模态同步去噪。
        2.  **阶段性调度（Staged Schedule）**：使某一模态（如人体）保持比其他模态（如物体）更清晰的状态（即噪声水平更低）。
    *   **步调诱导引导（Pace-induced Guidance）**：利用两个分支预测结果的差异来调整去噪方向。公式上类似于无分类器引导（CFG），但其“无条件”分支被替换为具有不同噪声水平的“弱条件”分支，从而捕捉细粒度的接触信号。
    *   **接触感知形状谱增强（Contact-Aware Shape-Spectrum Augmentation）**：通过优化算法将原始交互动作迁移到具有不同几何形状的同类物体上，训练模型识别“接触语义”在几何变化下的不变性。

### 3. 实验设计
*   **数据集**：主要使用大规模交互数据集 **InterAct**，并对比了其子集 **BEHAVE** 和 **OMOMO**。
*   **Benchmark 与对比方法**：
    *   **对比方法**：包括 HOI-Diff（基于分类器引导）、CHOIS（基于条件生成）、InterDiff（基于物理信息扩散）和 Text2HOI（基于静态接触图预测）。
    *   **评估指标**：R-Precision（文本对齐度）、FID（生成真实感）、MM Dist（多模态距离）、穿模率（Pene）、接触率（Contact）和足部滑动比（FSR）。
*   **评估器改进**：为了更客观，作者在 InterAct 数据集上重新训练了特征提取模型，以更好地衡量人机交互的质量。

### 4. 资源与算力
*   **硬件型号**：使用了 **NVIDIA A100 GPU**。
*   **训练细节**：采用混合精度训练（FP16）和 Flash Attention。
*   **训练时长**：在单张 A100 GPU 上，模型通常在 **24 小时内** 即可收敛。
*   **推理效率**：对于 64 个样本（每样本 300 帧）的批处理，带引导的推理约需 72 秒。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的定量对比（表 1、D、E）、定性可视化（图 3、5）以及消融实验。
*   **消融实验**：涵盖了模态分离策略、引导强度（$\omega_2$）、去噪偏移量（$\delta$）以及数据增强的效果。
*   **充分性评价**：实验设计非常充分。除了标准指标外，还包含了**用户研究（User Study）**和**泛化性测试**（针对训练中未见过的物体类别），证明了该方法在不同场景下的稳健性。

### 6. 主要结论与发现
*   **引导机制的有效性**：异步去噪产生的“软引导”比传统的“硬丢弃”CFG 更能捕捉低层级的接触细节，显著降低了穿模和悬空现象。
*   **模态分离的重要性**：将手部作为独立模态建模，能大幅提升抓取动作的精确度。
*   **数据增强的价值**：通过形状谱增强，模型学会了忽略不相关的几何细节，从而在面对新物体时表现出极强的泛化能力。
*   **性能领先**：LIGHT 在几乎所有关键指标上均优于现有的 SOTA 方法，尤其是在接触保真度和文本对齐度方面。

### 7. 优点与亮点
*   **创新性引导**：提出了一种无需额外分类器、完全基于去噪步调的引导方案，为扩散模型的研究提供了新思路。
*   **端到端数据驱动**：摆脱了对 IK 求解器或物理模拟器的依赖，使生成过程更简洁、更通用。
*   **鲁棒的泛化能力**：通过形状增强技术，解决了 HOI 生成中常见的“过拟合特定物体形状”的问题。

### 8. 不足与局限
*   **场景限制**：目前主要针对**单人与单个物体**的交互，尚未扩展到多人或多物体的复杂场景。
*   **环境感知缺失**：模型未显式考虑静态环境（如地板、墙壁、家具）的几何约束，可能在复杂室内场景中出现环境穿模。
*   **计算开销**：由于推理时需要运行多个分支并进行多次前向计算，其推理速度慢于不带引导的基础扩散模型。

（完）
