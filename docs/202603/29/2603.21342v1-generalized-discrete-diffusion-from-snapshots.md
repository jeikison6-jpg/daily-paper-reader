---
title: Generalized Discrete Diffusion from Snapshots
title_zh: 基于快照的广义离散扩散
authors: "Oussama Zekri, Théo Uscidda, Nicolas Boullé, Anna Korba"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21342v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 离散扩散模型和加噪过程的统一框架
tldr: 本研究提出GDDS框架，这是一个统一的离散扩散模型框架，支持在大规模离散状态空间上进行任意加噪过程。该框架通过均匀化技术实现快速前向加噪，并基于快照潜变量推导出简化的证据下界（ELBO）以优化训练。实验证明，GDDS在生成质量和效率上优于现有离散扩散方法，并在大规模任务上首次超越了自回归模型。
selection_source: fresh_fetch
motivation: 现有的离散扩散模型在处理大规模离散状态空间时缺乏灵活性，且训练效率和生成质量仍有提升空间。
method: 利用均匀化技术实现灵活的前向加噪过程，并基于快照潜变量推导出一种简化的ELBO，从而实现高效的逆向过程训练。
result: GDDS在生成质量和训练效率上均优于现有离散扩散方法，并在大规模词表生成任务中首次超越了自回归模型。
conclusion: GDDS为离散扩散建模提供了一个统一且高效的理论框架，显著提升了大规模离散数据的生成性能。
---

## 摘要
我们引入了广义快照离散扩散（GDDS），这是一个用于离散扩散建模的统一框架，支持在大规模离散状态空间上的任意加噪过程。我们的公式涵盖了所有现有的离散扩散方法，同时在破坏动力学（corruption dynamics）的选择上提供了显著更大的灵活性。前向加噪过程依赖于一致化（uniformization），并能够实现快速的任意破坏。对于反向过程，我们推导了一个基于快照潜变量（而非整个加噪路径）的简单证据下界（ELBO），这使得标准生成建模架构能够进行高效训练，并具有清晰的概率解释。我们在大规模词表离散生成任务上的实验表明，所提出的框架在训练效率和生成质量方面优于现有的离散扩散方法，并在此规模上首次超越了自回归模型。我们在项目页面提供了代码和博客文章：https://oussamazekri.fr/gdds。

## Abstract
We introduce Generalized Discrete Diffusion from Snapshots (GDDS), a unified framework for discrete diffusion modeling that supports arbitrary noising processes over large discrete state spaces. Our formulation encompasses all existing discrete diffusion approaches, while allowing significantly greater flexibility in the choice of corruption dynamics. The forward noising process relies on uniformization and enables fast arbitrary corruption. For the reverse process, we derive a simple evidence lower bound (ELBO) based on snapshot latents, instead of the entire noising path, that allows efficient training of standard generative modeling architectures with clear probabilistic interpretation. Our experiments on large-vocabulary discrete generation tasks suggest that the proposed framework outperforms existing discrete diffusion methods in terms of training efficiency and generation quality, and beats autoregressive models for the first time at this scale. We provide the code along with a blog post on the project page : \href{https://oussamazekri.fr/gdds}{https://oussamazekri.fr/gdds}.

---

## 论文详细总结（自动生成）

### 基于快照的广义离散扩散（GDDS）论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
离散扩散模型（dLLMs）作为自回归（AR）模型的竞争对手，在文本、图和分子生成领域备受关注。然而，现有离散扩散模型存在两个主要瓶颈：
*   **加噪过程单一**：大多仅限于简单的“掩码（Masking）”或“均匀分布（Uniform）”替换，忽略了离散空间中的结构信息（如词与词之间的语义接近度）。
*   **训练目标复杂且受限**：现有的路径依赖型证据下界（ELBO）目标函数在处理大规模词表和复杂加噪动力学时，计算负担重且难以与标准 Transformer 架构对齐。
**研究动机**：提出一个统一的数学框架，支持任意加噪过程，并简化训练目标，使其在大规模语言建模任务中具备超越 AR 模型的竞争力。

#### 2. 论文提出的方法论
GDDS 框架包含三个核心技术组件：
*   **广义插值离散扩散**：定义了一个通用的前向加噪框架，涵盖了所有现有的离散扩散方案。它通过混合矩阵 $\Pi_t$ 和混合速率 $\alpha_t$ 来描述概率质量随时间的重新分布。
*   **基于一致化（Uniformization）的高效加噪**：为了避免在大规模词表上进行昂贵的矩阵指数运算，GDDS 利用一致化技术，仅需访问速率矩阵的列即可实现精确的前向采样。
*   **快照 ELBO（Snapshot ELBO）与跳跃状态参数化**：
    *   **核心思想**：不再针对整个加噪路径进行优化，而是基于随机采样的“快照” $(x_t, t)$ 进行训练。
    *   **参数化**：将反向过程分解为“何时跳跃”（由预设调度决定）和“跳到哪里”（由神经网络预测），推导出一种简单的加权交叉熵损失函数。这使得模型能够直接预测原始清洁标记的后验分布 $q(x_0 | x_t, t)$。

#### 3. 实验设计
*   **数据集**：
    *   **Text8**：字符级建模，用于评估基础建模能力。
    *   **OpenWebText (OWT)**：BPE 分词的大规模文本数据集，用于评估 LLM 规模下的性能。
    *   **下游任务**：PTB、Wikitext103、LM1B、Lambada 等 7 个数据集，用于零样本（Zero-shot）迁移评估。
*   **Benchmark 与对比方法**：
    *   **自回归模型（AR）**：标准的 Transformer 解码器。
    *   **离散扩散基准**：UDLM（均匀扩散）、MDM（掩码扩散）、SEDD、GenMD4 等。
*   **评估指标**：BPC（每字符比特数）、困惑度（PPL）、生成困惑度（Gen-PPL）、词汇多样性（Distinct-n）和熵。

#### 4. 资源与算力
*   **硬件**：使用了 1 个节点，配备 **4 张 NVIDIA H100 GPU**。
*   **训练时长**：
    *   Text8 模型训练了 100 万步。
    *   OWT 模型训练了 50 万步（处理约 2620 亿个标记）。
*   **模型规模**：使用 DDiT 架构，非嵌入参数约为 96M。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从字符级到大规模 BPE 标记级的训练，并进行了广泛的零样本迁移实验。
*   **消融与对比**：对比了不同加噪策略（GDDS Absorb, GDDS Uniform, GDDS Gauss），并与重新训练的基准模型在相同算力下进行了公平对比。
*   **充分性**：实验不仅关注似然估计（PPL），还深入探讨了生成质量与多样性的权衡（Pareto 前沿分析），实验设计较为客观、全面。

#### 6. 论文的主要结论与发现
*   **性能突破**：GDDS 在 Text8 和 OWT 规模上**首次在离散扩散领域超越了同等规模的自回归模型**。
*   **语义加噪的优势**：引入语义信息（如 GDDS Gauss，利用词向量距离加噪）能显著提升模型的泛化能力和零样本迁移性能。
*   **训练效率**：快照 ELBO 简化了训练过程，使得离散扩散模型在标准 Transformer 架构上表现出极高的训练效率和稳定性。
*   **生成质量**：在相同的解码步数下，GDDS 能够提供比以往扩散模型更好的质量-多样性平衡。

#### 7. 优点
*   **理论统一性**：将多种离散扩散方法统一在一个数学框架下，具有很强的理论优雅性。
*   **计算高效**：通过一致化技术解决了大规模词表加噪的计算难题。
*   **架构友好**：快照式训练目标与当前主流的 Transformer 架构完美契合，无需复杂的路径采样。
*   **语义感知**：证明了在离散空间中引入语义结构加噪对提升模型性能至关重要。

#### 8. 不足与局限
*   **语义内核采样成本**：虽然训练高效，但在使用语义感知内核（如 SIK/Gauss）进行采样时，由于需要近似时间排序的矩阵指数，采样速度和精度仍面临挑战。
*   **超参数敏感性**：语义内核的带宽和温度调度等超参数可能需要针对不同任务进行精细调优。
*   **长文本一致性**：虽然在 1024 长度上表现优异，但在极长文本生成中的表现尚未在文中得到充分验证。

（完）
