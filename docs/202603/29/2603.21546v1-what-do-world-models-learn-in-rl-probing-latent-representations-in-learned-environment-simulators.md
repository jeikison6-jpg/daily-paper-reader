---
title: What Do World Models Learn in RL? Probing Latent Representations in Learned Environment Simulators
title_zh: 世界模型在强化学习中学习到了什么？探究学习到的环境模拟器中的潜表征
authors: Xinyu Zhang
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21546v1"
tags: ["query:rl-dm"]
score: 10.0
evidence: 探究强化学习中基于扩散模型的世界模型
tldr: 本研究探讨了强化学习中世界模型的内部表征。通过对 IRIS（离散 Transformer）和 DIAMOND（连续扩散 UNet）两种不同架构的模型进行线性探测、因果干预和注意力分析，发现这些模型在 Atari 游戏中学习到了结构化的、近似线性的环境状态表征（如物体位置、分数）。研究证明了这些表征不仅是可解码的，而且在模型预测中起到了功能性作用，为理解世界模型的内部机制提供了实证支持。
selection_source: fresh_fetch
motivation: 旨在揭示世界模型在模拟环境动态时，其内部究竟学习并表征了哪些具体信息。
method: 采用线性与非线性探测、因果干预、注意力分析及 Token 消减等可解释性技术，对两种不同架构的世界模型进行深入分析。
result: 发现模型内部存在可线性解码的游戏状态表征，且因果干预证实了这些表征对模型预测具有实际影响。
conclusion: 实验证据表明，不同架构的世界模型均能自发学习到结构化且近似线性的环境状态内部表征。
---

## 摘要
世界模型通过经验学习模拟环境动力学，从而实现高样本效率的强化学习。但这些模型内部究竟表征了什么？我们将可解释性技术（包括线性与非线性探测、因果干预和注意力分析）应用于两种架构迥异的世界模型：在 Atari Breakout 和 Pong 上训练的 IRIS（离散 token Transformer）和 DIAMOND（连续扩散 UNet）。通过线性探测，我们发现两种模型都形成了游戏状态变量（物体位置、得分）的线性可解码表征，而 MLP 探测仅产生略高的 R^2，证实了这些表征近似线性。因果干预——沿探测导出的方向偏移隐藏状态——在模型预测中产生了相关的变化，证明了这些表征在功能上被有效利用，而不仅仅是具有相关性。对 IRIS 注意力头的分析揭示了空间专业化：特定的注意力头优先关注与游戏物体重叠的 token。多基准 token 消融实验一致发现，包含物体的 token 具有不成比例的重要性。我们的研究结果提供了可解释性证据，表明在两种游戏和两种架构中，学习到的世界模型都形成了结构化的、近似线性的环境状态内部表征。

## Abstract
World models learn to simulate environment dynamics from experience, enabling sample-efficient reinforcement learning. But what do these models actually represent internally? We apply interpretability techniques--including linear and nonlinear probing, causal interventions, and attention analysis--to two architecturally distinct world models: IRIS (discrete token transformer) and DIAMOND (continuous diffusion UNet), trained on Atari Breakout and Pong. Using linear probes, we find that both models develop linearly decodable representations of game state variables (object positions, scores), with MLP probes yielding only marginally higher R^2, confirming that these representations are approximately linear. Causal interventions--shifting hidden states along probe-derived directions--produce correlated changes in model predictions, providing evidence that representations are functionally used rather than merely correlated. Analysis of IRIS attention heads reveals spatial specialization: specific heads attend preferentially to tokens overlapping with game objects. Multi-baseline token ablation experiments consistently identify object-containing tokens as disproportionately important. Our findings provide interpretability evidence that learned world models develop structured, approximately linear internal representations of environment state across two games and two architectures.

---

## 论文详细总结（自动生成）

以下是对论文《What Do World Models Learn in RL? Probing Latent Representations in Learned Environment Simulators》的结构化总结：

### 1. 核心问题与整体含义
该研究旨在揭示强化学习（RL）中**世界模型（World Models）的内部表征机制**。虽然世界模型（如 IRIS 和 DIAMOND）在 Atari 游戏中表现出极高的样本效率，但它们在预测未来图像时，内部是否真正理解并提取了游戏状态（如球的位置、得分等）尚不明确。本研究试图验证“线性表征假设”在视觉环境模拟器中的适用性，即模型是否通过简单的线性结构来组织复杂的环境信息。

### 2. 方法论
研究者采用了多种机械可解释性（Mechanistic Interpretability）技术，对两种架构迥异的模型进行剖析：
*   **探测分析（Probing）：** 使用线性脊回归（Ridge Regression）和 2 层 MLP 探测器，从模型的每一层隐藏状态中解码游戏状态变量（球的坐标、玩家位置、分数）。通过比较线性与非线性探测的 $R^2$ 差异（Selectivity Gap $\Delta$）来判断表征的线性程度。
*   **因果干预（Causal Intervention）：** 采用激活补丁（Activation Patching）技术。将隐藏状态沿探测器得到的权重方向进行偏移，观察模型输出（KL 散度、Token 变化率）的变化，以证明这些表征是被模型功能性地“使用”了，而非仅仅是相关。
*   **注意力与消融分析：** 针对 IRIS 模型，分析注意力头的空间选择性（Spatial Selectivity），并使用三种基准（零值、均值、随机 Token）进行 Token 消融实验，量化不同空间区域对预测的重要性。

### 3. 实验设计
*   **模型对象：** 
    *   **IRIS：** 基于离散 Token 的 Transformer 架构（VQ-VAE + GPT-2）。
    *   **DIAMOND：** 基于连续扩散过程的 UNet 架构。
*   **实验场景：** Atari 经典游戏 Breakout（打砖块）和 Pong（乒乓球）。
*   **基准数据：** 使用 Atari RAM 提取的真实状态变量作为 Ground Truth。
*   **对比基准：** 原始像素（Raw Pixels）、随机初始化模型、标签打乱（Shuffled Labels）对照组。
*   **数据量：** 每个游戏提取 10,000 帧图像进行探测训练与评估。

### 4. 资源与算力
论文中**未明确说明**具体的 GPU 型号、数量及训练时长。文中仅提到了模型的超参数（如 IRIS 为 10 层 Transformer，维度 256；DIAMOND 为 4 阶段 UNet，通道数 64），这属于轻量级到中等规模的模型配置。

### 5. 实验数量与充分性
*   **实验覆盖面：** 涵盖了两种完全不同的模型架构（Transformer vs. Diffusion）和两种不同动态特性的游戏，增强了结论的普适性。
*   **充分性：** 实验不仅停留在“相关性”探测上，还通过因果干预和多基准消融实验进行了交叉验证。
*   **客观性：** 引入了随机模型和原始像素作为对照，证明了模型确实学习到了非平凡的结构化信息。对于 5 页的研讨会论文而言，实验设计非常扎实。

### 6. 主要结论与发现
*   **线性表征普遍存在：** 两种模型都自发形成了近似线性的游戏状态表征。MLP 探测相比线性探测的提升极小（$\Delta \le 0.06$），证实了表征的线性度。
*   **架构差异显著：** 
    *   **IRIS** 的表征在 Transformer 各层间非常稳定，且 VQ-VAE 编码器已经完成了大部分状态提取工作。
    *   **DIAMOND** 呈现“倒 V 型”模式，抽象状态在 UNet 的瓶颈层（Bottleneck）达到线性解码的峰值，而解码器层则通过跳跃连接（Skip Connections）以非线性方式混合信息。
*   **功能性验证：** 因果干预证实，修改这些线性方向会直接且成比例地影响模型的预测结果。
*   **空间专业化：** IRIS 的注意力头表现出明显的分工，特定头会专门负责追踪球或球拍等特定空间区域。

### 7. 优点
*   **跨架构对比：** 成功在离散 Token 和连续扩散两种截然不同的世界模型中找到了共同的表征规律。
*   **因果论证：** 不满足于“能解码”，而是通过干预实验证明了表征与模型决策之间的因果联系。
*   **方法严谨：** Token 消融实验采用了三种不同的替代策略，且结果高度一致（相关系数 $\rho > 0.9$），增强了结论的可信度。

### 8. 不足与局限
*   **环境复杂度有限：** 实验仅限于 2D Atari 游戏，尚未在更复杂的 3D 环境或具有部分可观测性的任务中验证。
*   **时间维度缺失：** 探测主要基于单帧隐藏状态，可能忽略了 Transformer 在序列建模中编码的复杂时间动力学信息。
*   **干预手段较粗糙：** 沿单一线性方向偏移是一种较为基础的干预方式，未来可采用更精细的因果抽象方法。

（完）
