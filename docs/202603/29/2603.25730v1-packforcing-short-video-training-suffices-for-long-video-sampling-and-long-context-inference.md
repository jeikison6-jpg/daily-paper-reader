---
title: "PackForcing: Short Video Training Suffices for Long Video Sampling and Long Context Inference"
title_zh: PackForcing：短视频训练足以支持长视频采样与长上下文推理
authors: "Xiaofeng Mao, Shaohao Rui, Kaining Ying, Bo Zheng, Chuanhao Li, Mingmin Chi, Kaipeng Zhang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25730v1"
tags: ["query:rl-vg"]
score: 9.0
evidence: 用于长视频生成的自回归视频扩散模型
tldr: PackForcing 针对自回归视频扩散模型在长视频生成中面临的 KV 缓存增长、重复和误差累积问题，提出了一种创新的三分区 KV 缓存管理策略。该框架通过将历史上下文划分为 Sink、高度压缩的 Mid 和全分辨率 Recent 令牌，并结合动态 top-k 选择与时间 RoPE 调整，实现了在单 GPU 上生成高质量 2 分钟视频。该研究证明了仅需短视频训练即可实现卓越的长视频推理和 24 倍时间外推能力。
selection_source: fresh_fetch
motivation: 旨在解决自回归视频生成中 KV 缓存线性增长导致的内存瓶颈、时间重复以及长序列推理中的误差累积问题。
method: 采用三阶段 KV 缓存策略，通过双分支网络对中间帧进行 32 倍时空压缩，并利用动态 top-k 选择和时间 RoPE 调整来维持长程一致性。
result: 在单块 H200 GPU 上以仅 4GB 的固定 KV 缓存实现了 2 分钟长视频生成，并在 VBench 测试中达到 SOTA 级的时间一致性和动态表现。
conclusion: 证明了通过分层上下文压缩和位置编码动态调整，短视频训练的监督足以支持高质量、长时长的视频合成与推理。
---

## 摘要
自回归视频扩散模型已取得显著进展，但在长视频生成过程中仍面临难以处理的线性 KV 缓存增长、时间重复以及误差累积等瓶颈。为了应对这些挑战，我们提出了 PackForcing，这是一个通过创新的三分区 KV 缓存策略高效管理生成历史的统一框架。具体而言，我们将历史上下文分为三类：(1) Sink token，以全分辨率保留早期锚帧以维持全局语义；(2) Mid token，通过融合渐进式 3D 卷积与低分辨率 VAE 重编码的双分支网络实现大规模时空压缩（32 倍 token 缩减）；(3) Recent token，保持全分辨率以确保局部时间连贯性。为了在不牺牲质量的前提下严格限制内存占用，我们为 Mid token 引入了动态 top-k 上下文选择机制，并配合连续时间 RoPE 调整（Temporal RoPE Adjustment），以极低的开销无缝重新对齐由丢弃 token 引起的位置间隙。得益于这种基于原则的分层上下文压缩，PackForcing 能够在单块 H200 GPU 上生成分辨率为 832x480、帧率为 16 FPS 的连贯 2 分钟视频。它实现了仅 4 GB 的受限 KV 缓存，并支持惊人的 24 倍时间外推（从 5 秒到 120 秒），无论是零样本运行还是仅在 5 秒片段上训练均表现出色。在 VBench 上的广泛实验结果表明，该模型达到了最先进的时间一致性（26.07）和动态程度（56.25），证明了短视频监督足以实现高质量的长视频合成。https://github.com/ShandaAI/PackForcing

## Abstract
Autoregressive video diffusion models have demonstrated remarkable progress, yet they remain bottlenecked by intractable linear KV-cache growth, temporal repetition, and compounding errors during long-video generation. To address these challenges, we present PackForcing, a unified framework that efficiently manages the generation history through a novel three-partition KV-cache strategy. Specifically, we categorize the historical context into three distinct types: (1) Sink tokens, which preserve early anchor frames at full resolution to maintain global semantics; (2) Mid tokens, which achieve a massive spatiotemporal compression (32x token reduction) via a dual-branch network fusing progressive 3D convolutions with low-resolution VAE re-encoding; and (3) Recent tokens, kept at full resolution to ensure local temporal coherence. To strictly bound the memory footprint without sacrificing quality, we introduce a dynamic top-$k$ context selection mechanism for the mid tokens, coupled with a continuous Temporal RoPE Adjustment that seamlessly re-aligns position gaps caused by dropped tokens with negligible overhead. Empowered by this principled hierarchical context compression, PackForcing can generate coherent 2-minute, 832x480 videos at 16 FPS on a single H200 GPU. It achieves a bounded KV cache of just 4 GB and enables a remarkable 24x temporal extrapolation (5s to 120s), operating effectively either zero-shot or trained on merely 5-second clips. Extensive results on VBench demonstrate state-of-the-art temporal consistency (26.07) and dynamic degree (56.25), proving that short-video supervision is sufficient for high-quality, long-video synthesis. https://github.com/ShandaAI/PackForcing

---

## 论文详细总结（自动生成）

以下是对论文《PackForcing: Short Video Training Suffices for Long Video Sampling and Long Context Inference》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
自回归视频扩散模型在生成长视频（分钟级）时面临三大瓶颈：
*   **内存爆炸**：KV 缓存随视频长度线性增长。生成 2 分钟视频（832×480, 16 FPS）需约 74.9 万个 token，占用约 138 GB 显存，远超单张显卡容量。
*   **误差累积**：去噪过程中的微小预测误差会随时间迭代放大，导致语义漂移和质量下降。
*   **时间重复**：模型容易陷入循环或静态画面，缺乏长程一致性。
**研究动机**：探索如何仅通过短视频训练，在严格受限的显存预算下，实现高质量、高动态且具有长程一致性的长视频生成。

### 2. 方法论：核心思想与关键技术
PackForcing 提出了一个**三分区 KV 缓存管理策略**，将生成历史解耦为不同功能的区域：
*   **Sink Tokens（锚点令牌）**：以全分辨率保留最早生成的几帧（如前 8 帧）。它们作为全局语义锚点，防止场景布局和主体身份随时间漂移。
*   **Compressed Mid Tokens（压缩中间令牌）**：对绝大部分历史帧进行 **128 倍体积压缩（约 32 倍 token 缩减）**。
    *   **双分支压缩网络**：结合了 **HR 分支**（渐进式 3D 卷积提取细节）和 **LR 分支**（像素空间重编码提取全局语义），确保压缩后的 token 仍保留关键结构信息。
*   **Recent Tokens（近期令牌）**：以全分辨率保留最近生成的帧，确保局部动作的平滑连贯。
*   **关键算法细节**：
    *   **动态上下文选择（Dynamic Context Selection）**：基于查询-键（Query-Key）相似度，从压缩池中动态检索 top-k 个最相关的块，而非盲目使用全部历史。
    *   **时间 RoPE 调整（Temporal RoPE Adjustment）**：针对 token 丢弃导致的位置编码不连续问题，提出一种增量旋转机制，无需重新计算缓存即可修正时间位置偏差。

### 3. 实验设计
*   **基础模型**：Wan2.1-T2V-1.3B。
*   **数据集与基准**：使用 **VBench-Long** 评估协议，包含来自 MovieGen 的 128 个文本提示词。
*   **对比方法**：包括 CausVid、LongLive、Self-Forcing、Rolling Forcing 和 Deep Forcing 等当前主流的自回归长视频生成框架。
*   **评估指标**：动态程度（Dynamic Degree）、运动平滑度、主体一致性、背景一致性、图像质量以及随时间变化的 CLIP 分数（衡量文本对齐稳定性）。

### 4. 资源与算力
*   **训练**：在仅 **5 秒（20 个潜空间帧）** 的短视频片段上进行训练，迭代 3000 次，Batch Size 为 8。
*   **推理**：支持在单块 **NVIDIA H200 或 A100 (80GB)** 上运行。
*   **效率**：将 2 分钟视频的 KV 缓存从 138 GB 压缩至约 **4 GB**，推理速度保持在约 15-16 FPS。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 60 秒和 120 秒两种长度的生成对比。
*   **消融实验**：针对 Sink 尺寸（0 到 16 帧）、压缩分支（仅 HR、仅 LR、融合）、丢弃策略（随机、FIFO、动态选择）以及 RoPE 调整的有效性进行了详尽测试。
*   **充分性评价**：实验设计客观且全面，通过定量指标和定性可视化结果证明了该方法在 24 倍时间外推（5s 训练 -> 120s 生成）上的卓越性能。

### 6. 主要结论与发现
*   **短训练支持长推理**：证明了通过合理的上下文压缩和位置编码修正，短视频监督足以支撑分钟级视频的合成。
*   **内存与质量的平衡**：三分区策略在严格限制显存的同时，通过保留 Sink token 有效解决了语义漂移问题。
*   **动态性优势**：相比于为了维持一致性而牺牲运动的基线（如 LongLive），PackForcing 在保持高动态运动（Dynamic Degree 达 56.25）的同时，依然拥有极高的一致性。

### 7. 优点
*   **极高的显存效率**：实现了 O(1) 的注意力复杂度，使长视频生成在消费级/标准服务器显卡上成为可能。
*   **强大的外推能力**：24 倍的时间跨度外推能力显著优于现有方法。
*   **非破坏性记忆**：动态选择机制允许模型在需要时重新激活被“存档”的历史记忆，而非永久丢弃。

### 8. 不足与局限
*   **主体一致性微降**：在极长视频中，虽然动态性极佳，但主体一致性评分略低于某些牺牲运动以换取静止一致性的模型。
*   **压缩率固定**：目前采用固定的 32 倍 token 压缩率，未来可探索根据场景复杂度动态调整压缩率。
*   **分辨率限制**：实验主要集中在 832×480 分辨率，尚未深入探讨在 1080p 或更高分辨率下的表现。

（完）
