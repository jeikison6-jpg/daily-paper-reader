---
title: "GO-Renderer: Generative Object Rendering with 3D-aware Controllable Video Diffusion Models"
title_zh: GO-Renderer：基于 3D 感知可控视频扩散模型的生成式物体渲染
authors: "Zekai Gu, Shuoxuan Feng, Yansong Wang, Hanzhuo Huang, Zhongshuo Du, Chengfeng Zhao, Chengwei Ren, Peng Wang, Yuan Liu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23246v1"
tags: ["query:rl-vg"]
score: 9.0
evidence: 用于物体渲染的3D感知可控视频扩散模型
tldr: GO-Renderer 提出了一种结合 3D 代理与视频扩散模型的统一框架，旨在解决传统 3D 重建在复杂外观建模上的不足以及生成模型在视角控制上的缺失。该方法利用 3D 代理提供精确的视角引导，并借助扩散模型在无需显式建模材质和光照的情况下实现高质量渲染。实验证明，该方法在视角合成、重光照和视频物体插入等任务中均达到了 SOTA 水平。
selection_source: fresh_fetch
motivation: 现有的 3D 重建方法难以处理复杂外观，而生成式模型虽能合成逼真图像却缺乏精确的视角控制。
method: 提出 GO-Renderer 框架，通过 3D 代理引导视频扩散模型，在任意视角和光照条件下实现高质量物体渲染。
result: 实验表明该方法在视角合成、环境重光照以及物体视频插入等任务上均取得了领先的性能。
conclusion: GO-Renderer 成功融合了 3D 几何控制与生成式模型的表现力，为高质量、可控的物体渲染提供了有效方案。
---

## 摘要
从图像中重建可渲染的 3D 模型是一项有用但具有挑战性的任务。最近的前馈 3D 重建方法在高效恢复几何形状方面取得了显著成功，但仍无法准确建模这些 3D 重建模型的复杂外观。最近基于扩散的生成模型可以使用参考图像合成物体的逼真图像或视频，而无需显式建模其外观，这为物体渲染提供了一个充满前景的方向，但缺乏对视角的精确控制。在本文中，我们提出了 GO-Renderer，这是一个统一的框架，它集成了重建的 3D 代理（proxies）来引导视频生成模型，从而在任意光照条件下实现任意视角的高质量物体渲染。我们的方法不仅利用重建的 3D 代理实现了精确的视角控制，还能够利用扩散生成模型在不同的光照环境中实现高质量渲染，而无需显式建模复杂的材质和光照。大量实验表明，GO-Renderer 在各项物体渲染任务中均达到了最先进的性能，包括在新视角下合成图像、在新的光照环境中渲染物体以及将物体插入现有视频中。

## Abstract
Reconstructing a renderable 3D model from images is a useful but challenging task. Recent feedforward 3D reconstruction methods have demonstrated remarkable success in efficiently recovering geometry, but still cannot accurately model the complex appearances of these 3D reconstructed models. Recent diffusion-based generative models can synthesize realistic images or videos of an object using reference images without explicitly modeling its appearance, which provides a promising direction for object rendering, but lacks accurate control over the viewpoints. In this paper, we propose GO-Renderer, a unified framework integrating the reconstructed 3D proxies to guide the video generative models to achieve high-quality object rendering on arbitrary viewpoints under arbitrary lighting conditions. Our method not only enjoys the accurate viewpoint control using the reconstructed 3D proxy but also enables high-quality rendering in different lighting environments using diffusion generative models without explicitly modeling complex materials and lighting. Extensive experiments demonstrate that GO-Renderer achieves state-of-the-art performance across the object rendering tasks, including synthesizing images on new viewpoints, rendering the objects in a novel lighting environment, and inserting an object into an existing video.

---

## 论文详细总结（自动生成）

以下是对论文《GO-Renderer: Generative Object Rendering with 3D-aware Controllable Video Diffusion Models》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何在保证精确视角控制的同时，实现高质量、光照一致的物体渲染？
*   **研究背景**：
    *   **传统/神经重建（如 3DGS, NeRF）**：擅长恢复几何，但在处理复杂材质和环境重光照（Inverse Rendering）时面临病态求解问题，难以解耦材质与光照。
    *   **生成式视频模型**：能合成逼真的外观，但缺乏精确的 3D 空间控制，且在多视角一致性上表现较差（容易产生幻觉）。
*   **整体含义**：论文旨在结合 **显式 3D 几何约束** 与 **隐式生成式先验**，提出一种无需显式建模物理材质即可实现可控渲染的框架。

### 2. 方法论
核心思想是将粗糙的 3D 重建结果作为“代理（Proxy）”，引导视频扩散模型进行渲染。
*   **3D 代理与坐标映射（Coordinate Maps）**：
    *   利用前馈重建模型（如 ReconViaGen）获取物体的粗糙几何（网格或点云）。
    *   将 3D 几何投影到参考视角和目标视角，生成**物体中心坐标图**。每个像素的颜色编码了其在物体局部坐标系中的 $(x, y, z)$ 坐标。
*   **联合参考结构（Joint Reference Structure）**：
    *   将参考图像与其对应的坐标图在通道维度拼接。
    *   扩散模型通过共享的 3D 坐标作为“桥梁”，学习从参考视图中“查找”并转移纹理到目标视角。
*   **负向旋转位置编码偏移（Negative RoPE Shift）**：
    *   为了防止模型将参考帧误认为是视频序列的连续帧（导致过渡伪影），给参考帧分配负的时间索引，并设置时间间隔（Gap），在时间维度上隔离参考信息与生成序列。
*   **算法流程**：输入多视角参考图 -> 快速 3D 重建 -> 生成目标轨迹坐标图 -> 视频扩散模型（DiT）渲染 -> 输出高保真视频。

### 3. 实验设计
*   **数据集**：构建了一个包含 **57k 个高质量视频剪辑** 的混合数据集：
    *   **合成数据**：使用 Blender 渲染，包含精确的位姿和光照。
    *   **真实数据**：从 OpenVidHD 和 CO3D 中提取，使用 SAM3 分割并重建 3D 代理。
    *   **AI 生成数据**：利用 Wan2.2 生成多样化物体视频，再进行后处理标注。
*   **Benchmark 与对比方法**：
    *   **两阶段流水线**：UniLumos（结合 GT 几何、ReconViGen 或 AnySplat）。
    *   **主体驱动生成模型**：Phantom、Wan2.2Fun。
    *   **评估指标**：PSNR、SSIM（像素级）；VBench++（感知质量、运动平滑度）；CLIP/DINO（语义与结构一致性）。

### 4. 资源与算力
*   **算力投入**：使用了 **16 张 NVIDIA A800 GPU**。
*   **训练细节**：
    *   总训练步数：20,000 步。
    *   Batch Size：32。
    *   分辨率：480 × 832，视频长度 81 帧。
    *   优化器：AdamW。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了定量对比实验，涵盖了合成数据集和真实世界数据集。
    *   **消融实验**：针对时间偏移量 $g$ 的影响、模型对 3D 代理质量（空间扰动）的鲁棒性进行了详细测试。
    *   **应用展示**：展示了在 Blender 插件渲染和真实视频物体插入（Object Insertion）中的效果。
*   **充分性评价**：实验设计较为全面，不仅对比了传统的重建方法，还对比了最新的生成式模型。通过引入多种感知指标（VBench++），客观地评价了视频的生成质量。

### 6. 主要结论与发现
*   **性能领先**：GO-Renderer 在视角合成、重光照和多视角一致性方面均显著优于现有 SOTA 方法。
*   **几何引导的有效性**：坐标图能提供比单纯的深度图或点云更强的空间对应关系，有效解决了生成模型中的“幻觉”问题。
*   **鲁棒性**：即使 3D 代理比较粗糙或存在轻微扰动，模型仍能依靠 2D 扩散先验生成合理的图像，表现出较强的容错能力。

### 7. 优点（亮点）
*   **混合范式**：巧妙地避开了极其困难的物理材质解耦（PBR 恢复），利用生成模型直接处理复杂光影。
*   **精确控制**：通过坐标图实现了像素级的空间对齐，使得视角控制达到了传统渲染器的精度。
*   **实用性强**：能够无缝集成到现有 3D 工作流（如 Blender）中，并支持复杂的视频编辑任务（如物体插入）。

### 8. 不足与局限
*   **对代理的依赖**：虽然有一定鲁棒性，但如果 3D 代理与目标物体结构严重不匹配（如用瓶子的几何去渲染蛋糕），会导致严重的结构伪影。
*   **计算开销**：作为基于大型视频扩散模型（Wan2.2）的方法，推理成本高于传统实时渲染器。
*   **数据偏差**：尽管构建了大规模数据集，但对于极端罕见的物体类别或极度复杂的动态光影，可能仍存在泛化瓶颈。

（完）
