---
title: Improving Diffusion Generalization with Weak-to-Strong Segmented Guidance
title_zh: 通过弱到强分段引导提升扩散模型的泛化能力
authors: "Liangyu Yuan, Yufei Huang, Mingkun Lei, Tong Zhao, Ruoyu Wang, Changxi Chi, Yiwei Wang, Chi Zhang"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20584v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 扩散模型中无分类器引导 (CFG) 的分析
tldr: 扩散模型在迭代采样中常因梯度误差累积导致泛化能力受限。本文基于“从弱到强”（Weak-to-Strong）原则，分析了CFG和AG等引导技术的有效范围，并提出了一种混合引导方法SGG。SGG结合了不同引导方式的优势，不仅能作为推理时的无训练插件提升SD3等模型的表现，还能融入训练目标以增强非引导扩散模型的泛化性能。
selection_source: fresh_fetch
motivation: 旨在解决扩散模型在迭代采样过程中因梯度误差累积导致的泛化失败及现有引导方法适用场景不明的问题。
method: 提出了一种名为SGG的分段引导方法，通过结合CFG和AG的优势，并将其原理扩展至训练目标函数中。
result: 在SD3和SD3.5上的推理实验以及Transformer架构的训练实验均证明，SGG在生成质量和泛化能力上优于现有方法。
conclusion: 通过“从弱到强”的原则统一并优化引导策略，可以显著提升扩散模型在推理和训练阶段的泛化表现。
---

## 摘要
扩散模型通过迭代细化过程生成合成图像。然而，免模拟目标（simulation-free objective）与迭代过程之间的不匹配通常会导致采样轨迹上的累积梯度误差，从而导致结果不尽如人意且泛化失败。诸如无分类器引导（CFG）和自动引导（AG）之类的引导技术通过在主信号和劣质信号之间进行外推来缓解这一问题，从而实现更强的泛化。尽管取得了经验上的成功，但流行引导方法的有效运行机制仍未得到充分探索，导致在给定前提条件下选择合适的引导方法时存在歧义。在这项工作中，我们首先从“弱到强”（weak-to-strong）原则的角度进行合成比较，以隔离并展示以 CFG 和 AG 为代表的引导方法的有效机制。基于此，我们在该原则下提出了一种名为 SGG 的混合实例化方法，兼具两者的优点。此外，我们证明了 W2S 原则连同 SGG 可以迁移到训练目标中，从而提高无引导扩散模型的泛化能力。我们通过全面的实验验证了我们的方法。在推理阶段，对 SD3 和 SD3.5 的评估证实了 SGG 优于现有的免训练引导变体。在 Transformer 架构上的训练阶段实验证明了在有条件和无条件设置下的有效迁移和性能提升。代码可在 https://github.com/851695e35/SGG 获取。

## Abstract
Diffusion models generate synthetic images through an iterative refinement process. However, the misalignment between the simulation-free objective and the iterative process often causes accumulated gradient error along the sampling trajectory, which leads to unsatisfactory results and a failure to generalize. Guidance techniques like Classifier Free Guidance (CFG) and AutoGuidance (AG) alleviate this by extrapolating between the main and inferior signal for stronger generalization. Despite empirical success, the effective operational regimes of prevalent guidance methods are still under-explored, leading to ambiguity when selecting the appropriate guidance method given a precondition. In this work, we first conduct synthetic comparisons to isolate and demonstrate the effective regime of guidance methods represented by CFG and AG from the perspective of weak-to-strong principle. Based on this, we propose a hybrid instantiation called SGG under the principle, taking the benefits of both. Furthermore, we demonstrate that the W2S principle along with SGG can be migrated into the training objective, improving the generalization ability of unguided diffusion models. We validate our approach with comprehensive experiments. At inference time, evaluations on SD3 and SD3.5 confirm that SGG outperforms existing training-free guidance variants. Training-time experiments on transformer architectures demonstrate the effective migration and performance gains in both conditional and unconditional settings. Code is available at https://github.com/851695e35/SGG.

---

## 论文详细总结（自动生成）

### 论文总结：通过弱到强分段引导提升扩散模型的泛化能力

#### 1. 核心问题与整体含义（研究动机和背景）
扩散模型（如 Stable Diffusion, SD3）在生成图像时依赖迭代细化过程。然而，由于训练时的“免模拟”目标（局部优化）与推理时的“全局迭代”轨迹之间存在不匹配（即**曝光偏差**），采样过程中会产生累积误差。这导致无引导模型在处理复杂条件（如长文本）时泛化能力差，容易产生伪影或偏离分布。
现有的引导技术（如 **CFG** 和 **AG**）虽然能缓解此问题，但其有效运行机制（Operational Regimes）尚不明确：CFG 鲁棒但易导致模式崩溃（多样性降低）；AG 提升多样性但在模型拟合不足时易产生离群点。本论文旨在理清这些引导方法的适用边界，并提出一种更优的统一框架。

#### 2. 方法论：核心思想与关键技术
论文基于**“从弱到强”（Weak-to-Strong, W2S）**原则，将引导建模为强预测器与弱预测器之间的外推：
$v_w = v_{weak} + w(v_{strong} - v_{weak})$

*   **核心分类**：
    *   **条件相关引导 (CDG)**：如 CFG，通过丢弃条件（$c=\emptyset$）构建弱信号。擅长在高噪声阶段寻找正确的类别流形（解决“是什么”的问题）。
    *   **条件无关引导 (CAG)**：如 AG 或 SLG，通过弱化模型架构（如跳层、小模型）构建弱信号。擅长在低噪声阶段细化类内细节（解决“像不像”的问题）。
*   **分段引导 (SGG, Segmented Guidance)**：
    论文提出在采样前期（高噪声）使用 CDG 确保语义对齐，在采样后期（低噪声）切换为 CAG 提升感知质量和多样性。切换点由时间戳 $\tau$ 控制。
*   **训练集成 (W2S Training)**：
    将引导逻辑直接融入训练目标函数。通过在训练时让模型学习“强预测”与“弱预测”的差值外推，提升模型本身的泛化能力，从而在推理时减少或无需额外的引导计算开销。

#### 3. 实验设计
*   **数据集/场景**：
    *   **推理阶段**：使用 SD3-Medium 和 SD3.5-Medium。测试集包括 MS-COCO-1K 和 LAION-5B-1K。
    *   **训练阶段**：使用 SiT-B/2 架构，在 ImageNet 256x256 上进行有条件和无条件训练。
    *   **扩展场景**：在视频生成模型 Wan2.1-1.3B 上验证了 SGG 的有效性。
*   **Benchmark 与对比方法**：
    *   对比了 CFG、SLG（跳层引导）、PAG（扰动注意力引导）、S2-Guidance、CFG-Zero*、Rectified-CFG++ 等多种前沿引导变体。
    *   评估指标：HPSv2.1（人类偏好）、Aesthetic Score（美学评分）、FID（图像质量）、IS（多样性）、GenEval（指令遵循）。

#### 4. 资源与算力
*   **GPU 型号与数量**：训练实验主要在 **4 张 NVIDIA A100 GPU** 上完成。
*   **训练时长**：SiT-B/2 模型训练了 **400k 个迭代（iterations）**。
*   **效率监控**：论文详细记录了每步采样（NFE/s）和每轮训练迭代的时间（time/it）。例如，BR（分支弱模型）变体仅增加 2% 的训练开销，而完整的 SGG 训练增加约 22% 的开销。

#### 5. 实验数量与充分性
论文实验设计非常详尽且具有说服力：
*   **合成实验**：首先通过 2D Toy 实验（混合高斯模型）直观展示了 CDG 和 CAG 的失效模式。
*   **定量分析**：在 ImageNet 上计算了引导速度与理论最优速度的 Inception 距离，量化了不同阶段的误差修正能力。
*   **消融实验**：对分段点 $\tau$、引导权重 $w$、弱模型构建方式（分支、跳层、小模型）进行了多维度消融。
*   **跨模态验证**：从图像扩展到视频生成，证明了方法的通用性。整体实验设计客观、公平，对比基准涵盖了目前主流的所有引导技术。

#### 6. 主要结论与发现
*   **阶段性差异**：CDG（CFG）在采样早期（高噪声）纠错能力最强；CAG（AG/SLG）在采样后期（低噪声）对感知质量的提升更显著。
*   **SGG 的优越性**：SGG 在不增加额外推理成本的前提下（相比 CFG+SLG 等组合方法），同时提升了提示词遵循度（HPSv2.1）和美学质量。
*   **训练增益**：将 W2S 原理引入训练目标，可以使无引导模型（NFE=1）的生成质量甚至超过基准模型的引导生成（NFE=2）效果。

#### 7. 优点（亮点）
*   **理论统一**：用“弱到强”原则统一了看似不同的引导技术，提供了清晰的分类学视角。
*   **即插即用**：推理端的 SGG 是免训练的，可以直接应用于现有的预训练模型（如 SD3.5）。
*   **效率平衡**：提出的 BR（分支）弱模型构建方式极大地降低了训练集成时的计算负担。
*   **洞察深刻**：通过 2D 实验揭示了“条件粒度”与“模型拟合能力”如何影响引导效果。

#### 8. 不足与局限
*   **模态限制**：目前主要针对连续扩散模型（Continuous Diffusion），尚未在离散扩散模型（Discrete Diffusion）或其他生成范式上验证。
*   **超参数敏感性**：虽然论文给出了推荐值，但分段点 $\tau$ 在不同模型和任务之间可能仍需微调以达到最优帕累托前沿。
*   **训练成本**：虽然有高效变体，但完整的 SGG 训练集成仍比标准训练多出约 20% 的算力消耗。

（完）
