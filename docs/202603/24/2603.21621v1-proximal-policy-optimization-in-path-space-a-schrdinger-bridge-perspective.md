---
title: "Proximal Policy Optimization in Path Space: A Schrödinger Bridge Perspective"
title_zh: 路径空间中的近端策略优化：薛定谔桥视角
authors: "Yuehu Gong, Zeyuan Wang, Yulin Chen, Yanwei Fu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21621v1"
tags: ["query:rl-dm"]
score: 10.0
evidence: 路径空间中基于扩散策略的在线强化学习
tldr: 针对生成式策略（如扩散模型）在强化学习中难以直接应用传统PPO的问题，本文提出了GSB-PPO框架。该框架受广义薛定谔桥启发，将PPO的近端更新从动作空间提升至路径空间，实现了生成轨迹层面的策略优化。研究提出了Clip和Penalty两种目标函数，实验证明Penalty版本在稳定性和性能上更具优势，为生成式策略的在线强化学习提供了有效路径。
selection_source: fresh_fetch
motivation: 传统的PPO基于动作空间概率比，难以直接应用于以轨迹生成为特征的扩散或流匹配策略。
method: 提出GSB-PPO框架，利用广义薛定谔桥理论将PPO的近端正则化从单步动作扩展到完整的生成路径空间。
result: 实验表明，相比于裁剪式目标，基于惩罚项的GSB-PPO-Penalty在训练稳定性和最终性能上表现更优。
conclusion: 路径空间近端正则化是训练生成式策略的一种有效原则，为在线策略强化学习提供了统一视角。
---

## 摘要
基于生成式策略的同策略（On-policy）强化学习前景广阔，但仍有待深入探索。一个核心挑战在于，近端策略优化（PPO）传统上是根据动作空间概率比率来制定的，而基于扩散（diffusion）和流（flow）的策略则更自然地被表示为轨迹级的生成过程。在这项工作中，我们提出了 GSB-PPO，这是一种受广义薛定谔桥（GSB）启发的生成式 PPO 的路径空间表述。我们的框架将 PPO 风格的近端更新从终端动作提升到完整的生成轨迹，从而为生成式策略的同策略优化提供了一个统一的视角。在该框架内，我们开发了两个具体的目标函数：基于裁剪的目标函数 GSB-PPO-Clip 和基于惩罚的目标函数 GSB-PPO-Penalty。实验结果表明，虽然这两个目标函数都与同策略训练兼容，但惩罚项表述在稳定性和性能上始终优于裁剪项表述。总之，我们的结果强调了路径空间近端正则化是使用 PPO 训练生成式策略的一种有效原则。

## Abstract
On-policy reinforcement learning with generative policies is promising but remains underexplored. A central challenge is that proximal policy optimization (PPO) is traditionally formulated in terms of action-space probability ratios, whereas diffusion- and flow-based policies are more naturally represented as trajectory-level generative processes. In this work, we propose GSB-PPO, a path-space formulation of generative PPO inspired by the Generalized Schrödinger Bridge (GSB). Our framework lifts PPO-style proximal updates from terminal actions to full generation trajectories, yielding a unified view of on-policy optimization for generative policies. Within this framework, we develop two concrete objectives: a clipping-based objective, GSB-PPO-Clip, and a penalty-based objective, GSB-PPO-Penalty. Experimental results show that while both objectives are compatible with on-policy training, the penalty formulation consistently delivers better stability and performance than the clipping counterpart. Overall, our results highlight path-space proximal regularization as an effective principle for training generative policies with PPO.

---

## 论文详细总结（自动生成）

这篇论文《路径空间中的近端策略优化：薛定谔桥视角》（Proximal Policy Optimization in Path Space: A Schrödinger Bridge Perspective）探讨了如何将经典的近端策略优化（PPO）算法有效地应用于生成式策略（如扩散模型或流匹配模型）。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的 PPO 算法是在**动作空间**中定义的，依赖于新旧策略在单步动作上的概率比率。然而，现代生成式策略（如扩散模型）是一个多步的随机生成过程，其最终动作是完整生成轨迹的终端输出。
*   **核心挑战**：直接在动作空间应用 PPO 会忽略生成轨迹的内部结构，导致在处理复杂、多峰值的动作分布时，优化过程不够稳定或无法充分利用生成模型的表达能力。
*   **研究目标**：受广义薛定谔桥（GSB）理论启发，将 PPO 的近端更新从“单步动作空间”提升到“多步路径空间”，为生成式策略的在线（On-policy）训练提供一个统一且稳定的框架。

### 2. 论文提出的方法论
*   **核心思想**：将生成式策略视为路径空间上的概率分布 $P_\theta(a^{(0:N)}|s)$。通过优化完整生成轨迹的分布，而非仅仅是最终动作的边缘分布，来实现策略更新。
*   **关键技术细节**：
    *   **路径比率（Path Ratio）**：定义了新旧策略在整个生成路径上的似然比率，作为 PPO 更新的基础。
    *   **GSB-PPO-Clip**：直接将 PPO 的裁剪机制应用到路径比率上。但实验发现，由于似然偏移在多步生成中会累积，这种方式较为敏感且不稳定。
    *   **GSB-PPO-Penalty（核心推荐方案）**：放弃硬裁剪，转而使用基于路径空间的 KL 散度惩罚项。具体实现为新旧策略漂移函数（Drift Function）之间的均方误差（MSE）正则化。
*   **算法流程**：
    1. 使用旧策略采样生成轨迹并收集环境奖励。
    2. 使用广义优势估计（GAE）计算优势函数。
    3. 在路径空间计算路径比率。
    4. 根据选择的模式（Clip 或 Penalty）更新策略参数，其中 Penalty 模式通过约束生成动力学的变化来保证更新的平滑性。

### 3. 实验设计
*   **实验场景**：使用了 10 个基于 MuJoCo Playground 的连续控制环境（如 BallInCup, CartpoleBalance, CheetahRun, FingerSpin, ReacherHard 等）。
*   **Benchmark（基准方法）**：
    *   **标准 PPO**：使用高斯分布作为策略的传统 PPO。
    *   **FPO (Flow matching Policy Optimization)**：目前最先进的基于流匹配的在线生成式策略优化方法。
*   **对比维度**：对比了 GSB-PPO-Clip、GSB-PPO-Penalty 与上述基准方法在训练曲线、最终回报和稳定性上的差异。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **实现框架**：提到代码基于 JAX 和 MuJoCo Playground 框架构建，利用了 JAX 的并行加速特性。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同的控制任务上进行了测试，涵盖了从简单平衡到复杂运动的不同难度。
*   **消融实验**：针对 `GSB-PPO-Penalty` 中的关键超参数（KL 惩罚系数 $\beta$）在 CheetahRun 环境下做了专门的消融研究。
*   **充分性评价**：实验设计较为客观，通过与标准 PPO 和最新的 FPO 对比，证明了路径空间视角带来的增益。但在超参数敏感性分析方面，仅针对单一环境做了消融，覆盖面略显不足。

### 6. 论文的主要结论与发现
*   **路径空间的优越性**：将 PPO 提升到路径空间后，生成式策略在多个任务上的表现显著优于传统的高斯策略 PPO。
*   **惩罚项优于裁剪项**：在路径空间中，基于惩罚（Penalty）的正则化比直接裁剪（Clip）路径比率要稳定得多。裁剪机制在处理多步累积的概率比时容易失效。
*   **性能领先**：GSB-PPO-Penalty 在大多数测试环境中达到了与 FPO 相当或更优的性能，且提供了一个更具通用性的 PPO 扩展视角。

### 7. 优点
*   **理论视角新颖**：引入薛定谔桥视角，为生成式策略的在线优化提供了坚实的数学基础。
*   **统一框架**：将扩散模型和流匹配模型统一在路径空间分布的框架下，具有较强的通用性。
*   **设计洞察**：明确指出了路径空间中“惩罚项”比“裁剪项”更适合作为归纳偏置（Inductive Bias），为后续研究提供了实用的设计参考。

### 8. 不足与局限
*   **计算开销**：虽然论文未详述，但路径空间的优化涉及多步生成轨迹的梯度计算，其计算成本通常高于单步动作空间的方法。
*   **环境多样性**：实验主要集中在 MuJoCo 模拟环境，尚未在更复杂的视觉输入任务或真实机器人任务上验证。
*   **超参数敏感性**：惩罚系数 $\beta$ 对性能影响较大（如 Cartpole 任务需要特殊调整），这可能增加实际应用中的调参难度。

（完）
