---
title: Reward Sharpness-Aware Fine-Tuning for Diffusion Models
title_zh: 扩散模型的奖励锐度感知微调
authors: "Kwanyoung Kim, Byeongsu Sim"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21175v1"
tags: ["query:rl-dm"]
score: 10.0
evidence: 以奖励为中心的扩散强化学习 (RDRL)
tldr: 针对扩散模型在强化学习微调中出现的“奖励作弊”现象（即奖励分高但生成质量差），本文提出了RSA-FT框架。该研究发现奖励景观的剧烈波动是导致梯度不鲁棒的主因，因此通过对扩散模型参数和生成样本进行扰动，利用平滑后的奖励梯度进行微调。实验证明，RSA-FT能有效抑制奖励作弊，显著提升生成图像的感知质量和鲁棒性，为扩散模型与人类偏好对齐提供了更可靠的方案。
selection_source: fresh_fetch
motivation: 扩散模型在基于奖励的强化学习微调中容易陷入奖励作弊，导致奖励分数虚高而生成质量下降。
method: 提出RSA-FT框架，通过对模型参数和生成样本引入扰动来平滑奖励景观，从而利用更稳健的梯度进行微调。
result: 实验结果显示，该方法能有效缓解奖励作弊问题，并在多种任务中显著增强了生成结果的感知质量。
conclusion: RSA-FT是一种简单、通用且有效的微调框架，显著提升了扩散模型在强化学习对齐过程中的可靠性。
---

## 摘要
基于人类反馈的强化学习 (RLHF) 已被证明在使大语言模型与人类偏好对齐方面非常有效，这启发了以奖励为中心的扩散强化学习 (RDRL) 的发展，以实现类似的对齐和可控性。虽然扩散模型可以生成高质量的输出，但 RDRL 仍然容易受到奖励作弊 (reward hacking) 的影响，即奖励分数增加但感知质量却没有相应提升。我们证明了这种脆弱性源于奖励模型梯度的非鲁棒性，特别是当相对于输入图像的奖励景观 (reward landscape) 较为锐利时。为了缓解这一问题，我们引入了一些方法，在不需要重新训练奖励模型的情况下，利用来自鲁棒化奖励模型的梯度。具体而言，我们采用了来自平滑化奖励模型的梯度，该模型是通过对扩散模型进行参数扰动以及对其生成的样本进行扰动而获得的。实验表明，每种方法都能独立地缓解奖励作弊并提高鲁棒性，而它们的结合使用则进一步增强了这些益处。我们最终得到的框架 RSA-FT（奖励锐度感知微调）简单且具有广泛的兼容性，并能持续增强 RDRL 的可靠性。

## Abstract
Reinforcement learning from human feedback (RLHF) has proven effective in aligning large language models with human preferences, inspiring the development of reward-centric diffusion reinforcement learning (RDRL) to achieve similar alignment and controllability. While diffusion models can generate high-quality outputs, RDRL remains susceptible to reward hacking, where the reward score increases without corresponding improvements in perceptual quality. We demonstrate that this vulnerability arises from the non-robustness of reward model gradients, particularly when the reward landscape with respect to the input image is sharp. To mitigate this issue, we introduce methods that exploit gradients from a robustified reward model without requiring its retraining. Specifically, we employ gradients from a flattened reward model, obtained through parameter perturbations of the diffusion model and perturbations of its generated samples. Empirically, each method independently alleviates reward hacking and improves robustness, while their joint use amplifies these benefits. Our resulting framework, RSA-FT (Reward Sharpness-Aware Fine-Tuning), is simple, broadly compatible, and consistently enhances the reliability of RDRL.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reward Sharpness-Aware Fine-Tuning for Diffusion Models》（扩散模型的奖励锐度感知微调）的结构化深入分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在扩散模型的对齐过程中，以奖励为中心的强化学习（RDRL）虽然能提升模型表现，但普遍面临**“奖励作弊”（Reward Hacking）**问题。即模型通过生成带有伪影或不符合人类审美的图像来“欺骗”奖励模型，导致奖励分数虚高而实际视觉质量下降。
*   **核心发现**：作者发现奖励作弊与奖励模型的**梯度不鲁棒性**密切相关。当奖励景观（Reward Landscape）相对于输入图像非常“锐利”（即微小变化导致奖励剧烈波动）时，模型容易沿着这些非鲁棒的梯度方向优化，从而陷入作弊陷阱。
*   **整体含义**：本文旨在不重新训练奖励模型的前提下，通过平滑奖励景观来引导扩散模型进行更稳健的微调。

### 2. 论文提出的方法论：RSA-FT
*   **核心思想**：借鉴“锐度感知最小化”（SAM）和“对抗训练”（AT）的思想，提出**奖励锐度感知微调（RSA-FT）**。其核心逻辑是在微调时，不直接最大化原始奖励，而是最大化一个**“平滑化”后的奖励**（即局部邻域内的最小奖励值）。
*   **关键技术细节**：
    *   **输入空间扰动（对抗性）**：在生成的图像 $x_0$ 上施加微小扰动 $\delta$，寻找使奖励下降最快的方向。公式表达为寻找 $\min r(x_0 + \delta)$。
    *   **参数空间扰动（SAM式）**：在扩散模型参数 $\theta$ 上施加扰动 $\epsilon$，寻找导致奖励下降的参数邻域。
    *   **联合优化目标**：RSA-FT 同时在图像空间和参数空间进行扰动，最终目标函数为：$\max_{\theta} E [ r(x_0(\theta + \epsilon) + \delta) ]$。
*   **算法流程**：
    1.  采样噪声和文本提示。
    2.  生成图像 $x_0$。
    3.  计算图像空间的对抗扰动 $\delta$。
    4.  计算参数空间的扰动 $\epsilon$。
    5.  利用扰动后的参数和图像计算梯度，更新原始模型参数 $\theta$。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：HPSv2 训练集。
    *   **测试集/Benchmark**：DrawBench（通用评估）和 HPSv2 测试集。
*   **对比基准（Baselines）**：
    *   涵盖了主流的 RDRL 框架：**DRaFT-K、AlignProp、ReFL、DRTune**。
    *   涵盖了多种扩散模型骨干：**SD1.5、SDXL、SD3、Flux.1-dev**。
*   **评估指标**：
    *   自动指标：HPSv2.1、PickScore、ImageReward（作为代理指标）。
    *   人工评估：17位独立标注者进行视觉偏好和文本对齐度评分。

### 4. 资源与算力
*   **硬件设备**：
    *   **SD1.5 微调**：使用 2 张 NVIDIA H100 GPU。
    *   **SDXL / SD3 / Flux 微调**：使用 4 张 NVIDIA H100 GPU。
*   **训练配置**：
    *   SD1.5 训练 2500 次迭代；SDXL/SD3/Flux 训练 2000 次迭代。
    *   Batch Size 为 32，使用 AdamW 优化器。
    *   学习率设定在 $1\times10^{-5}$ 到 $2\times10^{-5}$ 之间。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   在 4 种不同的扩散模型架构上验证了通用性。
    *   在 4 种不同的强化学习框架上验证了兼容性。
    *   **消融实验**：分别验证了仅使用输入扰动、仅使用参数扰动以及两者结合的效果。
    *   **超参数分析**：对扰动半径 $\rho$ 进行了敏感度测试。
    *   **多奖励场景**：验证了在同时优化多个奖励模型时的表现。
*   **充分性评价**：实验设计非常充分且客观。通过跨模型、跨框架的对比，有力地证明了 RSA-FT 是一个“即插即用”且效果稳定的通用模块。

### 6. 论文的主要结论与发现
*   **负相关性确认**：实验证实奖励景观的锐度与人类偏好呈显著负相关（相关系数达 -0.802）。
*   **有效抑制作弊**：RSA-FT 能在提升主奖励（如 HPSv2）的同时，带动其他辅助奖励（如 PickScore）同步提升，证明了其生成的图像更符合人类真实偏好，而非仅仅刷高单一指标。
*   **视觉质量提升**：定性分析显示，RSA-FT 生成的图像在文本对齐度、人体结构完整性和画面清晰度上均优于原始 RDRL 方法。

### 7. 优点：亮点总结
*   **理论联系实际**：首次将 SAM 和对抗训练的对偶性引入扩散模型强化学习领域，为解决奖励作弊提供了坚实的理论视角。
*   **极强的通用性**：作为一种“即插即用”的方法，它不需要重新训练复杂的奖励模型，也不改变原有的 RDRL 框架结构。
*   **显著的鲁棒性**：在从 512 像素（SD1.5）到 1024 像素（SDXL/SD3/Flux）的不同分辨率和不同架构（U-Net vs Transformer）下均表现稳健。

### 8. 不足与局限
*   **计算开销**：由于每一步更新都需要计算额外的扰动梯度（两次前向/后向传播），训练时间会比标准 RDRL 有所增加。
*   **人类评估规模**：虽然引入了人工评估，但 17 人的规模相对较小，可能存在一定的统计偏差。
*   **代理指标依赖**：尽管使用了多个奖励模型进行交叉验证，但本质上仍依赖于现有的自动评估模型，而这些模型本身可能也存在偏见。
*   **平滑策略单一**：目前主要采用单步梯度上升来近似局部最小值，未来可能需要探索更复杂的平滑技术（如高斯平滑）。

（完）
