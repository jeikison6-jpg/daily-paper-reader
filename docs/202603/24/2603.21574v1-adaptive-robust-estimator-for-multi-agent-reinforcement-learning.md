---
title: Adaptive Robust Estimator for Multi-Agent Reinforcement Learning
title_zh: 面向多智能体强化学习的自适应鲁棒估计器
authors: "Zhongyi Li, Wan Tian, Jingyu Chen, Kangyao Huang, Huiming Zhang, Hui Yang, Tao Ren, Jinyang Jiang, Yijie Peng, Yikun Ban, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21574v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 鲁棒多智能体强化学习框架
tldr: 针对多智能体强化学习在协作推理中面临的信用分配困难及奖励噪声导致的训练不稳定问题，本文提出了一种鲁棒的多智能体框架。该框架包含DACR结构化流水线，将推理分解为回答、评论和重写三个阶段以明确贡献归属；同时引入ARE自适应鲁棒估计器，有效缓解重尾和噪声奖励对策略优化的干扰。实验证明该方法在数学推理和具身智能任务中表现优异，显著提升了训练的稳定性和鲁棒性。
selection_source: fresh_fetch
motivation: 解决多智能体协作推理中的交互模糊性导致的信用分配难题，以及噪声奖励引发的训练不稳定和策略偏差。
method: 提出由DACR（三阶段推理流水线）和ARE（自适应鲁棒估计器）组成的框架，实现明确的贡献归属和稳健的批次经验均值估计。
result: 在数学推理和具身智能基准测试中，该方法在同构和异构设置下均优于基线，且在噪声奖励环境下表现出极强的鲁棒性。
conclusion: 该研究通过结构化交互和鲁棒估计技术，有效防止了由噪声信号引起的优化失败，为多智能体协作推理提供了更稳定的训练动力。
---

## 摘要
多智能体协作已成为增强大语言模型推理能力的强大范式，但它面临着交互层面的模糊性，这种模糊性模糊了生成、批判和修订之间的界限，使得跨智能体的信用分配（credit assignment）变得困难。此外，在这种环境下的策略优化容易受到重尾（heavy-tailed）和噪声奖励的影响，这可能会使优势估计产生偏差，并引发不稳定甚至发散的训练。为了解决这两个问题，我们提出了一种用于协作推理的鲁棒多智能体强化学习框架，由两个组件组成：双智能体“回答-批判-重写”（DACR）和自适应鲁棒估计器（ARE）。DACR 将推理分解为一个结构化的三阶段流水线：回答、批判和重写，同时能够显式地将每个智能体的边际贡献归因于其合作伙伴的性能。ARE 在多智能体策略优化过程中提供批次经验均值的鲁棒估计。在数学推理和具身智能基准测试中，即使在存在噪声奖励的情况下，我们的方法在同构和异构设置下均始终优于基准方法。这些结果表明，该方法对奖励噪声具有更强的鲁棒性，且训练动态更稳定，有效防止了由噪声奖励信号引起的优化失败。

## Abstract
Multi-agent collaboration has emerged as a powerful paradigm for enhancing the reasoning capabilities of large language models, yet it suffers from interaction-level ambiguity that blurs generation, critique, and revision, making credit assignment across agents difficult. Moreover, policy optimization in this setting is vulnerable to heavy-tailed and noisy rewards, which can bias advantage estimation and trigger unstable or even divergent training. To address both issues, we propose a robust multi-agent reinforcement learning framework for collaborative reasoning, consisting of two components: Dual-Agent Answer-Critique-Rewrite (DACR) and an Adaptive Robust Estimator (ARE). DACR decomposes reasoning into a structured three-stage pipeline: answer, critique, and rewrite, while enabling explicit attribution of each agent's marginal contribution to its partner's performance. ARE provides robust estimation of batch experience means during multi-agent policy optimization. Across mathematical reasoning and embodied intelligence benchmarks, even under noisy rewards, our method consistently outperforms the baseline in both homogeneous and heterogeneous settings. These results indicate stronger robustness to reward noise and more stable training dynamics, effectively preventing optimization failures caused by noisy reward signals.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Robust Estimator for Multi-Agent Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）通过多智能体协作（如解决者与评估者配对）能有效提升推理能力。目前主流采用强化学习（如 GRPO）进行优化。
*   **核心问题**：
    1.  **信用分配困难**：多智能体交互中的生成、批判和修订界限模糊，难以衡量单个智能体对最终结果的边际贡献。
    2.  **奖励信号不稳定**：推理任务的奖励通常由验证器或奖励模型生成，具有**重尾分布（Heavy-tailed）**和**高噪声**特征。标准 GRPO 依赖批次经验均值进行归一化，极少数异常值（Outliers）就会导致优势估计偏差，引发训练震荡甚至发散。

### 2. 方法论
论文提出了一个鲁棒的多智能体强化学习框架，包含两个核心组件：
*   **DACR（双智能体回答-批判-重写协议）**：
    *   **结构化交互**：将协作分解为“独立回答”、“相互批判”和“根据反馈修订”三个显式阶段。
    *   **交叉评价奖励机制**：除了基础的正确性奖励，引入“边际改进项”，即如果智能体 A 的批判导致智能体 B 的修订版得分提升，则奖励智能体 A。这实现了轻量级且可解释的信用分配。
*   **ARE（自适应鲁棒估计器）**：
    *   **核心思想**：改进了经典的“均值中位数”（Median-of-Means, MoM）估计器。
    *   **技术细节**：ARE 将样本分成若干块，在块内不直接计算均值，而是通过最小化**自适应鲁棒损失函数（Barron Loss）**来估计位置参数。该损失函数通过形状参数 $\alpha$ 和尺度参数 $c$ 自动调节对离群值的敏感度。
    *   **优化流程**：采用迭代重权最小二乘法（IRLS）和渐进非凸性（GNC）技术，在保证鲁棒性的同时克服非凸优化难题，最终用各块估计值的中位数作为批次基准，替换 GRPO 中的脆弱均值。

### 3. 实验设计
*   **数学推理任务**：
    *   **数据集**：训练集使用 MATH 7.5k；测试集包括 MATH500（分布内）、AIME24/25、AMC23、Gaokao2023en、MinervaMath（分布外/OOD）。
    *   **模型设置**：涵盖同构和异构智能体对，包括 Qwen2.5 (1.5B/7B) 和 Llama3.1 (8B)。
    *   **对比方法**：标准 GRPO 基线、单智能体模型、无 DACR 交互的独立模型。
*   **具身智能任务（验证通用性）**：
    *   **场景**：无人机视觉语言导航（Aerial VLN）。
    *   **基准**：FlightGPT。
    *   **指标**：导航误差（NE）、成功率（SR）、路径长度加权成功率（SPL）等。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **8 张 NVIDIA A800 GPU** 的服务器上完成。
*   **软件框架**：使用 Ray 进行分布式训练编排，vLLM 作为推理引擎（4 个引擎，张量并行度为 2）。
*   **训练细节**：启用了梯度检查点（Gradient Checkpointing）和 Adam 参数卸载（Offloading）以优化显存。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的广泛测试，包括不同参数规模的模型（1.5B 到 8B）、跨模型家族的异构协作、以及针对奖励噪声的压力测试（注入柯西噪声）。
*   **消融实验**：专门针对 DACR 的三阶段交互协议进行了消融，验证了每一阶段对推理性能的提升。
*   **充分性评价**：实验设计较为全面，不仅覆盖了纯文本推理，还跨越到具身智能领域，证明了 ARE 估计器的通用鲁棒性。通过 OOD 数据集的测试，验证了方法的泛化能力。

### 6. 主要结论与发现
*   **性能提升**：在所有数学基准测试中，ARE+DACR 显著优于标准 GRPO。例如在 AMC23 任务上，Qwen2.5-1.5B 的准确率从 22.5% 提升至 32.5%。
*   **鲁棒性验证**：在人为注入重尾噪声的环境下，标准 GRPO 性能大幅下降，而 ARE 表现出极强的抗干扰能力，训练曲线更平滑。
*   **具身智能表现**：在无人机导航任务中，ARE 显著降低了导航误差，尤其在未见过的环境（Unseen environments）中提升最明显。

### 7. 优点与亮点
*   **理论与实践结合**：为 ARE 估计器提供了严谨的理论证明（包括有限方差和重尾分布下的收敛界限）。
*   **解决痛点**：直接针对强化学习在 LLM 训练中常见的“奖励模型不可靠”问题，提出了切实可行的数学解决方案。
*   **结构化创新**：DACR 协议通过强制性的“批判-重写”流程，将模糊的对话转化为清晰的训练轨迹，降低了多智能体 RL 的学习难度。

### 8. 不足与局限
*   **计算开销**：ARE 涉及内部的迭代优化（IRLS/GNC），相比直接计算均值，在每个训练批次中会增加额外的计算延迟。
*   **超参数敏感性**：虽然 ARE 具有自适应性，但 DACR 中的奖励权重 $\gamma$ 以及 ARE 的分块数量 $k$ 仍可能需要针对不同任务进行微调。
*   **多智能体规模限制**：实验主要集中在双智能体协作，尚未探讨更多智能体（如 3 个以上）在更复杂博弈环境下的表现。

（完）
