---
title: Counterfactual Credit Policy Optimization for Multi-Agent Collaboration
title_zh: 面向多智能体协作的反事实贡献策略优化
authors: "Zhongyi Li, Wan Tian, Yikun Ban, Jinju Chen, Huiming Zhang, Yang Liu, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21563v1"
tags: ["query:rl-dm"]
score: 8.0
evidence: 多智能体协作的策略优化
tldr: 针对多智能体大语言模型协作中的信用分配难题，本文提出反事实信用策略优化（CCPO）框架。该框架通过模拟移除特定智能体贡献后的反事实轨迹，估算每个智能体的边际贡献，从而提供角色敏感的学习信号。结合全局历史感知归一化方案，CCPO 有效解决了全局奖励导致的更新方差大和“搭便车”问题。在数学和逻辑推理任务上的实验证明，CCPO 显著提升了协作效率，优于现有的多智能体强化学习基准。
selection_source: carryover_cache
motivation: 协作式多智能体系统中共享的全局奖励难以区分个体贡献，导致强化学习训练中出现高方差和“搭便车”现象。
method: 提出 CCPO 框架，通过构建动态反事实基准来估算智能体的边际贡献，并利用全局历史感知归一化来稳定优势函数。
result: 在数学和逻辑推理基准测试中，CCPO 有效缓解了搭便车问题，并在顺序推理和投票协作拓扑下均优于强基准模型。
conclusion: CCPO 为多智能体 LLM 协作提供了更精细、有效的信用分配机制，显著增强了复杂推理任务中的策略优化效果。
---

## 摘要
协作式多智能体大语言模型（LLM）可以通过角色分解和多样化假设聚合来解决复杂的推理任务。然而，此类系统的强化学习（RL）往往受到贡献分配（credit assignment）问题的困扰：共享的全局奖励掩盖了单个智能体的贡献，导致更新方差增大并诱发“搭便车”现象。我们提出了反事实贡献策略优化（CCPO），该框架通过反事实轨迹估计每个智能体的边际贡献，从而分配特定于智能体的学习信号。CCPO 构建了动态反事实基准，模拟移除某个智能体贡献后的结果，为策略优化提供角色敏感的优势函数。为了进一步提高在异构任务和数据分布下的稳定性，我们提出了一种全局历史感知的归一化方案，利用全局采样（rollout）统计数据来校准优势函数。我们在两种协作拓扑结构上评估了 CCPO：顺序的“思考-推理”二元组和多智能体投票。在数学和逻辑推理基准测试中，CCPO 缓解了搭便车问题，且性能优于强力的多智能体强化学习基线，为协作式 LLM 训练提供了更细粒度且更有效的贡献分配。我们的代码可在 https://github.com/bhai114/ccpo 获取。

## Abstract
Collaborative multi-agent large language models (LLMs) can solve complex reasoning tasks by decomposing roles and aggregating diverse hypotheses. Yet, reinforcement learning (RL) for such systems is often undermined by credit assignment: a shared global reward obscures individual contributions, inflating update variance and encouraging free-riding. We introduce Counterfactual Credit Policy Optimization (CCPO), a framework that assigns agent-specific learning signals by estimating each agent's marginal contribution through counterfactual trajectories. CCPO builds dynamic counterfactual baselines that simulate outcomes with an agent's contribution removed, yielding role-sensitive advantages for policy optimization. To further improve stability under heterogeneous tasks and data distributions, we propose a global-history-aware normalization scheme that calibrates advantages using global rollout statistics. We evaluate CCPO on two collaboration topologies: a sequential Think--Reason dyad and multi-agent voting. Across mathematical and logical reasoning benchmarks, CCPO mitigates free-riding and outperforms strong multi-agent RL baselines, yielding finer-grained and more effective credit assignment for collaborative LLM training. Our code is available at https://github.com/bhai114/ccpo.

---

## 论文详细总结（自动生成）

### 论文总结：面向多智能体协作的反事实贡献策略优化 (CCPO)

#### 1. 核心问题与整体含义（研究动机和背景）
在多智能体大语言模型（LLM）协作系统中，多个模型通过分工（如一个思考、一个推理）或聚合（如投票）来解决复杂任务。然而，现有的强化学习（RL）训练面临**信用分配（Credit Assignment）**难题：
*   **奖励稀疏且共享**：通常只有最终答案的正确与否（0/1）作为奖励，难以区分每个智能体的具体贡献。
*   **搭便车现象（Free-riding）**：即使某个智能体输出了错误或冗余的信息，只要团队最终获胜，它也会获得正向更新，导致训练效率低下且梯度含噪。
*   **角色敏感性缺失**：不同协作模式（顺序 vs. 并行）对智能体的要求不同，简单的奖励共享无法捕捉这种差异。

#### 2. 方法论：核心思想与关键技术
CCPO 的核心思想是**反事实信用分配（Counterfactual Credit Allocation）**，即通过询问“如果移除该智能体的贡献，团队表现会如何？”来衡量其边际贡献。

*   **边际贡献计算**：定义为 $\Delta_i = R_{team} - R_{\neg i}$。其中 $R_{team}$ 是团队联合奖励，$R_{\neg i}$ 是移除智能体 $i$ 后的反事实奖励。
*   **两种协作拓扑的实例化**：
    *   **顺序协作（Think-Solve）**：智能体 1 生成思考过程，智能体 2 生成答案。计算智能体 1 的贡献时，让智能体 2 在没有思考过程的情况下独立回答，以此作为反事实基准。
    *   **并行协作（Voting）**：多个智能体独立投票。计算某智能体的贡献时，直接从投票池中剔除其结果并重新计票，无需额外推理开销。
*   **全局历史感知归一化**：为了稳定训练，CCPO 使用指数移动平均（EMA）维护全局的边际贡献均值和方差，对优势函数（Advantage）进行标准化处理，并使用 `tanh` 进行数值缩放。
*   **策略优化**：基于 GRPO（Group Relative Policy Optimization）框架，将计算出的智能体特定优势信号用于裁剪后的策略梯度更新。

#### 3. 实验设计
*   **数据集**：
    *   **数学推理**：训练集使用 MATH 7.5k；测试集包括 MATH500（分布内）以及 AMC23、Gaokao2023en、MinervaMath（分布外/OOD）。
    *   **逻辑推理**：使用 LogiQA 数据集。
*   **基准模型（Backbones）**：Qwen2.5 (1.5B/7B/72B)、Llama3.1-8B。
*   **对比方法**：
    *   单智能体 GRPO。
    *   共享奖励的多智能体 RL（如 ReMA）。
    *   未微调的协作基准（CoT 策略）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练时长**：论文对比了 CCPO 与 ReMA 的运行时间。在相同设置下，CCPO 的训练时间约为 **15.24 小时**，比 ReMA（11.57 小时）增加了约 **31.7%**。这主要是因为在顺序协作模式下，计算反事实奖励需要额外的推理步骤。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 7B 不同参数规模的模型，验证了算法的可扩展性。
*   **充分性**：
    *   **多场景验证**：同时验证了“思考-推理”异步协作和“投票”同步协作两种模式。
    *   **泛化性测试**：通过多个 OOD 数据集验证了模型不仅是记住了训练分布，而是提升了推理能力。
    *   **消融/直观分析**：通过可视化奖励分布（图 4）证明了反事实奖励比共享奖励更符合人类直觉（即：能识别出谁在真正出力）。
*   **公平性**：对比实验在相同的采样数（N=4）和超参数框架下进行，具有较高的客观性。

#### 6. 主要结论与发现
*   **性能提升**：CCPO 在所有数学和逻辑基准测试中均优于共享奖励的基线（如 ReMA），在 MATH500 上相比单智能体提升显著。
*   **缓解搭便车**：实验证明，CCPO 能够识别并惩罚负面贡献。例如，当智能体 2 独立回答比协作回答更准时，智能体 1 会收到负向反馈。
*   **协作有效性**：通过对比“有无思考过程”的实验，确认了智能体 2 确实在利用智能体 1 的输出，而非单纯学习忽略对方。

#### 7. 优点
*   **信用分配精准**：通过反事实逻辑，从数学上解决了多智能体强化学习中的“贡献模糊”问题。
*   **拓扑通用性**：方法论不局限于特定架构，可灵活应用于顺序流或并行流。
*   **训练稳定性**：引入全局历史归一化，有效控制了因反事实计算带来的潜在方差波动。

#### 8. 不足与局限
*   **计算开销**：在顺序协作中，为了获得反事实基准，需要额外的推理采样（如 Agent 2 的 Solo 运行），这增加了训练的时间成本。
*   **拓扑限制**：虽然支持投票和简单链式协作，但对于更复杂的图状协作或多轮对话协作，反事实轨迹的构建会变得极其复杂且昂贵。
*   **奖励函数依赖**：依然依赖于可自动判分的客观奖励（如数学题的正确性），对于主观性较强的任务（如创意写作协作）可能难以定义反事实基准。

（完）
