---
title: Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning
title_zh: 基于可微世界模型的离线强化学习模型预测控制
authors: "Rohan Deb, Stephen J. Wright, Arindam Banerjee"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22430v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 离线强化学习与扩散规划方法
tldr: 本文针对离线强化学习在推理阶段策略固定的局限性，提出了一种结合可微世界模型（DWM）的模型预测控制（MPC）框架。该框架利用预训练策略和学习到的动力学模型，在推理时通过想象轨迹的端到端梯度计算来实时优化策略参数。在D4RL基准测试上的实验表明，这种推理时自适应机制能显著提升离线RL算法的性能。
selection_source: fresh_fetch
motivation: 传统的离线强化学习方法在推理阶段缺乏灵活性，无法利用环境反馈或模型预测来实时优化已学习的策略。
method: 构建了一个可微世界模型流水线，通过MPC在推理时对想象的轨迹进行端到端梯度反向传播，从而动态调整策略参数。
result: 在D4RL的MuJoCo运动任务和AntMaze任务中，该方法相比现有的强离线RL基准展现出持续且显著的性能增益。
conclusion: 通过可微世界模型在推理阶段利用实时信息优化策略，是增强离线强化学习效果的一种有效且通用的手段。
---

## 摘要
离线强化学习（RL）旨在从固定的离线数据集中学习最优策略，而无需与环境进行进一步交互。此类方法通常训练一个离线策略（或价值函数），并在推理阶段直接应用，而不进行进一步的精细化调整。我们引入了一种受模型预测控制（MPC）启发的推理阶段自适应框架，该框架结合了预训练策略以及学习到的状态转移和奖励世界模型。虽然现有的世界模型和扩散规划方法在训练期间利用学习到的动力学生成想象轨迹，或在推理阶段采样候选规划，但它们并未利用推理阶段的信息来实时优化策略参数。相比之下，我们设计了一个可微世界模型（DWM）流水线，能够通过想象展开进行端到端的梯度计算，从而在推理阶段基于 MPC 进行策略优化。我们在 D4RL 连续控制基准（MuJoCo 运动任务和 AntMaze）上评估了该算法，结果表明，利用推理阶段的信息来优化策略参数，相较于强大的离线强化学习基准模型，能够带来持续的性能提升。

## Abstract
Offline Reinforcement Learning (RL) aims to learn optimal policies from fixed offline datasets, without further interactions with the environment. Such methods train an offline policy (or value function), and apply it at inference time without further refinement. We introduce an inference time adaptation framework inspired by model predictive control (MPC) that utilizes a pretrained policy along with a learned world model of state transitions and rewards. While existing world model and diffusion-planning methods use learned dynamics to generate imagined trajectories during training, or to sample candidate plans at inference time, they do not use inference-time information to optimize the policy parameters on the fly. In contrast, our design is a Differentiable World Model (DWM) pipeline that enables endto-end gradient computation through imagined rollouts for policy optimization at inference time based on MPC. We evaluate our algorithm on D4RL continuous-control benchmarks (MuJoCo locomotion tasks and AntMaze), and show that exploiting inference-time information to optimize the policy parameters yields consistent gains over strong offline RL baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的离线强化学习（Offline RL）方法通常在训练结束后得到一个固定的策略，在推理（部署）阶段直接应用。这种方式无法利用推理时遇到的具体状态信息来进一步优化决策。
*   **研究动机**：受大语言模型（LLM）推理时计算（Inference-time computation/Reasoning）能提升性能的启发，作者探讨在离线 RL 中，是否可以通过额外的推理时计算来弥补离线训练时价值函数估计不准或数据分布偏移的问题。
*   **整体含义**：本文提出了一种结合模型预测控制（MPC）和可微世界模型（DWM）的框架，允许智能体在推理阶段通过“想象”未来的轨迹并计算梯度，实时微调策略参数。

### 2. 论文提出的方法论：MPCwDWM
*   **核心思想**：将世界模型（状态转移、奖励函数）构建为可微的计算图。在推理时的每一个时间步，智能体先在“头脑”中进行多步模拟，根据模拟结果的反馈（梯度）更新策略参数，然后再执行动作。
*   **关键技术细节**：
    *   **可微扩散采样器 ($f_\theta$)**：使用条件扩散模型建模状态转移。通过固定扩散过程中的高斯噪声，使采样过程变成一个确定的、可对输入求导的计算图。
    *   **奖励模型 ($r_\xi$) 与终端价值 ($Q_\phi$)**：学习一个回归奖励模型，并利用预训练离线 RL 算法（如 ReBRAC）的 Critic 作为有限步展开后的终端价值估计。
    *   **推理时优化**：从当前状态 $s_t$ 出发，利用当前策略 $\pi_\psi$ 和世界模型生成 $H$ 步的想象轨迹。
    *   **梯度递归（Theorem 4.1）**：推导了通过扩散模型展开轨迹的链式法则。利用 Jacobian 矩阵递归计算目标函数（累积奖励 + 终端价值）对策略参数 $\psi$ 的梯度。
*   **算法流程**：观察状态 -> 采样噪声 -> 展开 $H$ 步轨迹 -> 计算代理目标函数 -> 执行 $E$ 步梯度上升更新策略参数 $\psi$ -> 执行更新后的首个动作。

### 3. 实验设计
*   **数据集/场景**：使用了标准的 **D4RL** 连续控制基准测试。
    *   **Gym MuJoCo 运动任务**：包含 HalfCheetah, Hopper, Walker2d 的 18 个子集（Random, Medium, Expert 等）。
    *   **AntMaze 任务**：包含 6 个更具挑战性的迷宫任务（u-maze, medium, large）。
*   **Benchmark 与对比方法**：
    *   **强基准离线 RL**：TD3+BC, IQL, CQL, SAC-RND, 以及作为基础策略的 ReBRAC。
    *   **生成式/模型化方法**：Decision Transformer (DT), Trajectory Transformer (TT), MOPO, MOReL, MBOP, 以及扩散规划方法 Diffuser。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了扩散模型和奖励模型的训练步数为 200k 步。在推理阶段，每个时间步需要进行 $E$ 次梯度更新（通常 $E$ 为较小的整数），这显著增加了推理时的计算延迟，但文中未给出具体的时间开销对比。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 D4RL 中最主流的 24 个数据集，每项任务均在 **10 个不同的随机种子**上运行并取平均值，这在强化学习实验中属于较高的标准。
*   **充分性与客观性**：
    *   对比了从传统保守派（CQL）到最新 SOTA（ReBRAC）以及生成式规划（Diffuser）的各类方法。
    *   提供了世界模型（状态预测和奖励预测）的 RMSE 误差随训练步数变化的曲线，证明了世界模型的可靠性。
    *   实验设计较为公平，通过在相同的预训练策略（ReBRAC）上增加推理优化来展示“增量”效果。

### 6. 论文的主要结论与发现
*   **性能提升**：MPCwDWM 在 18 个 MuJoCo 任务中的 12 个上实现了提升，平均分（85.33）超过了所有对比基准。
*   **复杂任务表现优异**：在 AntMaze 任务中提升显著（平均分从 77.62 升至 85.07），尤其是在 Large-Maze 任务中，证明了推理时优化对长程规划的帮助。
*   **超越扩散规划**：相比于直接采样轨迹的 Diffuser（均分 77.5），通过梯度优化策略参数的方法（均分 94.4，在特定子集上）表现更优。

### 7. 优点（亮点）
*   **端到端可微性**：成功将复杂的扩散模型集成进端到端可微的 MPC 框架中，解决了扩散模型采样过程难以直接优化策略的问题。
*   **推理时自适应**：打破了离线 RL “训练后即固定”的传统范式，为离线 RL 引入了类似人类“三思而后行”的机制。
*   **通用性**：该框架可以插在任何预训练的 Actor-Critic 算法之上作为增强模块。

### 8. 不足与局限
*   **推理延迟**：由于每个动作前都要进行多次梯度反向传播，该方法在实时性要求极高的场景（如高频机器人控制）中可能难以应用。
*   **模型依赖风险**：性能高度依赖于世界模型的准确性。如果世界模型在 OOD（分布外）区域预测错误，梯度优化可能会引导策略走向错误的“幻觉”区域。
*   **计算开销**：论文缺乏对推理时增加的计算成本（时间、内存）的量化分析。
*   **超参数敏感性**：推理时的展开步数 $H$、更新步数 $E$ 和学习率等可能需要针对不同任务精细调优。

（完）
