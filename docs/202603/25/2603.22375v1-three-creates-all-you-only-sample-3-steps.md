---
title: "Three Creates All: You Only Sample 3 Steps"
title_zh: 三生万物：仅需采样三步
authors: "Yuren Cai, Guangyi Wang, Zongqing Li, Li Li, Zhihui Liu, Songzhi Su"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22375v1"
tags: ["query:rl-dm"]
score: 8.0
evidence: 扩散模型中的少步采样与去噪动力学
tldr: 针对扩散模型推理速度慢的问题，本文发现传统的时间步调节是极少步采样的瓶颈。研究者提出了多层时间嵌入优化（MTEO），通过冻结预训练模型并蒸馏出层级、步级的时间嵌入，在不增加推理开销的前提下，仅需3步采样即可实现高质量生成，显著提升了采样效率并达到了SOTA水平。
selection_source: fresh_fetch
motivation: 标准的时间步调节方式在极少步采样时效果不佳，且忽略了模型不同层级间去噪动力学的差异。
method: 提出MTEO方法，冻结骨干网络并从参考轨迹中蒸馏出少量的层级与步级特定时间嵌入。
result: 在多个数据集和模型上实现了极少步采样的领先性能，且训练参数极少，不增加推理负担。
conclusion: MTEO作为一种即插即用的轻量化方案，有效缩小了蒸馏方法与轻量化采样方法之间的性能差距。
---

## 摘要
扩散模型能够实现高保真生成，但由于需要多次连续的网络评估，其推理速度仍然较慢。我们发现，标准的时间步条件（timestep conditioning）成为了少步采样（few-step sampling）的关键瓶颈。受层依赖去噪动力学（layer-dependent denoising dynamics）的启发，我们提出了多层时间嵌入优化（Multi-layer Time Embedding Optimization, MTEO），该方法冻结预训练的扩散骨干网络，并从参考轨迹中蒸馏出一组少量的逐步、逐层时间嵌入。MTEO 与现有的 ODE 求解器即插即用，不增加推理开销，且仅需训练极小比例的参数。在多种数据集和骨干网络上的广泛实验表明，该方法在少步采样中达到了最先进的性能，并显著缩小了基于蒸馏的方法与轻量级方法之间的差距。代码将会公开。

## Abstract
Diffusion models deliver high-fidelity generation but remain slow at inference time due to many sequential network evaluations. We find that standard timestep conditioning becomes a key bottleneck for few-step sampling. Motivated by layer-dependent denoising dynamics, we propose Multi-layer Time Embedding Optimization (MTEO), which freeze the pretrained diffusion backbone and distill a small set of step-wise, layer-wise time embeddings from reference trajectories. MTEO is plug-and-play with existing ODE solvers, adds no inference-time overhead, and trains only a tiny fraction of parameters. Extensive experiments across diverse datasets and backbones show state-of-the-art performance in the few-step sampling and substantially narrow the gap between distillation-based and lightweight methods. Code will be available.