---
title: "P-Flow: Prompting Visual Effects Generation"
title_zh: P-Flow：提示驱动的视觉效果生成
authors: "Rui Zhao, Mike Zheng Shou"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22091v1"
tags: ["query:rl-vg"]
score: 8.0
evidence: 用于视频高级视觉效果的生成模型
tldr: 针对视频生成中动态视觉效果（如爆炸、破碎）难以通过简单文本精确控制的问题，本文提出 P-Flow 框架。这是一种无需训练的提示词优化方法，利用视觉语言模型的语义和时间推理能力，在测试阶段根据参考视频与生成结果的差异迭代精炼提示词。该方法无需修改底层模型，即可在不同场景中实现高保真、多样化的视觉效果定制，显著提升了文本到视频和图像到视频生成的控制力。
selection_source: fresh_fetch
motivation: 复杂的动态视觉效果涉及高层语义和时空演变，现有的低级运动控制手段难以胜任，且人工编写精确提示词极其困难。
method: 采用无需训练的测试时提示词优化策略，通过视觉语言模型反馈循环，使提示词不断演化以匹配目标视觉效果。
result: 在多种视频生成任务中，P-Flow 能够生成比现有模型更符合预期、更具真实感的动态视觉特效。
conclusion: P-Flow 证明了通过自动化提示词优化可以有效实现复杂动态视觉效果的定制化生成，且具有良好的通用性和免训练优势。
---

## 摘要
近期视频生成模型的进展显著提升了其遵循文本提示的能力。然而，动态视觉效果（定义为随时间演变且由外观驱动的视觉现象，如物体破碎或爆炸）的定制化仍未得到充分探索。先前的运动定制或控制工作主要集中在主体或摄像机的低级运动上，这些运动可以通过运动轨迹等显式控制信号进行引导。相比之下，动态视觉效果涉及更高级别的语义，更适合通过文本提示进行控制。然而，人类很难且耗时地编写出能够准确指定这些效果的单一提示，因为它们需要复杂的时序推理和随时间的迭代优化。为了应对这一挑战，我们提出了 P-Flow，这是一种用于在视频生成中定制动态视觉效果的新型免训练框架，无需修改底层模型。通过利用视觉语言模型的语义和时序推理能力，P-Flow 执行测试时提示优化，根据参考视频与生成输出之间视觉效果的差异来细化提示。通过迭代细化，提示不断演化，以在新的场景中更好地诱导所需的动态效果。实验表明，P-Flow 实现了高保真且多样化的视觉效果定制，并在文本生成视频和图像生成视频任务上均优于其他模型。代码可在 https://github.com/showlab/P-Flow 获取。

## Abstract
Recent advancements in video generation models have significantly improved their ability to follow text prompts. However, the customization of dynamic visual effects, defined as temporally evolving and appearance-driven visual phenomena like object crushing or explosion, remains underexplored. Prior works on motion customization or control mainly focus on low-level motions of the subject or camera, which can be guided using explicit control signals such as motion trajectories. In contrast, dynamic visual effects involve higher-level semantics that are more naturally suited for control via text prompts. However, it is hard and time-consuming for humans to craft a single prompt that accurately specifies these effects, as they require complex temporal reasoning and iterative refinement over time. To address this challenge, we propose P-Flow, a novel training-free framework for customizing dynamic visual effects in video generation without modifying the underlying model. By leveraging the semantic and temporal reasoning capabilities of vision-language models, P-Flow performs test-time prompt optimization, refining prompts based on the discrepancy between the visual effects of the reference video and the generated output. Through iterative refinement, the prompts evolve to better induce the desired dynamic effect in novel scenes. Experiments demonstrate that P-Flow achieves high-fidelity and diverse visual effect customization and outperforms other models on both text-to-video and image-to-video generation tasks. Code is available at https://github.com/showlab/P-Flow.

---

## 论文详细总结（自动生成）

# 论文总结：P-Flow: Prompting Visual Effects Generation

## 1. 核心问题与整体含义
*   **研究动机**：尽管当前的视频生成模型（如 Wan 2.1, HunyuanVideo）在遵循文本提示方面表现出色，但对于**动态视觉效果**（如物体爆炸、挤压、消散等涉及复杂时空演变的现象）的精确控制仍然是一个挑战。
*   **核心痛点**：
    1.  **低级控制不足**：传统的运动控制（如轨迹引导）难以捕捉高层语义特效。
    2.  **人工提示词工程困难**：人类很难通过单一提示词准确描述特效的细微时序变化，通常需要反复迭代尝试。
*   **整体含义**：论文提出了 **P-Flow**，一个无需训练（Training-free）的框架，通过在测试阶段利用视觉语言模型（VLM）自动优化提示词，实现对视频特效的定制化生成。

## 2. 方法论
P-Flow 的核心思想是将提示词作为优化变量，利用 VLM 的推理能力闭环精炼生成结果。
*   **噪声先验增强（Noise Prior Enhancement）**：
    *   **流匹配逆转**：通过 Flow Matching Inversion 从参考视频中提取初始潜在噪声。
    *   **SVD 时空过滤**：利用奇异值分解（SVD）去除噪声中与特定外观（纹理、背景）相关的空间细节，仅保留主导运动的时空特征。
    *   **噪声混合**：将提取的运动先验噪声与随机噪声按比例（$\alpha$）混合，平衡生成的稳定性和多样性。
*   **测试时提示词优化（Test-time Prompt Optimization）**：
    *   **VLM 闭环反馈**：使用 Gemini 1.5 Pro 作为“导演”，对比参考视频与当前生成视频的差异。
    *   **结构化分析**：VLM 输出 JSON 格式的分析报告，指出运动速度、节奏、形态上的偏差，并据此更新提示词。
*   **历史轨迹维护（Historical Trajectory Maintenance）**：
    *   **逻辑上下文**：保留过去所有迭代的提示词和分析逻辑，避免重复错误。
    *   **高效视觉上下文**：仅向 VLM 输入参考视频、上一轮视频和当前视频，以节省 Token 开销并保持时序对比。

## 3. 实验设计
*   **数据集**：使用 **Open-VFX** 数据集，包含 15 类动态视觉效果（如爆炸、压碎、充气等），共 675 个高质量视频。
*   **任务场景**：涵盖文本到视频（T2V）和图像到视频（I2V）生成。
*   **对比方法**：
    *   基础模型：Wan 2.1, HunyuanVideo。
    *   人工反馈基准：在基础模型上进行人工提示词修改。
    *   专用模型：**VFX Creator**（基于训练的特效生成模型）。
*   **评价指标**：FID-VID（分布相似度）、FVD（时空连贯性）、Dynamic Degree（动态程度）以及人类偏好评估。

## 4. 资源与算力
*   **硬件环境**：NVIDIA A100 GPU 集群。
*   **推理配置**：采用 8-GPU 分布式推理。
*   **时间开销**：
    *   视频生成：每个视频约 69 秒（480x832 分辨率，81 帧）。
    *   VLM 提示词精炼：每次迭代约 16.3 秒。
    *   总迭代次数：实验中通常设置为 10 次。
*   **显存占用**：每张显卡约占用 40GB 显存。

## 5. 实验数量与充分性
*   **实验规模**：在 Open-VFX 的 15 类特效上进行了广泛测试，每种任务采样 100 个样本进行量化评估。
*   **消融实验**：系统地验证了噪声增强、逻辑上下文、视觉上下文对性能的提升，证明了各模块的必要性。
*   **客观性**：通过与训练驱动的 SOTA 模型（VFX Creator）对比，展示了免训练方法在动态程度上的优势。同时，15 名标注员参与的人类评估进一步验证了主观视觉质量。

## 6. 主要结论与发现
*   **免训练超越训练模型**：P-Flow 在无需微调模型参数的情况下，在动态程度指标上显著超过了专门训练的特效模型。
*   **提示词的力量**：复杂的物理特效可以通过精细化的长文本提示词有效诱导，VLM 能够捕捉到人类难以描述的时空细节。
*   **噪声先验的稳定性**：引入运动相关的噪声先验能显著提高优化过程的收敛速度和生成视频的连贯性。

## 7. 优点
*   **即插即用**：无需对底层视频生成模型进行任何修改，具有极强的通用性。
*   **灵活性高**：支持任意分辨率和长度的参考视频，不产生训练偏差（Dataset Bias）。
*   **语义理解深**：利用 VLM 的高层语义能力，解决了低级运动控制无法处理的复杂特效定制问题。

## 8. 不足与局限
*   **推理成本高**：由于需要多次迭代生成视频，总耗时是单次生成的数倍，不适合实时应用。
*   **依赖 VLM 性能**：优化的效果高度依赖于所使用的 VLM（如 Gemini）的视觉理解能力。
*   **缺乏自适应停止**：目前采用固定迭代次数，对于简单特效可能存在算力浪费，而极难特效可能迭代不足。

（完）
