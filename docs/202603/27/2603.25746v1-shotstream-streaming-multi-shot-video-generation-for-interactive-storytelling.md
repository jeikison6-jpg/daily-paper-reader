---
title: "ShotStream: Streaming Multi-Shot Video Generation for Interactive Storytelling"
title_zh: ShotStream：用于交互式叙事的流式多镜头视频生成
authors: "Yawen Luo, Xiaoyu Shi, Junhao Zhuang, Yutian Chen, Quande Liu, Xintao Wang, Pengfei Wan, Tianfan Xue"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25746v1"
tags: ["query:rl-vg"]
score: 9.0
evidence: 多镜头视频生成与分布匹配蒸馏
tldr: ShotStream 提出了一种用于交互式叙事的因果多镜头视频生成架构。针对现有双向模型延迟高、交互性差的问题，该研究将任务重新定义为基于历史上下文的下一镜头生成。通过双缓存记忆机制保持视觉一致性，并采用两阶段蒸馏策略缓解误差累积。该模型在单 GPU 上实现了 16 FPS 的生成速度，在保持高质量的同时支持实时交互式叙事。
selection_source: fresh_fetch
motivation: 现有的多镜头视频生成模型多采用双向架构，导致交互性受限且生成延迟高，难以满足实时叙事需求。
method: 提出因果生成架构，结合双缓存记忆机制和 RoPE 不连续性指示器，并利用两阶段蒸馏策略优化自回归生成。
result: 在单张 GPU 上实现了 16 FPS 的生成速度和亚秒级延迟，且视频质量与更慢的双向模型相当甚至更优。
conclusion: ShotStream 成功实现了高效、连贯且可交互的多镜头视频流式生成，为实时交互式叙事奠定了基础。
---

## 摘要
多镜头视频生成对于长篇叙事至关重要，但目前的双向架构面临交互性有限和高延迟的问题。我们提出了 ShotStream，这是一种新型的因果多镜头架构，能够实现交互式叙事和高效的即时帧生成。通过将任务重新表述为基于历史上下文的下一镜头生成，ShotStream 允许用户通过流式提示词动态地指导正在进行的叙事。我们首先将文本到视频模型微调为双向下一镜头生成器，然后通过分布匹配蒸馏（Distribution Matching Distillation）将其蒸馏为因果学生模型。为了克服自回归生成中固有的镜头间一致性和误差累积挑战，我们引入了两项关键创新。首先，双缓存记忆机制保持了视觉连贯性：全局上下文缓存保留条件帧以确保镜头间一致性，而局部上下文缓存保存当前镜头内生成的帧以确保镜头内一致性。此外，采用 RoPE 不连续性指示器来明确区分这两个缓存以消除歧义。其次，为了减轻误差累积，我们提出了一种两阶段蒸馏策略。该策略始于以真实历史镜头为条件的镜头内自强制（self-forcing），并逐步扩展到使用自生成历史的镜头间自强制，从而有效地弥合了训练与测试之间的差距。广泛的实验表明，ShotStream 能以亚秒级延迟生成连贯的多镜头视频，在单块 GPU 上达到 16 FPS。它的质量达到或超过了速度较慢的双向模型，为实时交互式叙事铺平了道路。训练、推理代码及模型均已发布。

## Abstract
Multi-shot video generation is crucial for long narrative storytelling, yet current bidirectional architectures suffer from limited interactivity and high latency. We propose ShotStream, a novel causal multi-shot architecture that enables interactive storytelling and efficient on-the-fly frame generation. By reformulating the task as next-shot generation conditioned on historical context, ShotStream allows users to dynamically instruct ongoing narratives via streaming prompts. We achieve this by first fine-tuning a text-to-video model into a bidirectional next-shot generator, which is then distilled into a causal student via Distribution Matching Distillation. To overcome the challenges of inter-shot consistency and error accumulation inherent in autoregressive generation, we introduce two key innovations. First, a dual-cache memory mechanism preserves visual coherence: a global context cache retains conditional frames for inter-shot consistency, while a local context cache holds generated frames within the current shot for intra-shot consistency. And a RoPE discontinuity indicator is employed to explicitly distinguish the two caches to eliminate ambiguity. Second, to mitigate error accumulation, we propose a two-stage distillation strategy. This begins with intra-shot self-forcing conditioned on ground-truth historical shots and progressively extends to inter-shot self-forcing using self-generated histories, effectively bridging the train-test gap. Extensive experiments demonstrate that ShotStream generates coherent multi-shot videos with sub-second latency, achieving 16 FPS on a single GPU. It matches or exceeds the quality of slower bidirectional models, paving the way for real-time interactive storytelling. Training and inference code, as well as the models, are available on our

---

## 论文详细总结（自动生成）

以下是对论文《ShotStream: Streaming Multi-Shot Video Generation for Interactive Storytelling》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多镜头视频生成模型主要采用**双向架构（Bidirectional）**，这导致了两个主要瓶颈：
    1.  **缺乏交互性**：用户必须预先提供所有提示词，无法在生成过程中根据已生成内容动态调整叙事。
    2.  **高延迟**：计算成本随序列长度呈平方级增长，生成长视频（如几百帧）往往需要数十分钟，无法满足实时应用需求。
*   **研究背景**：随着长篇叙事视频生成的需求增加，如何实现既能保持镜头间一致性，又能支持流式（Streaming）输入和实时生成的架构成为关键挑战。

### 2. 方法论：核心思想与关键技术
ShotStream 将多镜头合成重构为一个**自回归的下一镜头生成任务**，核心流程如下：
*   **双向教师模型训练**：首先将预训练的文本到视频模型（Wan2.1）微调为双向下一镜头生成器。它通过**动态采样策略**从历史镜头中提取稀疏上下文帧，并利用**时间标记拼接（Temporal Token Concatenation）**机制将历史信息注入模型。
*   **因果蒸馏（Causal Distillation）**：利用**分布匹配蒸馏（DMD）**算法，将多步迭代的教师模型蒸馏为仅需 4 步推理的因果学生模型。
*   **双缓存记忆机制（Dual-Cache Memory）**：
    *   **全局上下文缓存**：存储稀疏的历史条件帧，确保镜头间（Inter-shot）的一致性。
    *   **局部上下文缓存**：存储当前镜头内已生成的帧，确保镜头内（Intra-shot）的连贯性。
    *   **RoPE 不连续性指示器**：在旋转位置编码（RoPE）中引入相位偏移，显式区分全局和局部缓存，消除时间歧义。
*   **两阶段蒸馏策略**：
    *   **阶段 1（镜头内自强制）**：基于真实历史镜头，让模型学习如何块对块（chunk-by-chunk）地生成当前镜头。
    *   **阶段 2（镜头间自强制）**：基于模型自身生成的历史镜头进行训练，模拟推理时的误差累积环境，从而弥合训练与测试的差距。

### 3. 实验设计
*   **数据集**：使用包含 320K 个多镜头视频的内部数据集进行训练，视频包含 2 到 5 个镜头，总帧数达 250 帧。
*   **Benchmark**：利用 Gemini 2.5 Pro 生成了 100 个涵盖多样化主题的多镜头视频提示词作为测试集。
*   **对比方法**：
    *   **双向模型**：Mask2DiT, EchoShot, CineTrans。
    *   **因果/长视频模型**：Self Forcing, LongLive, Rolling Forcing, Infinity-RoPE。
*   **评估指标**：镜头内/间一致性（DINO/CLIP 相似度）、转换控制（SCA 准确率）、文本对齐度、美学质量及生成速度（FPS）。

### 4. 资源与算力
*   **训练算力**：使用了由 **32 台 NVIDIA H800 GPU** 组成的集群。
*   **推理算力**：在单张 **NVIDIA H200 GPU** 上进行测试。
*   **模型规模**：基于 Wan2.1-T2V-1.3B 参数模型。
*   **训练时长**：教师模型训练 10,000 步，因果适配初始化 2,000 步，两阶段蒸馏分别进行 500 步和 1,000 步。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的定量评估（100 组复杂提示词）和定性对比。
*   **消融实验**：针对上下文采样策略、提示词注入方式、缓存区分策略（RoPE vs 学习型嵌入）以及两阶段蒸馏的必要性进行了详细实验。
*   **用户研究**：邀请了 **54 名参与者**对 24 组随机生成的视频进行盲测，从视觉一致性、提示词遵循和视觉质量三个维度进行评价。
*   **充分性评价**：实验设计全面，涵盖了从底层架构选择到高层训练策略的验证，对比基准涵盖了当前主流的同类模型，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能卓越**：ShotStream 在单 GPU 上达到了 **16 FPS** 的生成速度，比传统的双向模型快 25 倍以上。
*   **质量领先**：在保持实时性的同时，其镜头间的一致性和文本遵循能力优于现有的因果长视频模型，甚至在某些指标上超过了慢速的双向模型。
*   **交互能力**：成功实现了流式提示词驱动的视频生成，用户可以在视频生成过程中实时更改后续剧情。

### 7. 优点：亮点总结
*   **实时交互性**：首个在保持高质量多镜头叙事的同时实现亚秒级延迟和流式交互的架构。
*   **创新的缓存管理**：通过双缓存和 RoPE 偏移巧妙解决了自回归生成中的上下文混淆问题。
*   **训练策略有效**：两阶段自强制蒸馏显著减轻了长序列生成中的误差累积（Exposure Bias）问题。

### 8. 不足与局限
*   **模型容量限制**：在处理极其复杂的场景或文本提示时，仍会出现视觉伪影或不一致现象，这可能受限于 1.3B 的基础模型规模。
*   **加速空间**：虽然已达到 16 FPS，但对于极致的实时交互体验，仍有进一步集成稀疏注意力或注意力汇聚（Attention Sink）技术的空间。
*   **数据依赖**：模型性能高度依赖于高质量的多镜头标注数据。

（完）
