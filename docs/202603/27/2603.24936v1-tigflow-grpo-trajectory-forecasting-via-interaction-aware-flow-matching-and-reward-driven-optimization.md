---
title: "TIGFlow-GRPO: Trajectory Forecasting via Interaction-Aware Flow Matching and Reward-Driven Optimization"
title_zh: TIGFlow-GRPO：基于交互感知流匹配与奖励驱动优化的轨迹预测
authors: "Xuepeng Jing, Wenhuan Lu, Hao Meng, Zhizhi Yu, Jianguo Wei"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24936v1"
tags: ["query:rl-dm"]
score: 9.0
evidence: 使用流匹配进行轨迹生成的奖励驱动优化
tldr: 针对自动驾驶和人群监控中的轨迹预测挑战，本文提出 TIGFlow-GRPO 框架。该框架首先利用条件流匹配（CFM）结合轨迹交互图（TIG）捕捉复杂的代理与场景交互；随后引入 Flow-GRPO 后训练阶段，通过随机 SDE 采样探索轨迹，并利用结合社会规范与物理约束的复合奖励进行优化。该方法有效解决了传统监督学习难以反映行为准则的问题，显著提升了预测的准确性与合规性。
selection_source: fresh_fetch
motivation: 现有的基于流匹配的轨迹预测方法过度依赖监督拟合，导致生成的轨迹往往缺乏对社会规范和物理场景约束的充分考虑。
method: 采用两阶段架构，先通过轨迹交互图增强上下文特征编码，再利用组相对策略优化（GRPO）与随机微分方程采样实现行为对齐。
result: 在 ETH/UCY 和 SDD 等主流数据集上的实验证明，该方法在保持高预测精度的同时，显著增强了轨迹的社会合规性与长时稳定性。
conclusion: 本研究成功将流匹配生成模型与奖励驱动的对齐技术相结合，为动态多媒体环境下的行为感知轨迹建模提供了新颖且有效的路径。
---

## 摘要
人类轨迹预测对于在视觉复杂环境中运行的智能多媒体系统（如自动驾驶和人群监控）至关重要。尽管条件流匹配（CFM）在根据时空观测建模轨迹分布方面表现出强大的能力，但现有方法仍主要侧重于监督拟合，这可能导致生成的轨迹无法充分反映社交规范和场景约束。为了解决这一问题，我们提出了 TIGFlow-GRPO，这是一个将基于流的轨迹生成与行为规则对齐的两阶段生成框架。在第一阶段，我们构建了一个带有轨迹交互图（TIG）模块的 CFM 预测器，以建模细粒度的视觉空间交互并增强上下文编码。该阶段更有效地捕捉了智能体与智能体以及智能体与场景之间的关系，为后续对齐提供了更具信息量的条件特征。在第二阶段，我们进行 Flow-GRPO 后训练，其中确定性流展开被重新表述为随机的 ODE 到 SDE 采样以实现轨迹探索，并且复合奖励结合了视图感知的社交合规性与地图感知的物理可行性。通过评估 SDE 展开探索的轨迹，GRPO 逐步引导多模态预测走向行为上合理的未来。在 ETH/UCY 和 SDD 数据集上的实验表明，TIGFlow-GRPO 在提高预测准确性和长时稳定性的同时，生成的轨迹更符合社交规范且具有物理可行性。这些结果表明，所提出的框架为在动态多媒体环境中连接基于流的轨迹建模与行为感知对齐提供了一种有效途径。

## Abstract
Human trajectory forecasting is important for intelligent multimedia systems operating in visually complex environments, such as autonomous driving and crowd surveillance. Although Conditional Flow Matching (CFM) has shown strong ability in modeling trajectory distributions from spatio-temporal observations, existing approaches still focus primarily on supervised fitting, which may leave social norms and scene constraints insufficiently reflected in generated trajectories. To address this issue, we propose TIGFlow-GRPO, a two-stage generative framework that aligns flow-based trajectory generation with behavioral rules. In the first stage, we build a CFM-based predictor with a Trajectory-Interaction-Graph (TIG) module to model fine-grained visual-spatial interactions and strengthen context encoding. This stage captures both agent-agent and agent-scene relations more effectively, providing more informative conditional features for subsequent alignment. In the second stage, we perform Flow-GRPO post-training,where deterministic flow rollout is reformulated as stochastic ODE-to-SDE sampling to enable trajectory exploration, and a composite reward combines view-aware social compliance with map-aware physical feasibility. By evaluating trajectories explored through SDE rollout, GRPO progressively steers multimodal predictions toward behaviorally plausible futures. Experiments on the ETH/UCY and SDD datasets show that TIGFlow-GRPO improves forecasting accuracy and long-horizon stability while generating trajectories that are more socially compliant and physically feasible. These results suggest that the proposed framework provides an effective way to connect flow-based trajectory modeling with behavior-aware alignment in dynamic multimedia environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《TIGFlow-GRPO: Trajectory Forecasting via Interaction-Aware Flow Matching and Reward-Driven Optimization》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的轨迹预测模型（如扩散模型或流匹配模型）主要依赖**监督学习（MLE）**来拟合数据分布。虽然能捕捉统计规律，但往往无法直接优化非微分的**社会规范**（如避免碰撞）和**物理约束**（如不穿墙）。
*   **研究背景**：在复杂的视觉场景中，人类运动受意图、感知范围、他人行为及场景几何结构的共同影响。单纯的分布拟合容易导致生成的轨迹在统计上合理但在行为上不可行（如发生碰撞或进入禁区）。
*   **研究目标**：建立一个既能保持多模态生成能力，又能通过强化学习（RL）实现行为对齐（Behavioral Alignment）的预测框架。

### 2. 方法论：TIGFlow-GRPO 框架
该方法分为两个阶段：

#### 第一阶段：基于 TIG-GAT 的条件流匹配预训练
*   **TIG-GAT 编码器**：提出“轨迹交互图-门控注意力（TIG-GAT）”模块。它不盲目聚合所有邻居，而是基于**视野（Field-of-View）**准则筛选相关邻居，并利用门控机制细化交互特征，生成更具感知意义的条件向量。
*   **CFM 生成器**：使用条件流匹配（Conditional Flow Matching）作为骨干网络，通过学习向量场将高斯噪声转化为未来轨迹。

#### 第二阶段：Flow-GRPO 后训练对齐
*   **ODE 到 SDE 的转换**：标准的流匹配推理是确定性的（ODE），不利于强化学习的探索。作者将其重新表述为随机微分方程（SDE）采样，在推理中引入受控随机性，从而构建可探索的策略空间。
*   **GRPO 优化**：采用组相对策略优化（Group Relative Policy Optimization）。它通过计算一组生成样本的相对优势（Advantage）来更新模型，无需额外的 Critic 网络，降低了显存开销。
*   **复合奖励函数**：
    *   **视图感知社交奖励**：根据智能体朝向划分强/弱/后方视野区域，对不同区域的距离侵犯施加差异化惩罚。
    *   **地图感知语义奖励**：利用预计算的有向距离场（SDF），对轨迹穿过障碍物或边界的行为进行连续惩罚。
    *   **精度与平滑度奖励**：确保轨迹不偏离真实值且运动自然。

### 3. 实验设计
*   **数据集**：
    *   **ETH/UCY**：包含 5 个行人场景，侧重于评估社交交互建模。
    *   **Stanford Drone Dataset (SDD)**：包含复杂的校园环境，侧重于评估物理边界约束。
*   **评估指标**：ADE（平均位移误差）、FDE（最终位移误差）、Col（碰撞率）。
*   **对比方法**：包括基于图的方法（GroupNet, EqMotion）、扩散模型（MID, SingularTrajectory）以及流匹配基准（MoFlow）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 3090 GPU** 上完成。
*   **训练细节**：论文提到了使用 AdamW 优化器，预训练学习率为 $1 \times 10^{-4}$。在后训练阶段，每组采样 $G=4$ 条轨迹进行 GRPO 优化。具体的训练时长未明确说明，但指出 GRPO 的设计减少了内存开销。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ETH/UCY 的 5 个子集上进行了留一法交叉验证。
    *   在 SDD 数据集上进行了像素级误差测试。
    *   进行了**消融实验**，分别验证了 TIG-GAT、社交奖励、地图奖励和 GRPO 阶段的必要性。
    *   进行了**长时稳定性分析**（1.2s 到 4.8s 的误差漂移测试）和**碰撞率定量分析**。
*   **评价**：实验设计较为充分且客观。通过引入“碰撞率”这一非位移指标，有效地证明了强化学习对齐在“合规性”上的优势，而不仅仅是刷低 ADE/FDE。

### 6. 主要结论与发现
*   **精度提升**：TIGFlow-GRPO 在 ETH/UCY 上的平均 ADE/FDE 达到 0.20/0.31，优于现有 SOTA 方法。
*   **合规性增强**：在 SDD 数据集上，碰撞率从基准的 19.74% 显著降低至 12.29%，证明了奖励驱动优化的有效性。
*   **长时稳定性**：在长达 4.8s 的预测周期内，该方法比传统流匹配模型表现出更强的抗漂移能力。
*   **行为合理性**：定性可视化显示，生成的轨迹束更加紧凑，且能更好地遵循人行道等物理边界。

### 7. 优点与亮点
*   **创新性对齐**：首次将 GRPO 这一在大语言模型（LLM）中流行的对齐技术引入到基于流匹配的轨迹预测中。
*   **随机性桥梁**：巧妙地通过 ODE-to-SDE 转换解决了流匹配模型在强化学习中缺乏探索性的问题。
*   **感知感知建模**：TIG-GAT 考虑了人类真实的视野范围，比单纯基于距离的图网络更符合认知心理学。

### 8. 不足与局限
*   **奖励权重敏感性**：复合奖励函数涉及多个超参数（如各种奖励的权重），在不同场景下可能需要精细调优。
*   **计算开销**：虽然 GRPO 省去了 Critic 网络，但后训练阶段需要多次采样（Rollout），相比纯监督学习增加了训练时间。
*   **视觉信息利用**：目前主要依赖坐标和语义地图，未来若能直接整合原始视频特征或 VLM（视觉语言模型）推理，可能会进一步提升对复杂动态场景的理解。

（完）
