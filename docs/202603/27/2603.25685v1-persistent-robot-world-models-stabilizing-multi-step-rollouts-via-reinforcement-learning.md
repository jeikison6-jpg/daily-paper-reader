---
title: "Persistent Robot World Models: Stabilizing Multi-Step Rollouts via Reinforcement Learning"
title_zh: 持久性机器人世界模型：通过强化学习稳定多步展开
authors: "Jai Bardhan, Patrik Drozdik, Josef Sivic, Vladimir Petrik"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25685v1"
tags: ["query:rl-vg"]
score: 10.0
evidence: 用于稳定视频世界模型的强化学习后训练
tldr: 针对机器人世界模型在长程自回归预测中容易出现误差累积和视觉质量退化的问题，本文提出了一种基于强化学习（RL）的后训练方案。该方法通过在模型自身的预测序列上进行训练，并结合对比RL目标、多视图视觉保真度奖励以及变长未来对比协议，显著提升了多步预测的稳定性。在DROID数据集上的实验表明，该方法在各项指标上均达到SOTA，并获得了极高的人类偏好率。
selection_source: fresh_fetch
motivation: 传统的机器人世界模型在长程自回归部署时，由于误差累积会导致预测画面迅速崩溃，难以支持长时间跨度的任务模拟。
method: 引入一种针对扩散模型的对比强化学习后训练方案，利用多视图视觉保真度奖励对模型自身的预测序列进行优化和偏好对齐。
result: "在DROID数据集上刷新了多步预测保真度的记录，LPIPS降低了14%，SSIM提升了9.1%，且在人类盲测中获得80%的偏好率。"
conclusion: 通过强化学习对世界模型进行自回归稳定性训练，可以有效解决长程视频生成的质量退化问题，为复杂机器人任务模拟提供了更可靠的工具。
---

## 摘要
动作条件化的机器人世界模型在给定机器人动作序列的情况下生成被操纵场景的未来视频帧，为模拟传统物理引擎难以建模的任务提供了一种极具前景的替代方案。然而，这些模型针对短期预测进行了优化，在自回归部署时会失效：每个预测的视频片段都会作为下一个片段的上下文反馈，导致误差累积，视觉质量迅速下降。我们通过以下贡献解决了这一问题。首先，我们引入了一种强化学习（RL）后训练方案，该方案在世界模型自身的自回归展开（rollouts）上而非真实历史数据上进行训练。我们通过将近期针对扩散模型的对比强化学习目标适配到我们的设置中来实现这一点，并证明其收敛性保证可以完全保留。其次，我们设计了一种训练协议，从同一个展开状态生成并比较多个候选的可变长度未来，从而强化高保真度预测并抑制低保真度预测。第三，我们开发了高效的多视图视觉保真度奖励，结合了跨摄像机视图的互补感知指标，并在片段级别进行聚合，以获得密集、低方差的训练信号。第四，我们展示了我们的方法在 DROID 数据集上的展开保真度达到了新的最先进水平，在所有指标上都优于最强的基准模型（例如，外部摄像机的 LPIPS 降低了 14%，手腕摄像机的 SSIM 提高了 9.1%），在成对比较中胜率为 98%，并在盲测人类研究中获得了 80% 的偏好率。

## Abstract
Action-conditioned robot world models generate future video frames of the manipulated scene given a robot action sequence, offering a promising alternative for simulating tasks that are difficult to model with traditional physics engines. However, these models are optimized for short-term prediction and break down when deployed autoregressively: each predicted clip feeds back as context for the next, causing errors to compound and visual quality to rapidly degrade. We address this through the following contributions. First, we introduce a reinforcement learning (RL) post-training scheme that trains the world model on its own autoregressive rollouts rather than on ground-truth histories. We achieve this by adapting a recent contrastive RL objective for diffusion models to our setting and show that its convergence guarantees carry over exactly. Second, we design a training protocol that generates and compares multiple candidate variable-length futures from the same rollout state, reinforcing higher-fidelity predictions over lower-fidelity ones. Third, we develop efficient, multi-view visual fidelity rewards that combine complementary perceptual metrics across camera views and are aggregated at the clip level for dense, low-variance training signal. Fourth, we show that our approach establishes a new state-of-the-art for rollout fidelity on the DROID dataset, outperforming the strongest baseline on all metrics (e.g., LPIPS reduced by 14% on external cameras, SSIM improved by 9.1% on the wrist camera), winning 98% of paired comparisons, and achieving an 80% preference rate in a blind human study.

---

## 论文详细总结（自动生成）

### 论文总结：Persistent Robot World Models (PersistWorld)

#### 1. 核心问题与整体含义（研究动机和背景）
该论文关注的是**机器人动作条件化世界模型（World Models, WM）**在长程预测中的失效问题。
*   **背景**：基于视频扩散的模型（如 Ctrl-World）在生成短期视频片段时表现出色，但在**自回归（Autoregressive）**部署时（即用前一步生成的帧作为下一步的上下文），会遭遇严重的**暴露偏差（Exposure Bias）**。
*   **核心问题**：由于模型在训练时仅见过完美的真实历史数据（Ground Truth），在推理时面对自身生成的带有微小误差的图像，误差会迅速累积，导致生成的物体“溶解”、机器人末端执行器漂移以及场景结构崩溃。
*   **目标**：通过强化学习（RL）后训练，使世界模型在面对自身生成的“不完美”历史时仍能保持长程预测的稳定性。

#### 2. 方法论
论文提出了一种名为 **PersistWorld** 的后训练框架，核心思想是让模型在自己的预测序列上进行学习。
*   **对比 RL 目标（Reward-Contrasted Denoising）**：
    *   适配了 DiffusionNFT 方法，将其从速度预测（v-prediction）推广到 **$x_0$ 预测参数化**。
    *   **公式核心**：构造正向分支 $\hat{x}_0^+$（向当前模型方向外推）和负向分支 $\hat{x}_0^-$（向反方向移动）。通过奖励权重 $r$ 调节损失函数：
        $$L(\theta) = \mathbb{E} [r \|\hat{x}_0^+ - x_0\|_2^2 + (1-r) \|\hat{x}_0^- - x_0\|_2^2]$$
    *   该目标函数在不需要通过扩散去噪链进行反向传播的情况下，实现了策略改进。
*   **变长分支训练协议**：
    *   **共享前缀（Shared Prefix）**：从真值出发，自回归生成长度为 $P$ 的随机前缀（$P \sim \text{Unif}\{0, \dots, 9\}$），模拟不同程度的误差累积。
    *   **候选分支**：从前缀末端独立采样 $K=16$ 个候选后续片段。
*   **多视图视觉奖励**：
    *   结合了 **LPIPS**（感知相似度）、**SSIM**（结构相似度）和 **PSNR**（峰值信噪比）。
    *   对三个摄像头视图（两个外部，一个手腕）的指标进行加权聚合，并进行**组内归一化（Group Normalization）**，将绝对奖励转化为相对优势信号。

#### 3. 实验设计
*   **数据集**：使用大规模机器人操纵数据集 **DROID**，包含多样化的桌面操作任务和三视角相机设置。
*   **Benchmark**：评估 14 步自回归展开（约 11 秒时长）。
*   **对比方法**：
    *   **WPE** (World Models as Environments)
    *   **IRASim** (Interaction-aware Robot Action Simulator)
    *   **Ctrl-World** (作为 Baseline 的原始预训练模型)
*   **评估指标**：除了标准的视觉指标，还引入了基于 **RoboEngine** 的掩码评估，专门测量物体区域和机器人手臂区域的保真度。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H200 GPU**。
*   **时长**：训练耗时 **3 天**。
*   **参数更新**：仅微调 LoRA 适配器（rank=64）和动作编码器，冻结了 U-Net 主干和 VAE。

#### 5. 实验数量与充分性
*   **实验规模**：在 DROID 验证集上进行了大规模测试，成对比较覆盖了约 98% 的样本。
*   **消融实验**：非常详尽，涵盖了学习率、奖励函数组合、前缀采样策略、组大小（Group Size）、KL 正则化、EMA 调度等多个维度。
*   **人类研究**：邀请了 8 位专家进行盲测，共收集了 217 次成对比较。
*   **充分性评价**：实验设计客观且公平，通过掩码评估证明了改进来自于对动态交互的建模而非背景的简单保留，具有很强的说服力。

#### 6. 主要结论与发现
*   **性能飞跃**：PersistWorld 在所有视觉指标上均刷新了 SOTA。外部相机 LPIPS 降低 14%，手腕相机 SSIM 提升 9.1%。
*   **长程稳定性**：在 11 秒的展开中，Baseline 模型通常在几秒内崩溃，而 PersistWorld 能保持物体的几何完整性和机器人的运动一致性。
*   **人类偏好**：在盲测中，人类用户在 80% 的情况下更倾向于 PersistWorld 生成的结果。
*   **策略评估相关性**：证明了该世界模型作为“虚拟评估器”时，其评估结果与真实世界策略表现的相关性更高（Pearson 相关系数 0.822）。

#### 7. 优点
*   **理论严谨**：成功将对比 RL 目标推导至 $x_0$ 预测模型，并给出了收敛性证明。
*   **训练协议高效**：通过随机前缀长度解决了模型对不同误差分布的鲁棒性问题。
*   **任务聚焦**：通过掩码评估和手腕相机指标的显著提升，证明了模型在处理复杂操纵任务（如接触、遮挡）时的优越性。

#### 8. 不足与局限
*   **计算开销**：训练时需要为每个更新步骤采样 $K=16$ 个候选序列，推理/采样成本较高。
*   **物理约束缺失**：目前的奖励函数完全基于像素和感知指标，尚未显式引入物理定律（如重力、碰撞检测）或几何约束。
*   **应用范围**：目前主要在桌面操纵场景验证，尚未测试其在更广阔环境（如移动机器人）中的泛化能力。

（完）
